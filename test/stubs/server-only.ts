// Test stub for the `server-only` package. The real module throws when
// imported outside a React Server Component bundler context; in unit tests we
// import server modules directly under Node, so this no-op stands in.
export {};
