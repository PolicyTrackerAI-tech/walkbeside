# Honest Funeral — Master Launch Checklist

_Generated 2026-06-09 from an 8-dimension codebase sweep. The single source of truth for getting to a real launch (taking real money + emailing real funeral homes). Check items off as you go._

**211 items** · 58 P0 blockers (36 open) · 114 P1 before-launch · 39 P2 soon-after.

_Update 2026-06-09: closed 13 code-side P0s. Batch 1: admin-auth hardening, RLS column hardening (+ the "DELETE policies" item, resolved as deny-by-default), Utah homes importer, env validator, explicit viewport, RLS audit. Batch 2: error-monitoring seam (lib/observability.ts), structured logging on the pay→send path, alerting on failed Stripe + Resend sends, and the signed-off smoke test (docs/SMOKE_TEST.md + `npm run smoke:check`). Batch 3: a vitest suite (`npm test`, 31 tests) closing 5 QA P0s — Stripe fee, OUTREACH_LIVE kill-switch, webhook idempotency, webhook signature verification, and the auth-bypass guards. See the ✅DONE notes inline. Remaining open P0s are overwhelmingly YOU/legal/external-account gates (domain, prod Supabase/Vercel/Stripe-live/Resend, real Utah homes + vetting, the licensing opinion)._

Owner key: **YOU** = founder action · **eng** = code/config (Claude can do) · **legal** = counsel. Effort: S/M/L.

---

## ✅ What's already shipped (the product itself)
The code is in good shape — this checklist is mostly **setup, data, legal, and QA**, not feature work. Already done: the full free-tools site; the upfront pay-to-send outreach flow (one payment mechanic, audited, no email until paid, idempotent + reconciling); congruent pricing copy everywhere; faith + glossary content AI-verified; FD/sister positioning removed; broken links fixed; strategy/legal docs in `docs/`. Build is green.

---

## ⭐ THE CRITICAL PATH (fastest route to a real Utah test launch)
Out of 211 items, **these are the true gates.** Do these and you can take real money + email real homes in Utah. Everything else hardens/scales after.

1. **Stand up production Supabase** — new project (Utah region), run `schema.sql` + all migrations, confirm RLS on every table, create the `price-lists` storage bucket, enable magic-link auth, set Auth URL config to the prod domain. _(eng)_
2. **Domain + DNS live** — `honestfuneral.co` pointed at Vercel. _(YOU)_
3. **Set all prod env vars in Vercel** — Supabase URL/anon/**service-role**, `NEXT_PUBLIC_APP_URL`, **Stripe live key + webhook registered → `STRIPE_WEBHOOK_SECRET`**, `RESEND_API_KEY` + `RESEND_FROM`, `HONEST_FUNERAL_FREE_EMAILS` (your email), `ADMIN_EMAILS`, `UNSUBSCRIBE_SECRET`, `CRON_SECRET`, and **`OUTREACH_LIVE=false`** (flip last). _(eng/YOU)_
4. **Email deliverability** — verify `honestfuneral.co` in Resend; add SPF/DKIM/DMARC at the registrar. Outreach is worthless if it lands in spam. _(eng/YOU)_
5. **Load + vet real Utah funeral homes** into `funeral_homes` (`vetted=true`, valid emails). Until this, the flow shows placeholder homes. **The #1 data gate.** _(YOU — I can write the import script)_
6. **Lawyer clearance** — funeral-broker licensing + FTC/UDAP read before taking money or contacting homes. `docs/LAWYER_BRIEF.md` is ready. _(legal)_
7. **Faith content human/clergy review** via `/admin/faith-qa` — it's public and unverified by a human. _(YOU)_
8. **Smoke-test the whole flow** in Stripe **test mode** with `OUTREACH_LIVE=false` (emails record as `dry_run`), then one real end-to-end with your free-email account + `OUTREACH_LIVE=true` → confirm a real home gets a real email. _(eng/YOU)_

## 🚀 Launch-day switch (in order — do NOT reorder)
1. Confirm steps 1–7 above are complete and the latest `main` is deployed.
2. Smoke test passes in **test mode** (dry-run): start → preview → pay → emails flip `pending`→`dry_run`; pick a home → home "notified"; closed page correct.
3. Swap Stripe **test → live** keys in Vercel; re-register the **live** webhook → update `STRIPE_WEBHOOK_SECRET`.
4. Set **`OUTREACH_LIVE=true`**.
5. Run **one real deal** with your `HONEST_FUNERAL_FREE_EMAILS` account → verify a real Utah home receives a real, correct email (and the reply pipeline works).
6. Open to the first real family. Watch the dashboard + logs. Keep the refund path ready.

---

## 🔴 P0 — BLOCKERS (must clear before launch) — 54 items

### Config, Environment & Infrastructure
- [ ] **Kill-switch: OUTREACH_LIVE = false until ready** — _YOU · S_ ⚠️PARTIAL
  - _Why:_ CRITICAL SAFETY: When OUTREACH_LIVE !== 'true', paid outreach is recorded as dry_run and NO email is sent to funeral homes. Default = 'false'. Flip to 'true' ONLY after: legal clearance, homes vetted, flow smoke-tested.
  - _How:_ .env.example line 52. .env.local already set to 'false'. DO NOT flip to 'true' until lawyer clearance + funeral_homes table fully vetted + smoke test passed. Checked in lib/negotiation/send.ts line 31.
- [ ] **Domain registration and DNS setup** — _YOU · M_
  - _Why:_ honestfuneral.co must resolve to Vercel, have SPF/DKIM/DMARC, MX for Postmark inbound.
  - _How:_ Register at Namecheap, Cloudflare, etc. Add Vercel's DNS records (Vercel Domains tab will guide you). Add Resend domain records (SPF/DKIM). Add Postmark MX records for reply.honestfuneral.co. All must be in place before going live.
- [ ] **Funeral homes directory: populate with real Utah homes (30–60)** — _YOU · L_
  - _Why:_ Without homes in the database, outreach has nowhere to go. MUST be done before OUTREACH_LIVE='true'.
  - _How:_ Sister researches Utah funeral homes, creates CSV with: name, email, phone, address, city, state, zip, google_rating, google_review_count, notes. LAUNCH_PLAYBOOK.md step 2 details format. Import via Supabase Table Editor → funeral_homes → Insert → Import CSV. Each row starts active=true, vetted=false. Sister must review + set vetted=true in /admin/vetting before they're eligible for outreach.
- [ ] **Funeral homes directory: mark homes as vetted=true after review** — _YOU · L_
  - _Why:_ SAFETY GATE: Only vetted homes appear in outreach. Sister must sign off on email, business legit, in launch area.
  - _How:_ Sister uses /admin/vetting (app/admin/vetting folder) or Supabase Table Editor. Set vetted=true, vetted_by='sister_name' for each home reviewed. Once vetted=true AND active=true, home is eligible for outreach.
- [ ] **Supabase project creation and schema deployment** — _eng · M_ ⚠️PARTIAL
  - _Why:_ Core database and authentication. REQUIRED before any feature works. Must run schema.sql + all migrations for tables (negotiations, outreach, messages, etc.) and RLS policies.
  - _How:_ Dev project exists (bhadjvukoyvfbzbcqunp.supabase.co). Production project: Create new Supabase project at supabase.com in Utah region. Run supabase/schema.sql, then ALL migrations in order: 2026-04-25-v2-unlock-and-directory.sql, 2026-04-26-v1.5-quote-items.sql, 2026-05-05-margaret-share-links.sql, 2026-05-08-margaret-paywall.sql, 2026-05-09-margaret-anniversary-checkins.sql, 2026-05-09-margaret-quote-notifications.sql, 2026-05-16-nurture-emails.sql, 2026-05-21-coordinator-messages.sql, 2026-06-03-funeral-home-vetting.sql via SQL Editor.
- [ ] **Supabase: enable magic-link email authentication** — _eng · S_
  - _Why:_ Families sign in via email links (no passwords). Required for user auth flow (/login, /auth/callback).
  - _How:_ Supabase dashboard: Authentication → Providers → Email. Enable Magic Link. Edit templates → 'Magic Link' subject = 'Sign in to Honest Funeral'.
- [ ] **Supabase: set Supabase Auth URL Configuration for production domain** — _eng · S_
  - _Why:_ Magic-link redirects must work in production. Must match NEXT_PUBLIC_APP_URL.
  - _How:_ Supabase dashboard: Project Settings → Authentication → URL Configuration → Site URL = 'https://honestfuneral.co' (after domain live). Redirect URLs = same domain. Dev has 'http://localhost:3000'.
- [ ] **Supabase service-role key rotation and secure storage** — _eng · S_ ⚠️PARTIAL
  - _Why:_ SUPABASE_SERVICE_ROLE_KEY is server-only; used for Stripe webhook, cron jobs, inbound email. Must be kept secret, never in client code.
  - _How:_ Copy from prod Supabase: Project Settings → API → service_role key. Store ONLY in Vercel env vars (never .env.local after go-live). Do NOT commit to repo. Current .env.local has dev key for local testing only.
- [ ] **NEXT_PUBLIC_APP_URL: set to production domain** — _eng · S_
  - _Why:_ Public env var used for Stripe success/cancel URLs, email links, sitemaps, og:url tags. Must be HTTPS.
  - _How:_ Dev = 'http://localhost:3000'. Prod = 'https://honestfuneral.co'. Set in .env.local for local testing, then in Vercel Project Settings → Environment Variables.
- [ ] **NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY: copy to Vercel** — _eng · S_ ⚠️PARTIAL
  - _Why:_ Public keys for Supabase client-side auth + DB reads. Safe to expose (limited by RLS).
  - _How:_ From prod Supabase: Project Settings → API → Project URL + anon key. Add both to Vercel env vars.
- [ ] **Stripe test-mode account setup and key acquisition** — _eng · M_ ⚠️PARTIAL
  - _Why:_ Payment processing for $49 upfront fee. Flat fee stored in lib/stripe.ts line 18 (FLAT_FEE_CENTS = 49_00).
  - _How:_ Current .env.local has sk_test_ key for testing. For production: stripe.com → Activate account (real bank required), then Developers → API keys. Copy sk_live_... key to Vercel (NOT .env.local after launch). LAUNCH_PLAYBOOK.md step 3 has full details.
- [ ] **Stripe webhook endpoint registration for checkout.session.completed** — _eng · S_
  - _Why:_ CRITICAL: When payment succeeds, webhook triggers lib/negotiation/send.ts to email funeral homes. If not registered, outreach silently fails.
  - _How:_ Stripe dashboard: Developers → Webhooks → Add endpoint → URL = 'https://honestfuneral.co/api/stripe/webhook' → Listen for 'checkout.session.completed' only → Copy signing secret (whsec_...) to STRIPE_WEBHOOK_SECRET env var in Vercel. Current .env.local has test secret. UPDATE this after domain goes live.
- [ ] **STRIPE_SECRET_KEY and STRIPE_WEBHOOK_SECRET in Vercel** — _eng · S_
  - _Why:_ Server env vars required by app/api/stripe/webhook and app/api/stripe/checkout.
  - _How:_ Vercel Project Settings → Environment Variables. Add both. Never commit to .env.local after launch.
- [ ] **Resend account creation and API key** — _eng · M_ ⚠️PARTIAL
  - _Why:_ Sends email to funeral homes + families. Without this, outreach is broken.
  - _How:_ resend.com → sign up. API Keys → Create API key 'production'. Copy to RESEND_API_KEY. Current .env.local has test key (re_4poe...) for dev; prod needs re_live_... key.
- [ ] **Resend domain verification (honestfuneral.co)** — _eng · M_
  - _Why:_ Must verify domain ownership before sending from @honestfuneral.co. Adds SPF/DKIM/DMARC records at registrar.
  - _How:_ Resend dashboard: Domains → Add 'honestfuneral.co'. Resend generates DNS records. Add them at domain registrar (Namecheap, Cloudflare, etc.). Verification takes 5–60 min. LAUNCH_PLAYBOOK.md step 4 details this.
- [ ] **RESEND_FROM: set to verified domain mailbox** — _eng · S_
  - _Why:_ Outbound emails use this From address. Must be on honestfuneral.co (or whatever domain you verify).
  - _How:_ Set to 'Honest Funeral Advocacy <advocate@honestfuneral.co>' (or hello@ or arrangements@, your choice). Defaults to 'Honest Funeral <hello@honestfuneral.co>' per lib/email.ts line 13. Add to Vercel env vars. Current .env.local has dev value (onboarding@resend.dev).
- [ ] **ANTHROPIC_API_KEY: acquire and set** — _eng · S_ ⚠️PARTIAL
  - _Why:_ Claude API for negotiation emails (lib/negotiation/prompts.ts), obituary drafting, price-list analysis. Optional but highly recommended for quality outreach.
  - _How:_ console.anthropic.com → API keys → Create key. Add $5–10 credit. Copy to Vercel. Current .env.local has dev key. Without this, text-based fallbacks run (less personalized).
- [ ] **Vercel project creation and GitHub integration** — _eng · M_
  - _Why:_ Deployment platform. Auto-deploys main branch, manages env vars, handles HTTPS + custom domain.
  - _How:_ vercel.com → New Project → import GitHub repo. Auto-detects Next.js 16. Add all env vars in Project Settings → Environment Variables (see other tasks). Deploy.
- [ ] **Vercel: add all environment variables from .env.example** — _eng · M_
  - _Why:_ Production env vars are set in Vercel, not .env.local. Keeps secrets out of repo.
  - _How:_ Project Settings → Environment Variables. Add all 15+ vars: NEXT_PUBLIC_*, SUPABASE_*, STRIPE_*, RESEND_*, ANTHROPIC_*, POSTMARK_*, CRON_SECRET, UNSUBSCRIBE_SECRET, ADMIN_EMAILS, etc. Keep .env.local only for local dev.
- [ ] **Vercel custom domain + SSL** — _eng · M_
  - _Why:_ HTTPS only. Families see https://honestfuneral.co. Vercel auto-provisions Let's Encrypt cert.
  - _How:_ Vercel Dashboard → Project → Domains → Add → enter honestfuneral.co → Follow DNS record instructions → Vercel issues free SSL cert automatically (usually within 5 min).
- [x] **Supabase RLS verification: all tables have policies enabled** — _eng · S_ ✅DONE 2026-06-09 (audited every table; all RLS-on with intended policies; column-level read hardening added — migration 2026-06-09-rls-hardening.sql)
  - _Why:_ Row-level security prevents users from accessing others' data. Must be enabled on all user-facing tables.
  - _How:_ Check each table: profiles (line 18), tasks (line 60), negotiations (line 87), negotiation_outreach (line 109), price_list_analyses (line 130), cert_trackers (line 148), obituaries (line 165), planning_signups (line 185), negotiation_messages (from migration). All use 'alter table ... enable row level security' + 'drop policy' + 'create policy' pattern. Schema is idempotent; re-running it re-applies all policies.
- [ ] **Supabase: funeral_homes table must have vetted=false gate + active flag** — _eng · S_ ⚠️PARTIAL
  - _Why:_ SAFETY: Prevents contacting unvetted funeral homes. Migration 2026-06-03 adds vetted flag. findHomesFromDirectory() in lib/negotiation/directory.ts requires both active=true AND vetted=true.
  - _How:_ Migration 2026-06-03-funeral-home-vetting.sql adds vetted boolean, vetted_at, vetted_by columns. All existing rows start vetted=false. Sister reviews each home in /admin/vetting before any can be contacted.
- [x] **Smoke test: full outreach flow end-to-end** — _eng · M_ ✅DONE 2026-06-09 (signed-off runbook docs/SMOKE_TEST.md + headless preflight `npm run smoke:check` → scripts/smoke-check.mjs. Still a YOU action to actually RUN it on prod before flipping the switch.)
  - _Why:_ Verify: payment → Stripe webhook → email send → Resend webhook (bounce/complaint) → family notification. All systems must chain correctly.
  - _How:_ 1. Set HONEST_FUNERAL_FREE_EMAILS to test user. 2. Run negotiation from /negotiate/start, add 2–3 homes. 3. Pay $49 (test card 4242...). 4. Verify: Stripe webhook logs 'sendOutreach', funeral_homes get dry_run entries (OUTREACH_LIVE=false). 5. Flip OUTREACH_LIVE=true in Vercel. 6. Repeat payment. 7. Receive test email at funeral home inbox. 8. Verify outreach.status='sent', initial_email_id set. 9. Hard-bounce test email → Resend webhook fires → funeral_homes.active flips false. 10. Family gets reply notification email. Document test results.

### Data & Content
- [ ] **Sister reviews + approves all faith tradition profiles in /admin/faith-qa** — _YOU · M_
  - _Why:_ FAITH_REVIEW_FINDINGS.md documents 15 needs-nuance items and 3 verified errors in faith-traditions.ts. Key reversible errors: (a) Hindu dispositionNorm incorrectly 'cremation-required' (should be 'cremation-preferred' — burial used for ascetics/sadhus), (b) Orthodox embalming marked 'common' (should be 'uncommon'/'discouraged' — varies by community, some forbid), (c) Sikh disposition marked 'cremation-required' (should be 'cremation-preferred' — standard but not absolute). These affect family guidance on a deeply personal choice.
  - _How:_ /Users/ryancurrie/FH/app/admin/faith-qa/FaithQAReview.tsx provides interface to review + correct each claim. Source: /Users/ryancurrie/FH/docs/FAITH_REVIEW_FINDINGS.md (rows 24–62). Every claim tagged // TODO-FD: in /Users/ryancurrie/FH/lib/faith-traditions.ts (19 instances). Task: (1) open /admin/faith-qa?key=[ADMIN_PREVIEW_KEY], (2) iterate each tradition (secular, atheist, 9 major + 6 sub-profiles), (3) review the 15 nuance items in the findings doc, (4) apply corrections to lib/faith-traditions.ts, (5) mark as reviewed. Do not ship without this — affects /decide flow recommendation engine and per-faith /faith/[tradition] pages.
- [ ] **Sister verifies glossary definitions against FTC/NFDA sources** — _YOU · M_
  - _Why:_ GLOSSARY_REVIEW_FINDINGS.md identifies 2 verified errors and 19 needs-nuance items in lib/glossary.ts (64 terms, 118 claims checked). Errors: (1) 'direct cremation' overstates FTC mandate (homes not required to *offer* direct cremation, only must price it separately *if* offered), (2) cremated remains weight inconsistent (entry says 'varies' but cremation entry expects 4–8 lbs). Nuance items include: basic-services-fee description, funeral insurance premiums, death doula fees, grave-liner vs vault pricing, state counts (coroner vs medical examiner distribution).
  - _How:_ Source: /Users/ryancurrie/FH/docs/GLOSSARY_REVIEW_FINDINGS.md (rows 21–39 for critical errors, rows 44–57 for per-category summaries). File: /Users/ryancurrie/FH/lib/glossary.ts (64 entries). Task: (1) go through each of the 19 needs-nuance items, (2) test examples against current FTC regulations (16 CFR Part 453), NFDA data, state vital-records offices, (3) update $$ figures to 2026 market data where noted, (4) fix the two errors (direct cremation FTC claim, remains weight), (5) add nuance to denomination language where applicable. Suggested time: 4–6 hours of source verification.
- [x] **Import + validate Utah funeral homes for launch city** — _eng · M_ ✅DONE 2026-06-09 (importer shipped: `npm run import:homes` → scripts/import-funeral-homes.mjs, validating/dedup/never-vetted, with --parse-only/--dry-run; docs in supabase/seed/IMPORT_HOMES.md. Still needs the real Utah CSV — see the YOU "populate with real Utah homes" item.)
  - _Why:_ funeral_homes table is currently empty. Directory lookup falls back to placeholder sample homes. Without real home data, the entire outreach flow is unusable even after OUTREACH_LIVE flips to true.
  - _How:_ /Users/ryancurrie/FH/lib/negotiation/directory.ts requires vetted=true + email before ANY home is contacted. Table structure defined in /Users/ryancurrie/FH/supabase/migrations/2026-04-25-v2-unlock-and-directory.sql. Task: (1) source Utah funeral home list (NFDA, state licensing board, Google Maps API), (2) ETL rows to public.funeral_homes (name, email, phone, address, city, state=UT, zip, google_rating, google_review_count, notes, active=true, vetted=false), (3) import into Supabase, (4) test directory lookup at /api/negotiate/start?zip=84101 returns rows, (5) do not set vetted=true until Sarah vets each row in /admin/vetting?key=[ADMIN_PREVIEW_KEY].

### Legal & Compliance
- [ ] **Prepare lawyer brief + supporting materials for counsel engagement** — _YOU · S_
  - _Why:_ LAWYER_BRIEF §7 lists materials to bring to counsel meeting. Must assemble: (1) the brief itself; (2) live site URLs (esp. /how-it-works, /faq, /terms, /privacy, /our-role); (3) outreach email template; (4) Stripe product description; (5) Supabase schema summary; (6) LLC docs + EIN.
  - _How:_ File: docs/LAWYER_BRIEF.md (already prepared, excellent baseline). Action: (1) schedule outside counsel (startup/consumer/FTC specialist); (2) send LAWYER_BRIEF in advance; (3) prepare email-body.ts snippet for review; (4) have live staging URL or Vercel preview ready; (5) compile questions in LAWYER_BRIEF §8 as discussion agenda. Target: 2–3 week engagement for licensing opinion + compliance review.
- [ ] **Verify OUTREACH_LIVE kill-switch is set to false in production and gated behind launch checklist** — _eng · S_ ⚠️PARTIAL
  - _Why:_ OUTREACH_LIVE env var controls whether funeral homes are actually emailed (true) or records are marked dry_run (false). Currently default is false (safe). Must ensure: (a) stays false in deployed environment until legal + data readiness are complete; (b) manual checklist before anyone flips to true; (c) audit trail of when/why it was changed.
  - _How:_ Files: .env.example (line 52, default false), lib/negotiation/send.ts (line 31, kill-switch check). Action: (1) confirm .env.local has OUTREACH_LIVE=false; (2) add pre-launch checklist in LAUNCH_PLAYBOOK or ops doc: 'Legal cleared? Data ready? Homes loaded?' before flipping to true; (3) consider adding toggle in /admin/* or Vercel env-var UI with change log. Code already has strong belt-and-suspenders protection (denylist + dry_run + webhook gate); this is procedural enforcement.
- [ ] **Obtain multi-state funeral licensing legal opinion: does advocate outreach require funeral/broker/similar license in Utah and other launch states?** — _legal · L_
  - _Why:_ This is the pre-launch gating question (LAWYER_BRIEF §B, §6). Contacting funeral homes on behalf of families as a paid advocate may trigger state funeral licensing, broker licensing, or similar regulations. Without this clear, the entire paid outreach model is at legal risk. No launch until counsel confirms scope of licensing requirements by state.
  - _How:_ File: docs/LAWYER_BRIEF.md §B. Assign to outside counsel specializing in funeral/consumer/state regulation. Must cover: (a) Utah specifically (launch state); (b) whether outreach = arranging/negotiating funerals requiring license; (c) whether can be gated by state. Law varies significantly by state.
- [ ] **Counsel review of outreach email template for FTC/impersonation compliance** — _legal · M_
  - _Why:_ The outreach email is the single most important representation we make (LAWYER_BRIEF §4.1). It must be transparent, non-impersonating, and legally defensible. Email template is deterministic (not AI-generated), which is strong, but counsel must verify: (a) does identifying ourselves as 'the family's advocate' + FTC Funeral Rule reference + static template hold up under FTC §5 and state deception laws? (b) any CAN-SPAM issues for B2B outreach to funeral homes?
  - _How:_ File: lib/negotiation/email-body.ts (buildOutreachEmail function, lines 37–56). Counsel reads the actual template and confirms: (1) no impersonation risk; (2) FTC Funeral Rule invocation is legally sound; (3) CAN-SPAM headers/opt-out (already included via funeralHomeOptOutUrl); (4) any anti-deception gaps.

### Security, Auth & Data Protection
- [ ] **Document and enforce OUTREACH_LIVE kill switch review process** — _YOU · S_
  - _Why:_ OUTREACH_LIVE=true is the gate for actually emailing funeral homes. If accidentally flipped to true in production before homes are vetted, real outreach could go to wrong/unvetted addresses. No confirmation prompt.
  - _How:_ Checklist before flipping OUTREACH_LIVE to true: (1) funeral_homes table is fully vetted (vetted=true) and curated for launch state. (2) at least 20 homes approved in the launch state. (3) Stripe webhook secret is set. (4) Resend webhook secret is set. (5) Email domain verified in Resend. (6) CRON_SECRET set in Vercel. Document in README.md or LAUNCH_CHECKLIST.md.
- [ ] **Enforce ADMIN_EMAILS allowlist is set before launch** — _YOU · S_
  - _Why:_ isAdminEmail() is 'open by default': if ADMIN_EMAILS is unset, any logged-in user is treated as admin (lib/admin.ts line 34). This is intentional for dev but must be locked down in production.
  - _How:_ Before deploying to production, set ADMIN_EMAILS in Vercel env to the founder's email(s) only. Verify adminAllowlistConfigured() returns true. Add a warning banner to /admin/* pages showing 'ADMIN_EMAILS not configured — any logged-in user can access!' if not set.
- [x] **Add DELETE policies to funeral_homes and negotiation_messages RLS** — _eng · S_ ✅DONE 2026-06-09 (RESOLVED, not as written: both tables are correctly deny-by-default for writes — no code deletes via a user client — so adding permissive DELETE policies would be a regression. Real fix shipped instead: column-level read hardening on funeral_homes so internal notes/vetted_by/vetted_at aren't world-readable via the anon key. Migration 2026-06-09-rls-hardening.sql documents the decision.)
  - _Why:_ Both tables have RLS enabled but lack DELETE policies. Incomplete RLS exposes users to unauthorized deletes (funeral_homes can delete via service role, negotiation_messages can be deleted). This blocks audit trails and data recovery.
  - _How:_ funeral_homes: add policy 'funeral_homes_no_delete' (only service role); negotiation_messages: add policy 'negotiation_messages_no_delete' (only service role). Edit /Users/ryancurrie/FH/supabase/migrations/2026-04-25-v2-unlock-and-directory.sql and /Users/ryancurrie/FH/supabase/migrations/2026-05-21-coordinator-messages.sql or create new migration.
- [x] **Replace ADMIN_PREVIEW_KEY query-param gating with timing-safe Bearer token in header** — _eng · M_ ✅DONE 2026-06-09 (went further than a bearer token: removed the shared secret entirely. /admin/* now requires a logged-in Supabase session on the ADMIN_EMAILS allowlist via lib/admin-auth.ts — requireAdminPage/requireAdminApi. All 4 pages + 3 API routes converted; ADMIN_EMAILS is enforced by the env validator when OUTREACH_LIVE=true.)
  - _Why:_ Query parameters are logged in server logs, browser history, and referrer headers. The string comparison (!== operator) is not timing-safe. An attacker sniffing logs or with access to history could extract the key. All admin routes (/admin/vetting, /admin/faith-qa, /admin/outreach-preview, /admin/messages, /api/admin/funeral-homes, /api/negotiate/preview) must move to Authorization header with crypto.timingSafeEqual.
  - _How:_ Files: /Users/ryancurrie/FH/app/admin/*/page.tsx, /Users/ryancurrie/FH/app/api/admin/funeral-homes/route.ts, /Users/ryancurrie/FH/app/api/negotiate/preview/route.ts. Change query-param checks to header checks. Use crypto.timingSafeEqual for comparison (like /Users/ryancurrie/FH/lib/nurture-email.ts line 143).
- [x] **Add authentication bypass test (smoke test) before launch** — _eng · M_ ✅DONE 2026-06-09 (vitest: lib/__tests__/admin-auth.test.ts + admin.test.ts cover requireAdminApi 401/403/pass, requireAdminPage redirect/notFound/pass, and the isAdminEmail allowlist. `npm test`.)
  - _Why:_ Critical flows (negotiate start → payment → outreach → results → choose home) rely on user_id matching in RLS. If RLS is misconfigured, users could see or modify each other's negotiations.
  - _How:_ Create a test in /Users/ryancurrie/FH/e2e or /Users/ryancurrie/FH/tests: (1) Create negotiation as user A. (2) Try to read/update negotiation as user B (should fail). (3) Try to mark quote as received for user A's negotiation as user B (should fail). (4) Verify RLS blocks cross-user access via Supabase client.

### Observability, Ops & Support
- [ ] **Document and test the outreach dry-run/kill-switch flow** — _YOU · S_ ⚠️PARTIAL
  - _Why:_ OUTREACH_LIVE env var is the master gate. When false, sends are recorded as dry_run instead of emailing homes. Must be thoroughly tested before flipping to true, and the behavior must be clearly documented for the team.
  - _How:_ Already implemented in lib/negotiation/send.ts. Document in docs/OUTREACH_TESTING.md: (1) default state OUTREACH_LIVE=false (safe); (2) when false: payments are processed, outreach rows created but set status=dry_run, NO email sent; (3) test checklist: create negotiation → checkout → stripe webhook fires → confirm negotiation.status='contacting' but negotiation_outreach rows show status='dry_run'; (4) HONEST_FUNERAL_FREE_EMAILS bypass is separate (founder/QA accounts). Flip OUTREACH_LIVE=true ONLY after legal cleared + 10 homes vetted.
- [ ] **Verify funeral_homes table is populated and vetted before launch** — _YOU · M_ ⚠️PARTIAL
  - _Why:_ Without real funeral homes in the directory, outreach goes to dead-end test data. The vetting migration (2026-06-03) requires vetted=true before any outreach, default is false, so no launch-city homes are eligible yet.
  - _How:_ Per LAUNCH_PLAYBOOK section 2: Sister must build CSV of 30-60 homes in launch city, import to funeral_homes table. Before payment goes live: (1) Verify count: SELECT COUNT(*) FROM funeral_homes WHERE state='UT' AND vetted=true (should match imported count); (2) Sister reviews each row in /admin/vetting (if exists) or Supabase Table Editor, marks vetted=true; (3) Spot-check emails are real (avoid deceased-home.com, test@test.com, etc). Outreach cannot happen without this step — it's a hard gate in findHomesFromDirectory().
- [x] **Install Sentry error monitoring** — _eng · M_ ✅DONE 2026-06-09 (chose a vendor-agnostic seam over adding the SDK: lib/observability.ts captureError() logs structured errors today + has a documented one-line Sentry drop-in. To use hosted Sentry: `npm i @sentry/nextjs`, set SENTRY_DSN, uncomment the seam line.)
  - _Why:_ Payment flows (Stripe webhook, checkout) and outreach sends (Resend) must be instrumented so failures are captured with full context. Currently errors are only visible in Vercel logs if someone manually checks.
  - _How:_ Install @sentry/nextjs, initialize in next.config.ts, set SENTRY_DSN in .env.example and Vercel. Enable for both client and server. Start with error monitoring + session replay on /negotiate/* paths. See: https://docs.sentry.io/platforms/javascript/guides/nextjs/
- [x] **Implement structured logging for critical paths** — _eng · M_ ✅DONE 2026-06-09 (lib/observability.ts logEvent/logWarn — JSON lines wired through send.ts, stripe webhook+checkout, negotiate reconciliation, resend-webhook.)
  - _Why:_ Currently using console.log/warn/error scattered across 11 locations in API routes (stripe/webhook, inbound/email, inbound/resend-webhook, planning/signup, cron routes, negotiate/choose). No correlation IDs, no request IDs for tracking a single transaction end-to-end.
  - _How:_ Create lib/logging.ts with structuredLog(level, context, message) that includes timestamp, request ID, user ID, negotiation ID. Call on: stripe webhook received → sent, payment checkout initiated → confirmed, negotiation outreach prepared → sent, inbound email received → stored, Resend bounce handled. Use Vercel's built-in log aggregation or pipe to external service (Datadog/LogRocket/etc).
- [x] **Add alerting on failed Stripe payments and webhook deliveries** — _eng · M_ ✅DONE 2026-06-09 (stripe/webhook captureError+alert when a paid send throws and returns 500 so Stripe retries; needs ALERT_WEBHOOK_URL set for push, else logged.)
  - _Why:_ Payment success/failure is silent in the current setup. A failed webhook delivery falls back to status-page reconciliation, but if both fail, the negotiation gets stuck in pending_payment forever with no alert.
  - _How:_ Use Stripe's built-in webhook retry + event status dashboard (visit dashboard, check for failed events). Configure Vercel alerting on 5XX responses from /api/stripe/webhook. Add a daily cron check that queries DB for negotiations stuck in pending_payment > 1 day with a paid Stripe session, surfaces as alert. Consider Vercel Error Tracking or PagerDuty if Stripe alerts alone insufficient.
- [x] **Add alerting on failed Resend sends (outreach emails)** — _eng · M_ ✅DONE 2026-06-09 (send.ts fires one aggregate alert on per-email send failures; resend-webhook alerts on bounce/complaint deactivations.)
  - _Why:_ Funeral home outreach emails are the core transaction. If sendEmail() throws in lib/negotiation/send.ts, the exception is caught silently and the row stays pending. No one knows a home wasn't contacted.
  - _How:_ sendOutreachForNegotiation() returns {sent, dryRun, skipped}. After calling it in stripe/webhook and stripe/checkout, check if sent + dryRun < expected rows. If discrepancy, log error + send alert. Add Resend webhook for email.failed event (in addition to existing bounced/complained). Set RESEND_WEBHOOK_SECRET in .env.example.
- [ ] **Verify Stripe webhook is registered and monitored** — _eng · S_ ⚠️PARTIAL
  - _Why:_ Critical path: Stripe checkout.session.completed → sends outreach. But no confirmation that the webhook endpoint was created during setup, and Stripe Dashboard doesn't alert if endpoint is 500ing.
  - _How:_ .env.example documents STRIPE_WEBHOOK_SECRET setup (create endpoint → /api/stripe/webhook listening for checkout.session.completed). Before launch: (1) verify webhook endpoint exists in Stripe Dashboard → Developers → Webhooks; (2) confirm status shows 'Enabled'; (3) check recent event log for successful deliveries; (4) test by creating a test checkout session in Stripe Dashboard or via CLI (`stripe listen --forward-to localhost:3000/api/stripe/webhook`). No code change needed if already done.

### SEO, Performance, Accessibility & Mobile
- [ ] **Create a launch checklist document listing all SEO/mobile/a11y sign-offs required before go-live to real users** — _YOU · S_
  - _Why:_ Launch-readiness requires verification that all metadata, schemas, robots rules, and accessibility standards are met. No pre-launch checklist documented, risking missed items in final sprint.
  - _How:_ Create /docs/LAUNCH_CHECKLIST_SEOPA.md (or add to existing LAUNCH_CHECKLIST). Include: ✓ Viewport meta tag, ✓ Charset meta, ✓ All page metadata (title, description, canonical), ✓ OG images on social-shareable routes, ✓ Noindex on auth pages, ✓ Article/FAQ schema on content pages, ✓ robots.txt blocks private pages, ✓ Sitemap includes all public routes, ✓ Core Web Vitals baseline <2.5s LCP, ✓ Color contrast audit (4.5:1), ✓ Form accessibility (aria-label, labels), ✓ Mobile responsive at 320px, 375px, 768px, 1024px, ✓ Print styles tested on /prep and /worksheet, ✓ Reduced-motion respected. Owner: Ryan to sign off before production Vercel deploy.
- [x] **Add viewport and charset meta tags to root layout.tsx metadata export** — _eng · S_ ✅DONE 2026-06-09 (explicit Next 16 `export const viewport` in app/layout.tsx; charset=utf-8 is injected by Next automatically.)
  - _Why:_ Viewport meta tag (width=device-width, initial-scale=1) is mandatory for mobile indexing and responsive design; charset ensures proper text encoding for all characters used (especially entities in pricing/obituary content). Google heavily penalizes missing viewport in mobile-first index.
  - _How:_ File: /Users/ryancurrie/FH/app/layout.tsx. Add to Metadata export: viewport: { width: 'device-width', initialScale: 1, viewportFit: 'cover' }, and implicit charset (Next.js 16 sets UTF-8 by default, but add explicit if needed). Verify in browser DevTools: <meta name="viewport"> and <meta charset> appear in <head>.

### QA, Testing & Edge Cases
- [x] **End-to-end upfront-pay flow smoke test (Stripe test mode)** — _eng · M_ ✅DONE 2026-06-09 (Phase 1 of docs/SMOKE_TEST.md covers the test-mode dry-run path; run it before launch.)
  - _Why:_ Core business model: $49 payment before any funeral home is contacted is the only revenue stream and the family trust mechanism. Must verify: auth → find homes → pay via Stripe → webhook fires → outreach sends. Complete failure mode.
  - _How:_ Path: /login → /negotiate/start (fill form) → /negotiate/[id]/preview (shows home count, blurred names, $49 fee) → 'Pay now' form POST to /api/stripe/checkout → Stripe Checkout session → success_url redirects to /negotiate/[id]/status?session_id={id}. Use Stripe test card 4242 4242 4242 4242. Verify: (1) negotiation status moves from 'pending_payment' → 'contacting'; (2) negotiation_outreach rows exist with status='pending' after /api/negotiate/start, then status='sent' (or 'dry_run' if OUTREACH_LIVE=false) after payment; (3) logs in Vercel show '[webhook] sendOutreach' or status page reconciliation message; (4) no emails sent if OUTREACH_LIVE=false; (5) stripe_payment_intent_id and fee_cents stored in negotiations table.
- [x] **Verify OUTREACH_LIVE kill-switch (dry_run mode vs live mode)** — _eng · M_ ✅DONE 2026-06-09 (vitest: send.test.ts asserts no email sent + rows flip dry_run when off, and sent when on. `npm test`.)
  - _Why:_ Safety gate: before launch, OUTREACH_LIVE must be false in prod (default in .env.example). Any paying family during testing should trigger dry_run records, not real emails. This is the only thing between QA and emailing real funeral homes.
  - _How:_ Set OUTREACH_LIVE=false (default) in .env.local. Run end-to-end flow. After payment, check negotiation_outreach.status in Supabase: should be 'dry_run' (not 'sent'). Check Resend logs (or console if RESEND_API_KEY not set): zero emails sent. Then set OUTREACH_LIVE=true, re-run with test account (use HONEST_FUNERAL_FREE_EMAILS to avoid charging). Verify negotiation_outreach.status='sent' and initial_email_id is populated (returned from Resend). Flip back to OUTREACH_LIVE=false before actual launch until fully ready.
- [x] **Webhook idempotency (double-fire, retries, status-page reconciliation)** — _eng · M_ ✅DONE 2026-06-09 (vitest: send.test.ts proves re-running with no pending rows sends nothing (idempotent — same fn the webhook + reconciliation both call); webhook.test.ts proves a send failure returns 500 so Stripe safely retries.)
  - _Why:_ Stripe retries webhooks if they don't 200; status page polls /api/negotiate/[id] for self-healing. Both must not double-send. sendOutreachForNegotiation is idempotent (only 'pending' rows sent, moves to 'contacting'), but verify it works.
  - _How:_ Run full flow; payment completes. Manually re-trigger the Stripe webhook from Stripe Dashboard (Developers → Events → pick the checkout.session.completed → Resend). Verify in logs: 'sent=0' (idempotent, already sent). Also test: webhook signature invalid (strip STRIPE_WEBHOOK_SECRET, change sig header) → 400 bad_sig. Test missing webhook secret env var → error thrown at runtime (requireServer throws). Test status-page polling: after payment, visit /negotiate/[id]/status?session_id=... → page polls GET /api/negotiate/[id] every 6 seconds. First poll should trigger reconciliation if webhook is delayed, second poll should find status='contacting' and not re-send.
- [x] **Stripe webhook signature verification (missing/invalid signature)** — _eng · S_ ✅DONE 2026-06-09 (vitest: webhook.test.ts asserts 400 on missing signature and 400 (not 500) on invalid signature, with no send.)
  - _Why:_ Webhook is the primary trigger for outreach. Unsigned or tampered requests must be rejected (400 bad_sig).
  - _How:_ POST /api/stripe/webhook with: (1) no stripe-signature header → 400 no_signature; (2) invalid signature (wrong STRIPE_WEBHOOK_SECRET or tampered body) → 400 bad_sig; (3) missing STRIPE_WEBHOOK_SECRET env var → requireServer throws, which should be caught somewhere (verify it's not a 500 ISE); (4) valid signature, valid checkout.session.completed event → 200 received. Local testing: stripe listen --forward-to localhost:3000/api/stripe/webhook auto-generates a valid whsec_ for local dev.
- [x] **Stripe fee amount verification ($49 = 4900 cents)** — _eng · S_ ✅DONE 2026-06-09 (vitest: stripe.test.ts asserts FLAT_FEE_CENTS===4900, calcFeeCents()===4900, fmtCents(4900)==="$49".)
  - _Why:_ Price is hard-coded in lib/stripe.ts. Verify amount shown to family, charged, and stored in DB matches spec.
  - _How:_ Check: (1) FLAT_FEE_CENTS = 49_00 (i.e., 4900 cents = $49.00) in /lib/stripe.ts; (2) Preview page shows correct formatted amount using fmtCents(); (3) Stripe Checkout line_item.price_data.unit_amount = fee (4900); (4) negotiation.fee_cents = 4900 after checkout; (5) Stripe test dashboard shows $49.00 charge (not $0.49 or $4900). If the amount ever changes, edit FLAT_FEE_CENTS and redeploy (no Stripe product/price objects to update).
- [x] **Environment variable completeness (all required vars documented and validated)** — _eng · S_ ✅DONE 2026-06-09 (lib/env.ts validateEnv/assertEnvAtBoot + instrumentation.ts: warns on missing core vars in dev, hard-errors at boot only when OUTREACH_LIVE=true — listing every missing send-path secret + sk_test_/sandbox-sender footguns. .env.example documents all vars.)
  - _Why:_ Missing env vars (e.g., SUPABASE_SERVICE_ROLE_KEY) cause runtime errors. .env.example must be complete and validated at startup.
  - _How:_ Verify: (1) .env.example documents all 15+ vars (check LAUNCH_PLAYBOOK.md vs .env.example); (2) requireServer() throws clear error if a required var is missing (e.g., 'Missing env var STRIPE_WEBHOOK_SECRET. Set it in .env.local before using this feature.'); (3) Startup validation: do we call requireServer() for all critical paths, or only on-demand? (If on-demand, first webhook might fail if STRIPE_WEBHOOK_SECRET is missing, rather than failing at boot.) (4) Vercel production config: all vars in Environment Variables tab, and Redeploy required if any are added. (5) Missing ANTHROPIC_API_KEY should not crash (FEATURES.claude() returns false, outreach emails use fallback); missing RESEND_API_KEY should log to console in dev.
- [x] **Launch day manual test script (signed-off checklist)** — _eng · M_ ✅DONE 2026-06-09 (docs/SMOKE_TEST.md with checkboxes + go/no-go.)
  - _Why:_ Before flipping OUTREACH_LIVE=true and taking real payments, a human must run the full flow once on prod and sign off.
  - _How:_ Create LAUNCH_TEST_SCRIPT.md: Step-by-step instructions to run on Vercel prod before launch. Include: (1) Log in with founder email (in HONEST_FUNERAL_FREE_EMAILS, or bypass with real card if preferred); (2) /negotiate/start → fill form, choose 3–5 mile radius, hit Next; (3) /negotiate/[id]/preview → verify homes shown blurred (or unblurred if already paid), see $49 fee, hit 'Pay now'; (4) Stripe Checkout → use 4242 4242 4242 4242 (test card, succeeds); (5) Redirect to status page → verify status='contacting', negotiation_outreach.status='pending' (dry_run if OUTREACH_LIVE=false); (6) Webhook should have fired (check Vercel Logs → stripe/webhook endpoint); (7) Go to /dashboard → negotiation shows in list; (8) Verify no errors in Vercel dashboard Logs; (9) Review Supabase negotiation and negotiation_outreach rows; (10) Sister reviews one dry_run email (check Resend logs or console), ensures format/tone are OK; (11) Check: if all above pass, flip OUTREACH_LIVE=true, redeploy, and re-run once more with a real payment to verify live outreach works. Requires founder + sister sign-off.
- [ ] **Pricing and cost messaging clarity (upfront $49, 14-day refund, no hidden fees)** — _eng · S_
  - _Why:_ Business model is upfront payment + refundable in 14 days. Copy must be unambiguous everywhere.
  - _How:_ Check every instance of pricing text: (1) /negotiate/[id]/preview shows '$49 flat fee' + refund language; (2) Stripe Checkout line_item.product_data.description mentions 'Refundable within 14 days if we don't save you anything'; (3) Email to family (buildOutreachEmail) does NOT mention fee (family is aware from preview); (4) Email to funeral homes does NOT mention fee; (5) /dashboard or help section clearly states 'This service is free except for the $49 outreach fee, which is refundable'; (6) No surprise fees on closed page. Verify copy is kind, clear, and legal (lawyer to review for FTC compliance).
- [ ] **Vercel deployment and environment variable inheritance** — _eng · S_
  - _Why:_ Prod env vars must be set in Vercel Project Settings, not .env.local. Redeploy required after env var changes.
  - _How:_ Verify: (1) All 15+ env vars from .env.example are in Vercel Project Settings → Environment Variables; (2) Prod values (STRIPE_SECRET_KEY, RESEND_API_KEY, ANTHROPIC_API_KEY, etc.) point to production accounts, not test keys; (3) OUTREACH_LIVE is set to 'false' before launch; (4) After adding a new env var to Vercel, Redeploy is required (can't just expect it to pick up on next push); (5) Check: NEXT_PUBLIC_* vars are visible to client (Vercel exposes these), secret vars are not (STRIPE_SECRET_KEY, SUPABASE_SERVICE_ROLE_KEY, etc.).
- [ ] **Stripe webhook endpoint registration and secret storage** — _eng · S_
  - _Why:_ Webhook is the critical outreach trigger. If endpoint is not registered in Stripe Dashboard, webhook will not fire.
  - _How:_ Checklist: (1) Stripe Dashboard (production or test mode) → Developers → Webhooks → 'Add endpoint'; (2) URL: https://honestfuneral.co/api/stripe/webhook (prod URL, HTTPS required); (3) Events: select 'checkout.session.completed' only; (4) Copy the signing secret (whsec_...) → paste into Vercel Environment Variables as STRIPE_WEBHOOK_SECRET; (5) Redeploy Vercel; (6) In Stripe Dashboard, click the endpoint and 'Send test event' → verify /api/stripe/webhook returns 200; (7) For local dev: stripe listen --forward-to localhost:3000/api/stripe/webhook (auto-generates whsec_ for local use, set in .env.local).


## 🟠 P1 — Before launch — 113 items

### Config, Environment & Infrastructure
- [ ] **ADMIN_EMAILS: set to comma-separated list of admin usernames** — _YOU · S_
  - _Why:_ Gates access to /admin/* pages (e.g., /admin/faith-qa, /admin/vetting). Defaults to any logged-in user if unset (unsafe in production).
  - _How:_ Set to email addresses allowed to access admin tools (Ryan + sister). .env.example line 62. Add to Vercel. Leave blank in dev to allow any login; restrict in prod.
- [ ] **Supabase: create 'price-lists' private storage bucket** — _eng · S_
  - _Why:_ Photo upload for price-list analyzer. Schema comment line 194 says 'create manually in Dashboard'.
  - _How:_ Supabase dashboard: Storage → New bucket → name 'price-lists', select 'Private'.
- [ ] **Resend webhook setup for bounce + complaint handling** — _eng · M_
  - _Why:_ Hard bounces and spam complaints flip funeral_homes.active=false so we stop hitting bad emails.
  - _How:_ Resend dashboard: Webhooks → Add Endpoint → URL = 'https://honestfuneral.co/api/inbound/resend-webhook' → Subscribe to 'email.bounced', 'email.complained' → Copy signing secret (whsec_...) to RESEND_WEBHOOK_SECRET in Vercel. Webhook route at app/api/inbound/resend-webhook/route.ts uses Svix HMAC verification.
- [ ] **RESEND_WEBHOOK_SECRET: copy to Vercel after webhook registration** — _eng · S_
  - _Why:_ Server env var for Svix signature verification in app/api/inbound/resend-webhook/route.ts (line 82).
  - _How:_ Add to Vercel env vars after registering webhook. NOT in .env.local post-launch.
- [ ] **Postmark account setup for inbound email parsing** — _eng · M_
  - _Why:_ Funeral homes reply to reply.honestfuneral.co → Postmark webhooks to /api/inbound/email → stores message thread.
  - _How:_ postmark.com → account setup. Server settings → Inbound → Set webhook URL = 'https://honestfuneral.co/api/inbound/email' with basic auth (see next task). Copy server token. Route at app/api/inbound/email/route.ts expects POSTMARK_INBOUND_USER + POSTMARK_INBOUND_SECRET for basic auth.
- [ ] **POSTMARK_INBOUND_USER and POSTMARK_INBOUND_SECRET: generate and set** — _eng · S_
  - _Why:_ Basic auth credentials for Postmark webhook (app/api/inbound/email/route.ts line 38–42). Protects webhook from spoofing.
  - _How:_ Generate two arbitrary strings (e.g., openssl rand -hex 32). Configure in Postmark server settings as username:password. Add both to Vercel env vars.
- [ ] **MX records for reply.honestfuneral.co → Postmark** — _eng · S_
  - _Why:_ Routes funeral-home replies to Postmark inbound parser. Without this, replies don't reach the app.
  - _How:_ DNS registrar (Cloudflare, etc.). Add MX records pointing to Postmark (Postmark docs specify exact values). Funeral homes reply to advocate+{negotiationId}@reply.honestfuneral.co per app/api/inbound/email/route.ts line 54.
- [ ] **CRON_SECRET: generate and set for all cron endpoints** — _eng · S_
  - _Why:_ Authorization header for Vercel cron invocations. Shared across /api/cron/anniversary, /api/cron/quote-notifications, /api/cron/nurture-emails. Prevents unauthenticated trigger.
  - _How:_ Generate: openssl rand -hex 32. Add to Vercel env vars. All three cron routes check Bearer token (e.g., anniversary/route.ts line 36). Vercel sends this header automatically on scheduled runs.
- [ ] **UNSUBSCRIBE_SECRET: generate for email unsubscribe links** — _eng · S_
  - _Why:_ HMAC signing for one-click unsubscribe URLs (/preferences/[token]). Prevents tampering with user preferences.
  - _How:_ Generate: openssl rand -hex 32. Add to Vercel env vars. Referenced in env.ts but used for anniversary + nurture email links.
- [ ] **Vercel cron job registration (vercel.json)** — _eng · S_ ⚠️PARTIAL
  - _Why:_ Three scheduled jobs: anniversary emails (daily 14:00 UTC), quote notifications (hourly :15), nurture emails (daily 14:00 UTC). Already defined in vercel.json.
  - _How:_ vercel.json already has 3 cron entries (lines 2–16). Verify they're correct on deploy. No action needed unless you want to change schedule. All three routes require CRON_SECRET to execute.
- [ ] **Kill-switch: OUTREACH_NOTIFICATIONS_ENABLED = false until ready** — _eng · S_
  - _Why:_ Cron at /api/cron/quote-notifications sends emails when funeral homes reply. Default = disabled. Flip to 'true' only after outreach goes live.
  - _How:_ app/api/cron/quote-notifications/route.ts line 38 checks this. Set in Vercel when OUTREACH_LIVE='true'.
- [ ] **Kill-switch: ANNIVERSARY_EMAILS_ENABLED = false until ready** — _eng · S_
  - _Why:_ Cron at /api/cron/anniversary sends 1mo/6mo/1yr check-in emails to paid users. Default = disabled. Flip to 'true' only after outreach flow tested.
  - _How:_ app/api/cron/anniversary/route.ts line 44 checks this. Set in Vercel when ready (probably after first few deals).
- [ ] **Kill-switch: NURTURE_ENABLED = false until ready** — _eng · S_
  - _Why:_ Cron at /api/cron/nurture-emails sends 2-step welcome sequence to planning_signups (pre-need leads). Default = disabled.
  - _How:_ app/api/cron/nurture-emails/route.ts line 35 checks this. Set in Vercel when nurture marketing is live.
- [ ] **Stripe test mode: run sample checkout flow** — _eng · M_
  - _Why:_ Verify payment processing before going live. Stripe test mode is free and safe.
  - _How:_ Use test card 4242 4242 4242 4242, any future expiry, any CVC. Vercel dev URL or local: /negotiate/[id]/preview → POST to /api/stripe/checkout → redirects to Stripe session. Verify success_url redirect. Check Stripe dashboard for test payment recorded.

### Data & Content
- [ ] **Probate-by-state content: verify thresholds + timelines against current state law** — _YOU · M_
  - _Why:_ lib/probate-by-state.ts carries TODO-FD comment: 'review thresholds and timelines against current state code before sister sends this to a real family.' The file documents estate probate for top 10 US states (CA, TX, FL, NY, PA, IL, OH, GA, NC, MI). Small-estate thresholds, timeline estimates (9–18 months per state), and attorney-requirement flags affect family planning. This must be accurate before launch-state pages go live.
  - _How:_ /Users/ryancurrie/FH/lib/probate-by-state.ts (lines 1–10 TODO). Launch step: (1) pull current state probate court rules for Utah (target launch state), (2) verify small-estate threshold (currently set per state in STATE_GUIDES array), (3) check typical timeline, (4) verify community-property status, (5) test /estate/[state-slug] pages render correctly. Longer term: expand to top 10 states + Utah before national rollout.
- [ ] **Rights page: Sarah reviews state-specific embalming rules + vault/liner nuance** — _YOU · S_
  - _Why:_ app/rights/page.tsx line 22 TODO comment: 'Sarah review — state-specific embalming rules and vault/liner nuance.' The RIGHTS array on that page lists 9 line items families can decline. Embalming rules vary by state (15 states require embalming OR refrigeration after 24–48h; a few forbid it for communicable diseases). Vault requirements vary (some states/cemeteries; some not). Families need accurate, state-specific guidance.
  - _How:_ /Users/ryancurrie/FH/app/rights/page.tsx (lines 21–30, RIGHTS array items). Task: (1) cross-reference current Utah state law on embalming (check utah.gov vital records + state funeral board), (2) clarify vault requirements vs cemetery requirements, (3) update the 'law' field for embalming item to reflect Utah specifics, (4) add watchOut note if any Utah quirks exist. For national: make this dynamic per state once estates/[state] pages roll out.
- [ ] **Cemetery vendors directory: re-verify all 30+ monument companies before launch** — _YOU · M_
  - _Why:_ lib/cemetery-vendors.ts includes TODO-FD comment: 'every entry below should be re-verified before sister recommends to a family. Hours, websites, and ownership change. Especially the smaller regional companies.' The VENDORS array has 30+ entries (Northeast, Mid-Atlantic, Southeast, Midwest, Southwest, West regions). Bad phone numbers or defunct websites undermine family trust.
  - _How:_ /Users/ryancurrie/FH/lib/cemetery-vendors.ts (lines 13–16 TODO, VENDORS array starting line 39). Task: (1) spot-check each vendor's website + phone (especially small regional ones), (2) flag any defunct/changed contacts, (3) add website + phone if missing, (4) note 'ftcVerified' status. Not critical for Utah-only launch if you curate a small Utah-specific subset, but important before national expansion.
- [ ] **Confirm all {{...}} and TODO placeholders are removed or documented** — _YOU · S_ ⚠️PARTIAL
  - _Why:_ Search of codebase for {{, TODO, FIXME, placeholder confirms: (1) app/terms/page.tsx line 1: 'TODO: Ryan to discuss with counsel — clause 8 (binding arbitration with class action)' — legal review open, (2) app/rights/page.tsx: Sarah review pending (covered above), (3) Multiple TODO-FD comments in faith-traditions.ts, probate-by-state.ts, cemetery-vendors.ts, lib/faith-traditions.ts — all FD/sister review items (covered above). Input placeholders ('Margaret Ann Whitfield', '44106', etc.) are form examples, not stale data.
  - _How:_ Search results: /Users/ryancurrie/FH/app/terms/page.tsx line 1 (TODO on binding arbitration clause — legal issue, separate from data-content), /Users/ryancurrie/FH/app/rights/page.tsx line 22 (Sarah review, already tasked). All other TODOs are expert review flags (TODO-FD) already listed above. Task: (1) confirm all input-field placeholders are example text, not broken content, (2) ensure /terms arbitration clause is resolved before launch, (3) no {{...}} template syntax found in live code — clean.
- [ ] **Pricing data: validate national benchmarks against Utah General Price Lists** — _eng · M_ ⚠️PARTIAL
  - _Why:_ lib/pricing-data.ts PRICING_LAST_UPDATED = '2026-04-01' and dataSourceForZip() always returns 'national-adjusted' for all zips. Pricing data is national fair-price benchmarks (not locally validated). The UI correctly labels this as 'Based on national benchmarks adjusted for your region' (DATA_SOURCE_LABEL.national-adjusted). BUT: once Utah homes are imported and vetted, their actual GPLs should be checked against the benchmarks. If Utah homes charge significantly higher/lower, the UI should surface that with confidence.
  - _How:_ /Users/ryancurrie/FH/lib/pricing-data.ts (lines 494–511). Current approach correct for V1 (national with regional multiplier via zip-regions.ts). Task for Utah launch: (1) collect 3–5 Utah funeral homes' GPLs (request via email or scrape websites), (2) spot-check line-item ranges (basic-services, casket, embalming, transfer) against LINE_ITEMS table, (3) if Utah averages differ >15% from national, flag for Sarah review, (4) keep dataSourceForZip() returning 'national-adjusted' for now, but add comment that per-metro validation work is underway. Future: add 'validated' state once Utah data is vetted.
- [x] **Verify sample-homes fallback is clearly labeled in outreach preview + admin tools** — _eng · S_ ✅DONE 2026-06-09 (Batch 4 — verified: /admin/outreach-preview shows the home source incl. "no match in directory — placeholder"; directory only ever returns vetted real homes for live sends)
  - _Why:_ Until funeral_homes table is populated + vetted, findHomesFromDirectory() falls back to placeholder sample homes from sample-homes.ts (20 template entries like 'Brookside Funeral Home', 'office@brookside-funerals.example'). The PreviewForm.tsx correctly labels source as 'placeholder'. BUT: if OUTREACH_LIVE is flipped to true before data loads, placeholder emails will be contacted. The kill-switch and vetting gates prevent this, but confirmation needed.
  - _How:_ /Users/ryancurrie/FH/lib/negotiation/sample-homes.ts (lines 17–38, TEMPLATES array). /Users/ryancurrie/FH/app/admin/outreach-preview/PreviewForm.tsx line 27 correctly labels placeholder source. Task: (1) verify directory.ts line 50 returns findHomes(zip, n) only when funeral_homes query fails, (2) confirm /admin/outreach-preview shows 'source: placeholder' for test emails, (3) test /api/negotiate/start?zip=99999 (non-Utah) returns placeholder homes, (4) document in /admin that placeholder homes should NEVER be live until real data is loaded. OUTREACH_LIVE kill-switch protects against accidental send.
- [ ] **Vetting admin tool is functional and accessible** — _eng · S_ ⚠️PARTIAL
  - _Why:_ app/admin/vetting/page.tsx provides the interface for Sarah (or designated FD) to mark homes as vetted before outreach. This is the final gate. Must be tested with real data.
  - _How:_ /Users/ryancurrie/FH/app/admin/vetting/page.tsx (requires ?key=[ADMIN_PREVIEW_KEY] for access). VettingClient.tsx provides UI to: (1) filter homes by state, status (pending/approved/rejected), email presence, (2) edit email + notes, (3) approve/reject/reset decisions, (4) stamps reviewer name + timestamp. Task: (1) test with sample funeral_homes rows in staging Supabase, (2) verify approve button sets vetted=true + active=true + vetted_at + vetted_by, (3) verify reject button sets vetted=true + active=false, (4) confirm all changes POST to /api/admin/funeral-homes with ?key=[ADMIN_PREVIEW_KEY], (5) dry-run full vetting workflow for 10 Utah homes before launch.
- [ ] **ADMIN_EMAILS is configured for /admin/* access control** — _eng · S_
  - _Why:_ .env.example line 61-62: 'ADMIN_EMAILS=' comma-separated list of emails allowed to view /admin/* (faith-qa, vetting, outreach-preview). By default if unset, any logged-in user can view. Must be locked down before production.
  - _How:_ .env.example line 61-62. Task: set ADMIN_EMAILS=sarah@[honestfuneral.co],ryan@honestfuneral.co (or Sarah's licensed FD email + Ryan's). Verify /admin/faith-qa, /admin/vetting, /admin/outreach-preview all check this permission before rendering. Before launch, confirm only Sarah + Ryan can access these tools.

### Payments, Email & Deliverability
- [ ] **Document manual refund process in operational playbook** — _YOU · S_
  - _Why:_ LAUNCH_PLAYBOOK explicitly states refunds are manual for first 10 deals ("refund manually in Stripe Dashboard"), but no runbook exists for Ryan to follow. Without written steps, refunds may be missed or done incorrectly.
  - _How:_ Create a doc at /Users/ryancurrie/FH/docs/REFUND_PROCESS.md (or update LAUNCH_PLAYBOOK) with: (1) When to trigger: family emails within 14 days with 'refund' request + proof of no savings; (2) Refund method: Stripe Dashboard → Payments → select Checkout session → click 'Refund' (full amount, reason 'Customer requested - no savings documentable'); (3) What to verify before issuing: negotiation status, date of charge, best_quote_cents vs savings_cents; (4) Notification: send family email via support@honestfuneral.co confirming refund + Stripe processing time (typically 5-10 days); (5) Track in a simple spreadsheet (date, family email, negotiation ID, stripe charge id, refund reason, refund date) for first 10 until the in-product button is built.
- [ ] **Configure Resend domain authentication (DKIM, SPF, DMARC)** — _YOU · S_ ⚠️PARTIAL
  - _Why:_ Outreach emails go from honestfuneral.co domain. Without DKIM/SPF/DMARC, emails will land in spam or be rejected entirely, killing deliverability to funeral homes. LAUNCH_PLAYBOOK step 4 says 'Resend gives you DNS records,' but doesn't confirm setup completion or testing.
  - _How:_ Resend dashboard → Domains → honestfuneral.co → DNS Records. Copy the 3 records (DKIM, SPF, DMARC) into registrar (Cloudflare/Namecheap). Wait 5–60 min for verification. In Vercel, confirm RESEND_FROM env var is set to a @honestfuneral.co address (currently 'Honest Funeral <hello@honestfuneral.co>' in lib/email.ts line 12). For outreach, override to 'arrangements@honestfuneral.co' via lib/negotiation/email-body.ts line 117. Test: send a test email from /api/inbound/email webhook simulator to yourself to verify it lands in inbox (not spam). Document passing this check before flipping OUTREACH_LIVE.
- [ ] **Establish email warmup sequence before real outreach** — _YOU · M_
  - _Why:_ Resend has a brand-new IP delivering from honestfuneral.co. Funeral homes (conservative businesses) are more likely to reject mail from an unknown sender. Gradual warmup (low volume first 2–3 weeks) improves deliverability and reputation.
  - _How:_ Coordinate with Sister: (1) Week 1 of launch (OUTREACH_LIVE = true): send to max 5 funeral homes per day (25 total), pick homes with known good email practices; (2) Week 2: increase to 15/day (100 total); (3) Week 3+: full ramp. Monitor bounce rate in Resend dashboard (target <2%) + complaint rate (target 0%). If a home bounces or complains, the resend-webhook auto-flips funeral_homes.active = false (app/api/inbound/resend-webhook/route.ts lines 96–142), so no retry to bad addresses. Document this rhythm in a 'Warmup Plan' section in /docs or in a comment on OUTREACH_LIVE env var logic.
- [ ] **Set up Stripe receipt/invoice emails** — _YOU · S_
  - _Why:_ Families pay $49; Stripe can auto-email receipts. Currently no receipt config is in the code. Families need proof of payment for their records and to show the 14-day refund window starts now.
  - _How:_ Stripe dashboard → Settings → Email settings → enable 'Customer receipt emails' (off by default). Set BCC if you want a copy. Verify a test charge in test mode generates a receipt email. Add to .env.example and Vercel that this is configured. No code change needed; Stripe handles it server-side.
- [ ] **Configure Resend bounce + complaint webhook in Vercel** — _YOU · S_
  - _Why:_ app/api/inbound/resend-webhook/route.ts expects RESEND_WEBHOOK_SECRET env var (line 82). Without the webhook registered in Resend dashboard, bounces/complaints are never delivered to the app, so funeral_homes.active never auto-flips to false, and bad emails keep getting hammered.
  - _How:_ Resend dashboard → Webhooks → Create Webhook. URL: https://honestfuneral.co/api/inbound/resend-webhook. Events: select 'email.bounced', 'email.complained'. Copy signing secret → RESEND_WEBHOOK_SECRET in Vercel env. Test locally: use Resend CLI or generate a mock bounce event JSON, call the webhook with correct signature, verify funeral_homes row flips to active=false. Add to LAUNCH_CHECKLIST.
- [ ] **Document Postmark inbound webhook (funeral-home replies) setup** — _YOU · S_
  - _Why:_ app/api/inbound/email/route.ts expects Postmark to POST replies to /api/inbound/email. LAUNCH_PLAYBOOK doesn't mention Postmark setup steps. Without this, funeral-home replies never reach the family, and the whole negotiation thread is broken.
  - _How:_ Postmark dashboard → Inbound Rules → Create Rule. Domain: reply.honestfuneral.co (MX records must point to Postmark; set up in registrar). Webhook URL: https://honestfuneral.co/api/inbound/email. Auth: Basic Auth using POSTMARK_INBOUND_USER + POSTMARK_INBOUND_SECRET (both in .env.example line 68–70). Test: send an email to advocate+<negotiation-uuid>@reply.honestfuneral.co from an external address; verify negotiation_messages row is created + family is notified (notifyFamilyOfReply is called, line 146 in app/api/inbound/email/route.ts). Add MX + webhook test to LAUNCH_CHECKLIST.
- [ ] **Test Stripe webhook endpoint configuration** — _YOU · S_
  - _Why:_ LAUNCH_PLAYBOOK step 3 says create webhook, but doesn't confirm it's deployed + reachable. If webhook URL is wrong or unreachable, Stripe retries then stops, and outreach never fires (status page reconciliation is backup, but not reliable).
  - _How:_ After Vercel deploy: Stripe dashboard → Developers → Webhooks → click your endpoint. Verify URL is https://honestfuneral.co/api/stripe/webhook. Click 'Send test event' → checkout.session.completed. Inspect the response log to confirm 200 OK. In Vercel logs, verify [webhook] sendOutreach log line appears. If not, check env var STRIPE_WEBHOOK_SECRET is set correctly. Test a real Checkout flow (test card) end-to-end: payment succeeds → webhook fires → negotiation_outreach rows move to 'sent' + initial_email_id is populated.
- [ ] **Document the OUTREACH_LIVE kill-switch activation sequence** — _YOU · S_
  - _Why:_ OUTREACH_LIVE toggles between dry-run (records 'dry_run' status, no send) and live (sends real emails to funeral homes). No runbook exists for when/how to flip it. Flipping prematurely or with unvetted homes will spam them.
  - _How:_ Create /docs/OUTREACH_LIVE_ACTIVATION.md with checklist: ✓ Domain DKIM/SPF/DMARC verified & Resend domain active. ✓ Resend bounce/complaint webhook configured + tested. ✓ Postmark inbound webhook for replies configured + tested. ✓ Stripe webhook endpoint reachable + tested (test payment → outreach triggered). ✓ Sister has vented & approved ≥30 funeral homes in funeral_homes table (vetted=true, active=true). ✓ No ADMIN_EMAILS set (or set to founder+sister emails only). ✓ HONEST_FUNERAL_FREE_EMAILS set to founder test account(s) for QA. ✓ First 3 test families use free-email bypass (founder accounts) to dry-run flow without charges. ✓ Manual process doc (above) is ready. ✓ Ran end-to-end smoke test: founder signs up, starts negotiation, pays $49 (test card), receives confirmation email, Sister verifies outreach emails landed in real FD inboxes (test homes). Only after ✓ all: set OUTREACH_LIVE=true in Vercel env + redeploy.
- [ ] **Verify NURTURE_ENABLED kill-switch is set correctly** — _YOU · S_
  - _Why:_ Nurture cron (app/api/cron/nurture-emails/route.ts line 35) checks NURTURE_ENABLED !== 'true' and returns early if false. Without this env var set in Vercel, nurture emails never send (good for launch safety), but it must be explicitly enabled.
  - _How:_ In Vercel env vars: add NURTURE_ENABLED = false (or omit, defaults to false). Before launch, you can flip to true to enable 2-step follow-ups (step 1 at +7 days, step 2 at +21 days after signup). Cron schedule is in vercel.json line 12–14 (30 14 * * * = 2:30 PM UTC daily). Test: create a planning_signup row, wait for cron, verify nurture_step advances + email is sent (or logged if Resend is in dry-run).
- [ ] **Configure Stripe signing secret correctly for webhook auth** — _YOU · S_ ⚠️PARTIAL
  - _Why:_ app/api/stripe/webhook/route.ts line 23 constructs the event using webhook secret. If secret is wrong, all webhooks fail with 400 'bad_sig'.
  - _How:_ LAUNCH_PLAYBOOK step 3 says copy the signing secret. Verify: (1) Stripe dashboard → Developers → Webhooks → endpoint → copy 'Signing secret' (starts with whsec_). (2) Add to Vercel env: STRIPE_WEBHOOK_SECRET = whsec_xxxxx. (3) Redeploy. (4) Test: in Stripe dashboard, send a test checkout.session.completed event; check Vercel logs for [webhook] line (no error). If 400, secret mismatch.
- [ ] **Verify Stripe test mode → live mode transition plan** — _YOU · S_
  - _Why:_ Code has sk_test_ secret hardcoded. Switching to production (sk_live_) requires changing STRIPE_SECRET_KEY + STRIPE_WEBHOOK_SECRET env vars + re-registering webhook endpoint.
  - _How:_ Before launch: (1) Get Stripe live keys: Stripe dashboard → Developers → API Keys → live mode toggle (requires account activation + verification). (2) Copy sk_live_xxx to Vercel STRIPE_SECRET_KEY. (3) Create new webhook endpoint in Stripe live mode (same URL). (4) Copy whsec_xxx to Vercel STRIPE_WEBHOOK_SECRET. (5) Redeploy. (6) Update any internal docs referring to test card numbers (4242 4242...) to note that prod now uses real cards. Test: process a real payment with the founder's card, verify charge appears in Stripe live dashboard + outreach fires.
- [ ] **Ensure Honest Funeral team email accounts are in free-email bypass list** — _YOU · S_ ⚠️PARTIAL
  - _Why:_ HONEST_FUNERAL_FREE_EMAILS (env var) lets specified accounts (founder, Sister) use outreach without paying. For testing, both must be in this list to avoid $49 charges during QA.
  - _How:_ In .env.local & Vercel: set HONEST_FUNERAL_FREE_EMAILS = 'ryan@honestfuneral.co, sister@honestfuneral.co' (comma-separated, case-insensitive). isPaidUser() check (app/api/stripe/checkout/route.ts line 55) determines if the free bypass applies. Test: founder logs in with free account, starts negotiation, goes to /negotiate/[id]/preview, clicks 'Pay now' → should redirect to status page with ?freebypass=1 query param and outreach sent without Stripe charge.
- [x] **Verify CAN-SPAM compliance in outreach email footers** — _eng · S_ ✅DONE 2026-06-09 (Batch 4 — verified compliant: clear sender ID, accurate subject, one-click HMAC opt-out, and a real physical postal address (lib/postal-address.ts) in every outreach + selection footer. Counsel review of the template is still a separate legal P0.)
  - _Why:_ FTC requires: clear opt-out link in every marketing email + physical mailing address. Funeral-home opt-out (lib/negotiation/email-body.ts line 55) and postal address (postalAddressLine()) are included in outreach body. However, no verification that opt-out token generation + page actually work at scale, and address format must comply.
  - _How:_ Verify: (1) Outreach emails include funeralHomeOptOutUrl() (lib/negotiation/email-body.ts line 150–159) with HMAC-SHA256 token. (2) /funeral-home-opt-out page (app/funeral-home-opt-out/page.tsx) verifies token + flips funeral_homes.active=false. Test: click the opt-out link in a sent email, verify token validates + page shows success, check DB that row is inactive. (3) Postal address via postalAddressLine() must match FTC requirements (full street + city/state/zip). Check lib/postal-address.ts format. (4) No hard requirement for family-side unsubscribe in outreach (only FD-side), but welcome + nurture emails have it (lib/nurture-email.ts line 149–150, /unsubscribe page). Verify all transactional paths include unsubscribe for their audience.
- [ ] **Enable + test Resend bounce webhook signature verification** — _eng · S_
  - _Why:_ app/api/inbound/resend-webhook/route.ts implements Svix HMAC verification (lines 49–74) but only works if RESEND_WEBHOOK_SECRET is correct and webhook is registered. No test exists yet.
  - _How:_ Test locally: (1) Generate a mock bounce event JSON (see ResendBounceEvent interface, line 25–47). (2) Use Resend SDK or curl to compute the HMAC signature: id=<svix-id>, timestamp=<svix-timestamp>, signature=v1,<base64-hmac>. (3) POST to localhost:3000/api/inbound/resend-webhook with headers. Verify 200 response + funeral_homes.active = false. (4) If signature fails, verify error is 401. (5) Add a unit test or smoke-test script to /tests or /docs for this flow.
- [ ] **Test unsubscribe tokens for both family (nurture) and FD (outreach)** — _eng · S_
  - _Why:_ Two independent token schemes: families (lib/nurture-email.ts), funeral dirs (lib/negotiation/email-body.ts). Both use HMAC-SHA256 with UNSUBSCRIBE_SECRET. If the secret is wrong or stale, tokens validate incorrectly, and unsubscribes fail.
  - _How:_ Verify: (1) UNSUBSCRIBE_SECRET is set in .env.local & Vercel (required, no fallback in prod). (2) Family unsubscribe: send a nurture email (or trigger via cron), click the unsubscribe link, verify planning_signups.unsubscribed_at is set + future nurture emails don't send. (3) FD opt-out: same token flow but updates funeral_homes.active=false. (4) Both endpoints validate token with crypto.timingSafeEqual (defense against timing attacks). Test with truncated/invalid tokens to verify 400-level responses.
- [ ] **Test family reply-relay email thread (end-to-end)** — _eng · M_
  - _Why:_ Family sends message via /api/negotiate/[id]/messages → relayed to FD → FD replies via Postmark inbound → notifyFamilyOfReply fires. No test scenario is documented.
  - _How:_ Manual smoke test: (1) Founder signs up, starts negotiation, gets outreach sent to Sister's email (test FD). (2) Sister replies to advocate+<negotiation-id>@reply.honestfuneral.co. (3) Verify: negotiation_messages row created (direction='inbound_fd', from_address=Sister's email, outreach_id matched correctly). (4) Verify: family receives email notification via notifyFamilyOfReply (subject: 'Sister replied — check your status page'). (5) Verify: family can see reply on /negotiate/[id]/status page + can send a reply back via the form. (6) Check all three message rows: inbound_fd (FD reply) + outbound_to_fd (family relay) + optionally outbound_to_family (internal notify). Log line should show '[inbound] stored msg neg=xxx from=yyy outreach=zzz'.
- [ ] **Document email content compliance (Funeral Rule § 453.2)** — _legal · S_ ⚠️PARTIAL
  - _Why:_ Outreach emails request General Price Lists & itemized quotes. FTC Funeral Rule requires clear, conspicuous disclosure of Honest Funeral's role (not a funeral establishment, advocate only). Current emails include this (lib/negotiation/email-body.ts lines 53, 96) but no audit/legal review is noted.
  - _How:_ Email footers currently state: 'Honest Funeral is a consumer advocacy service, not a licensed funeral establishment. We help families gather pricing and prepare for the arrangement meeting; the family makes all arrangements directly with the funeral home they select.' Verify: (1) Legal has read & approved all outreach + family email templates (welcome, nurture, selection, family notifications). (2) No claims of savings guarantee (current copy says 'typical savings $2,000–$5,000' in nurture email, which is forward-looking, not guaranteed). (3) No affiliate/kickback disclosures needed (we take 0 from homes). (4) Check app/faq/page.tsx, app/how-it-works/page.tsx for any conflicting claims.

### Legal & Compliance
- [ ] **Document the founder's LLC entity status, EIN, and ensure all contracts are in LLC name** — _YOU · S_ ⚠️PARTIAL
  - _Why:_ LAWYER_BRIEF §K notes LLC entity, EIN exist, but not fully verified. Must confirm: (a) LLC is properly formed in which state? (b) EIN is tied to LLC name; (c) all contracts (Stripe, Supabase, Resend, Vercel, domain registrar) are in LLC name to preserve liability shield.
  - _How:_ Files: None in repo (corporate docs live externally). Action: (1) collect LLC formation docs (Articles of Organization, state filing); (2) verify EIN (IRS letter); (3) audit all vendor accounts (Stripe, Supabase, Vercel, Resend, domain) are registered to Honest Funeral LLC, not personal name; (4) title deed to domain (honestfuneral.co) is in LLC; (5) save copies in a compliance folder. This is a liability-shield prerequisite.
- [ ] **Implement and test account deletion / data-retention enforcement** — _eng · M_
  - _Why:_ Privacy Policy (§5) promises 30-day deletion on account request. Must confirm: (a) user can actually delete account from dashboard; (b) deletion cascade removes personal data from all tables (profiles, negotiations, negotiation_outreach, obituaries); (c) aggregate/anonymized benchmarks are retained (allowed per policy); (d) financial records kept only as long as required by law. No evidence of a delete-account endpoint yet found.
  - _How:_ Files: app/api/*, supabase/schema.sql (tables have cascade deletes set; must verify all paths trigger them). Action: (1) grep for DELETE endpoints in app/api—if none exist, build /api/account/delete endpoint; (2) test cascade deletion from profiles → all child tables; (3) verify financial records (stripe_payment_intent_id in negotiations table) are retained per data-retention policy (counsel advises how long); (4) add deletion confirmation email. Supabase RLS policies already prevent cross-user access (good).
- [ ] **Verify advertising substantiation for savings/price claims** — _legal · M_ ⚠️PARTIAL
  - _Why:_ Homepage and marketing claim families 'overpay by $2,000–$5,000' and 'save $1,500–$3,000'. These are FTC Act §5 claims requiring substantiation (LAWYER_BRIEF §D, CLAIMS_VALIDATION §2). Current data is national benchmarks, not locally validated. Must either: (a) validate against local real price lists before launch; OR (b) add clear disclaimer that figures are national estimates, not locally verified.
  - _How:_ Files: app/page.tsx (lines 79–82), docs/CLAIMS_VALIDATION.md §2. Code comments already note 'not locally validated'. Action: (1) counsel confirms what substantiation is required for the $2k–$5k and $1.5k–$3k claims; (2) either run local validation (DATA_PLAN.md §3–9) OR add prominent disclaimer on homepage, /prices, and /faq saying 'national averages, adjust by local market.'  Pricing data is in lib/pricing-data.ts.
- [ ] **Confirm family authorization scope for outreach and liability** — _legal · M_ ⚠️PARTIAL
  - _Why:_ We contact homes 'on behalf of' families, invoking FTC Funeral Rule rights. Legal question: what written authorization do we need (currently in-product consent only)? What is our liability for quoted prices we relay or advice we give? (LAWYER_BRIEF §C)
  - _How:_ Files: app/api/negotiate/start/route.ts (line 50, 'authorizationAccepted' check), app/terms/page.tsx (§3, disclosure that families attend meeting and sign directly). Terms cover this but counsel should confirm: (1) is in-product checkbox + /terms acceptance sufficient? (2) do we need separate 'power of attorney' or more explicit authorization? (3) disclaimer language for quote reliance is already in /terms §2; counsel reviews adequacy. Note: we do NOT handle payments or sign anything; family signs directly with home.
- [ ] **Full CCPA/GDPR/state privacy law audit for sensitive bereavement data** — _legal · L_ ⚠️PARTIAL
  - _Why:_ We collect sensitive data: family account info, deceased person details, upload photos of price lists, funeral/cremation preferences. This is bereavement + financial data across 50 states. Must comply with CCPA (California), GDPR (EU residents), and other state privacy laws. Privacy Policy exists (app/privacy/page.tsx) but needs counsel review against actual data flows and legal obligations. (LAWYER_BRIEF §F)
  - _How:_ Files: app/privacy/page.tsx, supabase/schema.sql (tables: profiles, negotiations, negotiation_outreach, obituaries, price_list_analyses). Privacy Policy covers: no-sale (§1), what we collect (§2), retention/deletion (§5), third-party vendors (§8), breach notification (§7). Counsel review scope: (1) 'sensitive personal information' definition under CCPA/state laws—deceased data, bereavement data; (2) retention limits (currently 'within 30 days' for deletions); (3) right to deletion implementation (check if account-delete endpoint exists); (4) breach notification process; (5) GDPR adequacy if any EU residents access. Data is on Supabase (US-hosted).
- [ ] **Counsel review of Terms of Service §8 (binding arbitration + class waiver) vs consumer-advocate positioning** — _legal · S_
  - _Why:_ Terms already flag this as a tension (app/terms/page.tsx, line 4–7 TODO comment). Binding arbitration with class action waiver is standard in B2C but conflicts with our 'consumer advocate' brand. Counsel must advise: (a) keep as-is with explanation; (b) carve out small claims court (claims <$10k) as a compromise; (c) remove class waiver entirely. Decision required before launch.
  - _How:_ File: app/terms/page.tsx §8 (lines 158–176). Currently: full binding arbitration, class waiver. TODO: counsel decides on carve-out (small claims court, JAMS/AAA process, etc.). Update /terms and document the decision in a legal memo for the file.
- [ ] **Verify state-specific regulatory disclosures and compliance (embalming timing, vault requirements, etc.)** — _legal · M_ ⚠️PARTIAL
  - _Why:_ Product gives state-nuanced advice on embalming, vaults, cremation (CLAIMS_VALIDATION §4). Some claims are tagged 'TODO: expert review' (e.g., embalming timing rules vary ~15 states). Must confirm: (a) FTC Funeral Rule state-specific caveats are accurate; (b) no incorrect state-specific advice goes out. (LAWYER_BRIEF §5.A)
  - _How:_ Files: app/rights/page.tsx (has TODO tag), lib/scenarios.ts, lib/pricing-data.ts (contains state notes). Counsel or FTC expert reviews: (1) state-specific lines for accuracy; (2) any missing state caveats; (3) add disclaimers where state variation is unclear. Run a state-by-state check of the most regulated topics (embalming, vaults, disposition) in /rights and /prep pages.
- [ ] **Review refund policy disclosure for completeness and enforceability** — _legal · M_ ⚠️PARTIAL
  - _Why:_ Promise: $49 refundable within 14 days if we don't save the family anything (stated in homepage, /prices, /terms, Stripe product description). Must verify: (a) Stripe refund flow is built (currently non-refundable in code; refunds done manually in Stripe dashboard per LAUNCH_PLAYBOOK §6); (b) refund terms clearly disclosed pre-charge; (c) no state law conflicts (e.g., cooling-off period, specific refund-disclosure wording).
  - _How:_ Files: app/api/stripe/checkout/route.ts (line 86, refund mention in product description), lib/stripe.ts (FLAT_FEE_CENTS = $4,900), LAUNCH_PLAYBOOK.md §6 (refund = manual until post-launch). Action: (1) counsel confirms refund-policy wording is compliant + adequate per state laws; (2) decide: keep manual-refund model (QA/founder oversight pre-launch) OR build in-product refund flow; (3) update /terms §3 with exact refund process (who to email, timeline, what 'savings' means). Stripe webhook already stores stripe_payment_intent_id for refunds.
- [ ] **Determine launch-state gating for outreach (legal clearance + email coverage)** — _legal · L_
  - _Why:_ Product serves all 50 states but funeral regulation is state-specific (LAWYER_BRIEF §6, DATA_PLAN §10). Before go-live with OUTREACH_LIVE=true, must map which states we legally cleared AND have sufficient funeral-home email coverage for. Recommend gating outreach to those states until we expand.
  - _How:_ Files: docs/DATA_PLAN.md §5–6 (sourcing + coverage tracking), docs/LAWYER_BRIEF.md §6, lib/negotiation/directory.ts (currently no state filtering). Action: (1) counsel clears Utah first (launch state); (2) after licensing opinion, identify next-tier states to clear; (3) add env-var or DB flag to gate outreach-eligible states; (4) DATA_PLAN shows email-coverage is a blocker per state. Supabase schema has state column on funeral_homes but no filtering in /api/negotiate/start yet.
- [ ] **Counsel review of pricing-data 'deal rating' (good/fair/overpriced) for defamation/trade-libel risk** — _legal · M_
  - _Why:_ Product shows deal-rating opinion on named funeral homes (LAWYER_BRIEF §4.4, CLAIMS_VALIDATION §2). Stating a named business is 'overpriced' could trigger defamation/trade-libel claims. Must verify: (a) is the rating opinion (protected) or factual claim (not protected)? (b) are disclaimers adequate? (c) any homes we've contacted already that might object?
  - _How:_ Files: app/negotiate/[id]/results/page.tsx, lib/negotiation/compare.ts (compare logic). Deal rating is derived from user-input quote vs fair-range data. Counsel advises: (1) is current rating/disclaimer sufficient? (2) consider softening language (e.g., 'estimate suggests X' vs 'home is overcharging'); (3) ensure family is explicitly told the rating is based on data they provided, not hard validation. Add prominent 'opinion based on your inputs' disclaimer if not already present.
- [ ] **Confirm no unlicensed practice of law, financial advice, or insurance brokerage** — _legal · S_ ⚠️PARTIAL
  - _Why:_ Product gives guidance on estate, veterans benefits, insurance options, financing. Must ensure we don't cross into licensed activity. (LAWYER_BRIEF §J, TERMS §1)
  - _How:_ Files: app/terms/page.tsx §1 (disclaimer present: not a law firm, not financial advisor), lib/veterans-benefits.ts, lib/body-donation-programs.ts (informational only). Terms already disclaim legal/financial/medical advice. Counsel: (1) review /estate, /benefits, /insurance-referrals pages for any language that crosses into advice (should be 'consult a licensed X'); (2) confirm secondary-revenue referrals (insurance, financing, attorney) will require specific disclosures/licensing per §H below.
- [ ] **Disclaimer review for price-list analyzer AI-generated analyses and quote parsing confidence** — _legal · M_ ⚠️PARTIAL
  - _Why:_ Analyzer uses Claude to parse uploaded price lists and flag overcharges. Analyst may misread data. Quote parsing from home replies extracts numbers without confidence threshold (CLAIMS_VALIDATION §3, §6). Must add disclaimers + confidence indicators so families verify against originals. (LAWYER_BRIEF §I)
  - _How:_ Files: app/api/analyze-price-list/route.ts, app/analyzer/page.tsx, lib/negotiation/compare.ts. Current state: analyzer gives opinions on line items; quote parser extracts numbers. Action: (1) add 'verify against original' disclaimer to analyzer output; (2) add confidence-scoring to parsed data (high/medium/low confidence); (3) legal review of liability disclaimers in /terms §5 (AI-generated content)—already present but counsel confirms adequacy.
- [ ] **Faith content must not launch as authoritative—require expert review or visible disclaimer** — _legal · L_ ⚠️PARTIAL
  - _Why:_ Faith traditions content (11 traditions, ~80 claims) is built from model knowledge, not expert-verified (CLAIMS_VALIDATION §1). Highest-risk claims: cremation/burial requirements (Hindu, Muslim, Orthodox Jewish), embalming forbidden (Muslim, Jewish), burial timing (24 hours). Wrong claim = actively misadvising a grieving family on religiously binding matter. (CLAIMS_VALIDATION §1 flags this as blocker.)
  - _How:_ Files: lib/faith-traditions.ts (all claims tagged // TODO-FD:), /admin/faith-qa (validation tool exists). Action: (1) Do NOT present faith content as authoritative at launch; (2) add visible disclaimer on /faith/* and decide-flow: 'General guidance—confirm with your clergy before making decisions'; (3) run expert review via /admin/faith-qa (funeral director, clergy) to sign off on content; (4) mark content as 'reviewed & approved by [expert]' or keep disabled until review complete. This is a hard blocker for launch—wrong advice here is the worst failure mode.

### Security, Auth & Data Protection
- [ ] **Document PII handling and retention policy** — _YOU · S_
  - _Why:_ Families' names, deceased names, and zip codes are stored in profiles/negotiations. Funeral home emails and replies are stored in negotiation_messages. No documented retention or deletion policy for completed negotiations.
  - _How:_ Create docs/PRIVACY_RETENTION.md: (1) Profile data retained until account deletion. (2) Negotiations + messages deleted 1 year after close (or manually via /admin). (3) planning_signups unsubscribed_at set on opt-out (not deleted; needed for suppression). (4) Backup/export policy for compliance (GDPR, etc.).
- [x] **Add rate limiting to public API endpoints** — _eng · M_ ✅DONE 2026-06-09 (Batch 1 — lib/rate-limit.ts via proxy.ts; docs/SECURITY.md; Upstash upgrade path noted)
  - _Why:_ POST /api/planning/signup, POST /api/share/create, GET /api/share/[id], POST /api/negotiate/start lack rate limiting. Attackers can spam planning signups, create share-link floods, or brute-force negotiation IDs. No protection against enumeration attacks.
  - _How:_ Use Vercel's rateLimit or @upstash/ratelimit. Per-IP limits: planning signup (5/hour), share (10/hour), start negotiation (10/hour). Files: /Users/ryancurrie/FH/app/api/planning/signup/route.ts, /Users/ryancurrie/FH/app/api/share/*.ts, /Users/ryancurrie/FH/app/api/negotiate/start/route.ts.
- [x] **Add HTTPS enforcement and security headers middleware** — _eng · M_ ✅DONE 2026-06-09 (Batch 1 — HSTS + headers + CSP report-only in next.config.ts; verified emitting)
  - _Why:_ No explicit HTTPS redirect or X-Frame-Options, X-Content-Type-Options, Strict-Transport-Security headers configured. On Vercel, HTTPS is automatic but headers are not. Missing headers expose to clickjacking, MIME-type sniffing, and downgrade attacks.
  - _How:_ Create /Users/ryancurrie/FH/middleware.ts with headers middleware (next/headers). Set: X-Frame-Options: DENY, X-Content-Type-Options: nosniff, Strict-Transport-Security: max-age=31536000; includeSubDomains, X-XSS-Protection: 1; mode=block, Referrer-Policy: strict-origin-when-cross-origin.
- [x] **Validate input length and format on /api/negotiate/start form fields** — _eng · S_ ✅DONE 2026-06-09 (Batch 1 — Zod + readLimitedJson body caps on start AND every public/webhook route)
  - _Why:_ Form fields (senderFirstName, targetHomeName, timing, notes, extras) accept up to 120–800 chars but are stored directly in negotiation_outreach.initial_email_body without sanitization. If rendered in emails, could inject HTML/phishing. notes field is 800 chars with no content validation.
  - _How:_ File: /Users/ryancurrie/FH/app/api/negotiate/start/route.ts. Add Zod refinement to sanitize user text (e.g., no <, >, &, quotes). Use DOMPurify or similar if rendering in emails. Email body is stored but also transmitted—verify no XSS in email rendering.
- [x] **Document and enforce session expiry policy** — _eng · S_ ✅DONE 2026-06-09 (Batch 1 — documented in docs/SECURITY.md: 1h JWT auto-refresh, 400-day cookie, SameSite=Lax)
  - _Why:_ No explicit session timeout configured. Supabase default is long-lived tokens. Families may access negotiation data indefinitely after login, even if account is compromised. Funeral home replies could be read by an attacker weeks later.
  - _How:_ Configure Supabase JWT expiry (default 1 hour). Set refresh token expiry (default 30 days). Document in /Users/ryancurrie/FH/.env.example. Consider shorter expiry (30 min) for sensitive negotiation pages. Check Supabase project settings → API → JWT defaults.
- [x] **Remove or redact sensitive data from server logs** — _eng · M_ ✅DONE 2026-06-09 (Batch 1 — maskEmail/hashId across pay→send + inbound/bounce logs; dropped raw emails/bodies)
  - _Why:_ Email addresses (funeral home, family), negotiation IDs, user emails, and message content are logged to console.log/console.warn. If logs are aggregated (Vercel Logs, Sentry), they expose PII and can reveal patterns for account enumeration.
  - _How:_ Files: /Users/ryancurrie/FH/app/api/inbound/email/route.ts (lines 67, 85, 131), /Users/ryancurrie/FH/app/api/stripe/webhook/route.ts (line 50), /Users/ryancurrie/FH/app/api/inbound/resend-webhook/route.ts (lines 131, 138). Remove or hash email addresses, only log UUIDs or redact first/last char of emails.
- [x] **Add CSRF token to negotiation state transitions (form-based)** — _eng · M_ ✅DONE 2026-06-09 (Batch 1 — validateOrigin Origin/Referer check on checkout/choose/signout atop SameSite=Lax)
  - _Why:_ POST /api/negotiate/choose and POST /api/stripe/checkout accept form submissions without CSRF tokens. A malicious site could forge a POST and trigger payment or home selection on behalf of a logged-in user.
  - _How:_ Files: /Users/ryancurrie/FH/app/api/negotiate/choose/route.ts, /Users/ryancurrie/FH/app/api/stripe/checkout/route.ts. Use next-csrf or manual HMAC-SHA256 token validation on form. Add hidden field to forms in /negotiate/[id]/preview and /negotiate/[id]/closed pages.
- [ ] **Verify PII minimization in /preferences and /unsubscribe flows** — _eng · S_ ⚠️PARTIAL
  - _Why:_ Both /preferences/[id] and /unsubscribe use UUID-based auth (no login required). If a user's UUID is leaked, anyone can change their preferences or unsubscribe them. The 'knowing the UUID = auth' model is acceptable for one-click links in emails, but the UUID should not be predictable or guessable.
  - _How:_ File: /Users/ryancurrie/FH/app/preferences/[id]/page.tsx, /Users/ryancurrie/FH/app/unsubscribe/page.tsx. UUIDs are cryptographically random (v4), so guessing is infeasible. Confirm profiles.id is UUID v4 in schema. If users can share account URLs, add a warning that the UUID in the URL is a secret (don't share).
- [x] **Add validation on /api/share/create to limit payload size and prevent DoS** — _eng · S_ ✅DONE 2026-06-09 (Batch 1 — readLimitedJson 100KB cap → 413)
  - _Why:_ POST /api/share/create accepts any JSON payload and stores it as JSONB in the database. No size limit means a user could submit 10MB of data to exhaust storage or cause slowdowns. Payload could contain malicious JSON structures.
  - _How:_ File: /Users/ryancurrie/FH/app/api/share/create/route.ts. Add payload size check (max 64KB). Validate payload structure with Zod (whitelist expected keys: faithChoices, decideAnswers, etc.). Reject oversized or unexpected objects.
- [ ] **Verify negotiation_messages RLS delete policy completeness** — _eng · S_
  - _Why:_ RLS policies exist for read/insert but the migration does not explicitly block delete. Service role can delete message records, which could erase proof of communication.
  - _How:_ File: /Users/ryancurrie/FH/supabase/migrations/2026-05-21-coordinator-messages.sql. Add: 'CREATE POLICY negotiation_messages_no_delete ON public.negotiation_messages FOR DELETE TO authenticated, anon USING (FALSE);' and for service role separately.
- [ ] **Verify share_links RLS prevents unauthorized modification** — _eng · S_
  - _Why:_ share_links RLS allows anyone to create, update (opened_at), and read non-expired rows. No explicit delete policy. A malicious user could update a link's opened_at to cover tracks or theoretically delete a link.
  - _How:_ File: /Users/ryancurrie/FH/supabase/migrations/2026-05-05-margaret-share-links.sql. Add policy: 'CREATE POLICY share_links_no_delete ON public.share_links FOR DELETE TO authenticated, anon USING (FALSE);'. Prevent update of payload (allow only opened_at): check in the 'update' policy USING/WITH CHECK.
- [x] **Add IP address logging and rate limiting to auth endpoints** — _eng · M_ ✅DONE 2026-06-09 (Batch 1: rate-limit on our POST endpoints; Supabase hosts magic-link auth itself)
  - _Why:_ POST /login (email/password) is not rate-limited. Attackers can brute-force passwords at scale. planning/signup hashes IP but doesn't use it for rate limiting.
  - _How:_ Supabase Auth handles rate limiting on its own (see Supabase docs). Verify via dashboard under Auth → Rate limiting. If custom rate limiting needed on /api/planning/signup, use IP-based limits (5 signups per IP per hour). File: /Users/ryancurrie/FH/app/api/planning/signup/route.ts already hashes IP; extend to rate limit.
- [ ] **Verify Stripe webhook idempotency and payment double-charge prevention** — _eng · M_ ⚠️PARTIAL
  - _Why:_ Stripe webhook is idempotent (only pending_payment status sends outreach), but if webhook is missed and family retries payment, second payment would go through and second outreach would send.
  - _How:_ File: /Users/ryancurrie/FH/app/api/stripe/webhook/route.ts. Webhook is idempotent. Status page (/api/negotiate/[id]) reconciles missed webhooks. BUT: verify checkout form prevents double-submit (add disabled state on button). Also test: user pays, webhook sent but delayed, user clicks status page, outreach sent early, webhook arrives late (should be handled by negotiation_outreach status check).

### Observability, Ops & Support
- [ ] **Build refund-handling runbook and SOP** — _YOU · M_
  - _Why:_ The business model promises $49 refunds within 14 days if families don't get documented savings. Currently, refunds are manual (Stripe Dashboard). No process, no audit trail, no criteria for approval.
  - _How:_ Document in docs/REFUND_SOP.md: (1) criteria for approval (quote not honored, < $49 savings, no response from home); (2) approval process (Ryan decides, sister checks quote validity); (3) Stripe refund step-by-step; (4) update negotiation.status to 'refunded', add note in negotiation_outreach; (5) notify family via email. For V1, manual is OK. Create in-app refund button after 10 deals as per LAUNCH_PLAYBOOK.
- [ ] **Create incident response and on-call playbook** — _YOU · M_
  - _Why:_ No documented response plan for: Stripe webhook failures, Resend outage, funeral home directory corruption, Postmark inbound failures, cron job failures, database outage. Solo founder + part-time sister director means unclear escalation/notification.
  - _How:_ Create docs/INCIDENT_RESPONSE.md: (1) critical incidents (payment/outreach blocked); (2) escalation (Ryan primary, sister secondary); (3) monitoring/alert channels (Sentry, Vercel, Stripe dashboard); (4) communication (notify affected families?); (5) root cause + post-mortem template. Define SLAs: P0 response < 1 hour, P1 < 4 hours. Add Ryan's phone number to this doc (private file).
- [ ] **Create support inbox and response SOP** — _YOU · S_
  - _Why:_ The business model says 'reply to this email if anything looks off — a person reads it' (in /api/cron/quote-notifications/route.ts). But there's no actual support inbox configured, no mailbox, no ticket system.
  - _How:_ Set up support@honestfuneral.co inbox (or reuse hello@honestfuneral.co). Document: (1) who monitors (Ryan primary, sister secondary); (2) response SLA (24 hours for families, 4 hours for funeral homes); (3) what to do with complaints vs. bug reports vs. refund requests (route to SOP); (4) Resend does NOT forward mail to this inbox — manual setup in email provider (Gmail, Postmark, etc). Add to .env.example as SUPPORT_EMAIL if needing to reference in code.
- [ ] **Create operational runbook for manual interventions** — _YOU · M_
  - _Why:_ For the first 10 deals (per LAUNCH_PLAYBOOK), manual oversight is required: reviewing outreach emails, marking replies, making refund decisions. No documented process for these operations.
  - _How:_ Create docs/OPERATIONS_RUNBOOK.md: (1) Daily checks (morning): review Sentry errors, check new negotiations, verify pending outreach completed; (2) Outreach review: visit /admin/negotiations or Supabase Table Editor, check generated emails before webhook sends (only possible if OUTREACH_LIVE=false); (3) Reply handling: sister reviews negotiation_messages, updates negotiation_outreach.status to 'replied', notes quality; (4) Refund requests: see REFUND_SOP.md. Include links to key pages (/admin/*), SQL queries for status checks.
- [ ] **Implement webhook dead-letter queue for failed Postmark inbound emails** — _eng · M_
  - _Why:_ Postmark inbound webhook (funeral home replies) can fail to insert into negotiation_messages table for reasons (constraint violation, network hiccup, rate limit). Currently returns 500, Postmark will retry, but no visibility on how many retries fail or permanent loss of a reply.
  - _How:_ Create negotiation_webhook_dead_letters table: {id, webhook_type, payload_json, error_message, created_at, resolved_at}. On any exception in /api/inbound/email/route.ts or /api/inbound/resend-webhook/route.ts, insert before returning 500. Create /api/admin/dead-letters endpoint to review + retry. Sister can manually fix and reprocess.
- [ ] **Verify database backup strategy and restore testing** — _eng · M_
  - _Why:_ Supabase is the single source of truth for negotiations, outreach, messages, and customer data. No backup documentation exists, and prod likely uses Supabase's managed backups (default 7 days retention). Disaster recovery untested.
  - _How:_ Confirm in Supabase project: Project Settings → Backups → review retention policy (default 7 days). Enable daily backups if on paid plan. Document: backup location, retention, restore procedure, RTO/RPO. Test restore to a staging project monthly. Add to ops runbook (see next task). Backups should auto-retain at least 30 days before launch.
- [ ] **Document webhook retry/failure handling for Stripe and Postmark** — _eng · S_ ⚠️PARTIAL
  - _Why:_ Both services retry webhooks on failure, but retry behavior differs. Stripe retries up to 3 days, Postmark retries hours. The negotiation_messages.inbound_message_id unique partial index provides dedup for Postmark, but Stripe webhook retries could trigger duplicate outreach sends if not carefully handled.
  - _How:_ Stripe webhook idempotency is handled: sendOutreachForNegotiation only sends rows in 'pending' status, so retries are safe. Document in /api/stripe/webhook/route.ts. Postmark: verify the negotiation_messages.inbound_message_id unique partial index exists (it does in 2026-05-21 migration). No action needed, already correct. Add comment in both routes explaining retry safety.
- [x] **Set up rate limiting on API endpoints** — _eng · M_ ✅DONE 2026-06-09 (Batch 1 — see "Add rate limiting to public API endpoints")
  - _Why:_ No rate limiting on POST endpoints (/api/stripe/checkout, /api/planning/signup, /api/inbound/*). Vulnerable to brute force, spam, or accidental abuse.
  - _How:_ Use Vercel's built-in rate limiting via next.config.ts middleware, or npm package (redis-based like Upstash). Apply: (1) /api/planning/signup: 5 per IP per hour (prevent spam); (2) /api/inbound/* (Postmark/Resend webhooks): unlimited from their IPs only, blocked from others; (3) /api/stripe/*: standard. Configure in middleware.ts or as edge middleware. See: https://vercel.com/docs/edge-functions/edge-middleware#rate-limiting
- [ ] **Add monitoring for cron job execution and failures** — _eng · M_
  - _Why:_ Three cron jobs run daily/hourly (anniversary, quote-notifications, nurture-emails). No monitoring that they completed. If a cron 500s, Vercel retries but there's no alert to Ryan.
  - _How:_ Each cron (/api/cron/*) already returns structured JSON with sent count + errors array. Setup: (1) Vercel Cron Monitoring (native, free); (2) OR use cron.is or similar to ping a status endpoint on success. Each route should return {success: true, sent, errors} and HTTP 200 always (so Vercel doesn't retry). Add alert if cron doesn't complete within expected time window (60s max).
- [ ] **Set up Resend webhook for bounce/complaint handling** — _eng · S_ ⚠️PARTIAL
  - _Why:_ Hard bounces and spam complaints should auto-disable funeral homes (flipping active=false). The endpoint /api/inbound/resend-webhook is built but Resend webhook is not configured, so bounces are silently ignored.
  - _How:_ Setup: (1) Resend Dashboard → Webhooks → Add Endpoint; (2) URL: https://app.honestfuneral.co/api/inbound/resend-webhook (prod URL); (3) Events: email.bounced, email.complained; (4) Copy signing secret → add RESEND_WEBHOOK_SECRET to Vercel env; (5) Verify in .env.example it's documented. Code already exists (2025-05-21 verified). Test: send test email to bouncing address, confirm funeral_homes.active flips to false.
- [ ] **Document and test Postmark inbound parsing setup** — _eng · M_ ⚠️PARTIAL
  - _Why:_ Funeral home replies come via Postmark Inbound webhook. Setup is configured in .env.example (POSTMARK_INBOUND_USER/SECRET) but never verified to be working end-to-end.
  - _How:_ Postmark setup: (1) Postmark Dashboard → Inbound → Enable Inbound Parsing; (2) set MX records for reply.honestfuneral.co to Postmark (Postmark Dashboard shows exact records); (3) Webhook URL: https://app.honestfuneral.co/api/inbound/email with Basic Auth (POSTMARK_INBOUND_USER:POSTMARK_INBOUND_SECRET). Test flow: send email from a funeral home to advocate+{negotiationId}@reply.honestfuneral.co, verify it appears in negotiation_messages table + family is notified.
- [x] **Configure exception handling and custom error pages for 5XX failures** — _eng · S_ ✅DONE 2026-06-09 (Batch 2 — app/error.tsx, global-error.tsx, not-found.tsx, negotiate/error.tsx + loading states; calm + on-brand + help phone)
  - _Why:_ If an API endpoint 500s due to an unhandled exception, the family sees raw error. No graceful fallback, no support contact info, no tracking that it happened.
  - _How:_ Create app/error.tsx (React error boundary) and app/global-error.tsx (Next.js global handler). Both should: (1) catch uncaught exceptions; (2) log to Sentry with context; (3) display user-friendly message ("Something went wrong. Email support@honestfuneral.co"); (4) show support phone number (NEXT_PUBLIC_HELP_PHONE); (5) include error ref ID from Sentry. Don't expose stack traces in prod. Already partially done for API routes (catch blocks), this fills gaps in client-side errors.

### SEO, Performance, Accessibility & Mobile
- [x] **Add metadata export to 16 page.tsx files currently missing it (prep, login, dashboard, after, paywall, and 11 dynamic routes)** — _eng · M_ ✅DONE 2026-06-09 (Batch 3 — audited every page.tsx: metadata is provided by route layouts + dynamic routes use generateMetadata; after/paywall are redirects that need none)
  - _Why:_ While parent layouts provide fallback metadata, explicit page-level metadata ensures correct title/description for each route, prevents cascade mismatches, and improves crawl clarity. Missing metadata on user-visible pages (prep, worksheet) signals incomplete SEO coverage to auditors.
  - _How:_ Missing pages: /Users/ryancurrie/FH/app/prep/page.tsx (use prep/layout.tsx as template), /login/page.tsx (already in layout), /dashboard/page.tsx, /after/page.tsx (redirects but needs robots: {index: false}), /paywall/page.tsx, /estate/[state]/page.tsx, /funeral-costs/[city]/page.tsx (HAS generateMetadata ✓), /guidance/[scenario]/page.tsx (HAS generateMetadata ✓), /funeral-homes/[zip]/page.tsx (HAS generateMetadata ✓), /glossary/[slug]/page.tsx (HAS generateMetadata ✓), /faith/[tradition]/page.tsx (HAS generateMetadata ✓), /negotiate/[id]/compare, /negotiate/[id]/closed, /negotiate/[id]/status, /negotiate/[id]/results. For client-side pages (prep, login, dashboard), add 'use client' + metadata at top. For dynamic routes without generateMetadata, add it now.
- [x] **Add robots: {index: false, follow: true} to all authenticated user pages not already set** — _eng · M_ ✅DONE 2026-06-09 (Batch 3 — dashboard/layout + new negotiate/[id]/layout noindex; verified the robots noindex meta emits)
  - _Why:_ Pages like /negotiate/[id]/results, /negotiate/[id]/closed, /dashboard, /resume/[id] contain user-specific data and should not be indexed by search engines. Currently missing explicit noindex declaration, risking accidental indexing if URLs leak.
  - _How:_ Pages to update: /negotiate/[id]/results/page.tsx, /negotiate/[id]/closed/page.tsx, /negotiate/[id]/status/page.tsx, /negotiate/[id]/compare/page.tsx, /dashboard/page.tsx, /preferences/[id]/page.tsx, /resume/[id]/page.tsx. Already done correctly: /login/layout.tsx (robots: {index: false, follow: true} ✓), /negotiate/[id]/preview/page.tsx (robots: {index: false, follow: false} ✓). Verify all authenticated routes have explicit noindex before payment/auth flow goes live.
- [ ] **Generate and assign OpenGraph images to all dynamic content routes (city pages, faith guides, state estate pages, dynamic funeral-homes/zip)** — _eng · M_ ⚠️PARTIAL
  - _Why:_ OG images increase social sharing click-through by 30–50% (and funerals are grief-shared heavily on social). Currently only static pages and app-level opengraph-image.tsx used. Dynamic routes like /funeral-costs/[city] do use ogImage() ✓ but /guidance/[scenario], /estate/[state], /faith/[tradition] lack OG images, missing opportunity for discovery.
  - _How:_ Already using ogImage() in: /funeral-costs/[city]/page.tsx ✓, /app/og/route.tsx (dynamic generator working ✓). Add to: /app/guidance/[scenario]/page.tsx (generateMetadata section, add openGraph: { images: [ogImage(...)] }), /app/estate/[state]/page.tsx (add ogImage), /app/faith/[tradition]/page.tsx (add ogImage), /app/glossary/[slug]/page.tsx (optional but high value). Reuse ogImage() helper from /lib/og.ts — it handles URL encoding and returns {url, width, height, alt}.
- [ ] **Audit and fix color contrast across all interactive elements to meet WCAG AA minimum (4.5:1 for text, 3:1 for graphics)** — _eng · M_
  - _Why:_ Users grieving (often older, stressed, on mobile) rely on readable text. Current color palette uses warm neutrals; primary-deep (#20453a) on surface (#ffffff) may not meet 4.5:1. Low contrast causes form rejection, accessibility lawsuits, and abandonment.
  - _How:_ File: /Users/ryancurrie/FH/app/globals.css (color definitions in :root). Test with WebAIM Color Contrast Checker or Stark plugin: test all combinations: ink (#1d2b22) on surface (#ffffff), primary-deep (#20453a) on surface, all button states, link hover, input focus states, borders. Focus ring (primary #2f5d4f on bg #fbf9f5) also needs testing. If any fail, adjust --primary-deep or --ink to higher contrast versions, or add text-shadow/background modulation. Document final ratios in a comment.
- [x] **Add aria-label and aria-describedby to all form inputs (not just visible labels) for screen-reader clarity** — _eng · M_ ✅DONE 2026-06-09 (Batch 3 — Field binds label/htmlFor; added aria-labels to CopyButton/account-menu/back; combobox + email-capture already labeled)
  - _Why:_ Grieving users may use screen readers; all input fields must have accessible names and descriptions. Currently labels exist but some inputs (hidden, prefilled, optional) lack clear associations. Non-labeled inputs violate WCAG 2.1 Level A.
  - _How:_ Audit: /components/ui/Field.tsx (has Label + htmlFor, good ✓), /components/EmailCaptureForm.tsx (has aria-label='Email address' ✓), /components/funeral-homes/ZipSearchForm.tsx (check label binding), /components/ui/StateCombobox.tsx (has role + aria-* attributes ✓). Check all <input type='hidden'>, <input type='text'> without <label>, and optional/conditional fields. Add aria-label='Description of field' to standalone inputs.
- [ ] **Test and document Core Web Vitals baseline (LCP, FID, CLS) before launch; identify and fix large layout shifts** — _eng · L_
  - _Why:_ Core Web Vitals are Google ranking signals (now in core algorithm). Large Contentful Paint (LCP) and Cumulative Layout Shift (CLS) hurt mobile bounce rates and SEO. Currently no performance monitoring or baseline documented.
  - _How:_ Steps: (1) Use PageSpeed Insights (pagespeed.web.dev) to audit honestfuneral.co in production (once deployed). Check LCP (should be <2.5s), FID (<100ms), CLS (<0.1). (2) Identify large cumulative shifts: watch /prices page (PriceCalculator hydration), /negotiate/start (map load?), forms (input focus expansion). (3) Fix: defer non-critical JS, add size hints to images/form containers, prevent layout recalc on input focus. (4) Add web-vitals library import and log: import {getCLS, getFID, getLCP} from 'web-vitals'; getCLS(console.log), etc. (5) Document baseline in /docs/PERFORMANCE.md before launch.
- [x] **Ensure all user-facing images and icons have descriptive alt text; audit SVG icons for aria-hidden or aria-label** — _eng · M_ ✅DONE 2026-06-09 (Batch 3 — audited: logo/mobile-menu/combobox aria-hidden, Analyzer img has alt, account-menu ▾ now aria-hidden)
  - _Why:_ Alt text is mandatory for WCAG 2.1 Level A. Icons used as buttons (menu toggle, expand/collapse, copy-to-clipboard) must have labels. Empty alt='' or missing alt attributes will fail accessibility audit and harm image search.
  - _How:_ Audit: /app/icon.svg (app favicon), /app/opengraph-image.tsx (has alt ✓), /components/Brand.tsx (logo with aria-hidden ✓), /components/SiteHeader.tsx (menu toggle has aria-label ✓, check chevron icons). For every SVG icon used as button or visual content, add aria-label='Action' or aria-hidden='true' if purely decorative. No <img> tags found in main app (using Next.js Image or inline SVG, good). If SVGs used in cards/articles, add alt-text via title or aria-label.
- [ ] **Verify mobile responsiveness across all breakpoints (xs, sm, md, lg) using DevTools; test on real devices at 320px and 375px widths** — _eng · M_ ⚠️PARTIAL
  - _Why:_ Grieving users access on small phones while emotionally stressed (single-handed, bad connectivity). Current design uses responsive Tailwind (sm:, md:, lg:) but no device test documented. Missing edge cases (very small phones, rotated tablets) can cause form breaks, unreadable text, or key CTA hidden.
  - _How:_ Test pages: /page.tsx (hero, 3-column grid should stack), /prices/page.tsx (calculator width on mobile), /prep/page.tsx (cheatsheet printable on mobile), /worksheet/page.tsx (form inputs), /negotiate/start/page.tsx (zip search). Use Chrome DevTools mobile emulation (iPhone SE 375px, iPhone 12 390px, Pixel 5 393px, Galaxy Z Fold 280px*). Check: text readability at 17px base, button touch targets ≥48px, no horizontal scroll except deliberate. Use Tailwind's sm: (640px), md: (768px), lg: (1024px) breakpoints — verify responsive classes in key components (SiteHeader, max-w-*, px-5, grid-cols-3→2→1).
- [x] **Document and test noindex status for all auth-walled pages (dashboard, negotiate results, preferences, resume) — verify robots.txt allows indexing of public pages only** — _eng · M_ ✅DONE 2026-06-09 (Batch 3 — all per-negotiation pages noindex via [id] layout; dashboard noindex; preferences/resume/vault already set; verified meta emits)
  - _Why:_ If user-specific pages are accidentally indexed, Google Search Console will flag duplicate content and crawl budget waste. Current robots.txt disallows /dashboard and /login (good) but doesn't explicitly block individual negotiation IDs. Verify no leakage.
  - _How:_ robots.ts file: /Users/ryancurrie/FH/app/robots.ts. Verify disallow list: ['/dashboard', '/login', '/api/', '/signup']. Check if /negotiate/[id]/* is explicitly disallowed (currently relies on noindex meta tag in individual pages). If needed, add /negotiate/*/preview, /negotiate/*/results, etc. to disallow, OR verify noindex meta tags are present on all dynamic negotiate pages. Run: curl https://honestfuneral.co/robots.txt and verify in Google Search Console (verify ownership first).
- [x] **Test and ensure print styles work correctly for all printable content pages (prep kit, worksheet, cheatsheet, obituary, analyzer results)** — _eng · M_ ✅DONE 2026-06-09 (Batch 3 — site header + footer + HelpFooter all print:hidden so printable pages print clean)
  - _Why:_ Users print cheat sheets, worksheets, and obituary drafts. Print CSS already exists (globals.css @media print) but not all pages have been tested end-to-end. Missing print: hide buttons, show input values, avoid page breaks mid-section.
  - _How:_ Files: /Users/ryancurrie/FH/app/globals.css (print styles present ✓), /app/prep/page.tsx (has no-print class ✓), /app/worksheet/page.tsx (printable). Test: Open /prep in browser, Cmd+P / Ctrl+P, preview PDF — should show clean sheet without buttons. Check /worksheet/page.tsx: form inputs should show as text, progress hidden, buttons hidden. If page breaks mid-section, add print-keep-together class. Test all classes: no-print, print-only, print-break-before, print-break-after work as expected.
- [ ] **Test keyboard navigation and focus order on all interactive pages (forms, search, navigation, modals) — ensure logical tab order** — _eng · M_
  - _Why:_ Grieving users may be older or disabled, relying on keyboard. Tab order must be logical (top-to-bottom, left-to-right, no focus traps). Currently focus-visible ring is styled but tab order not audited.
  - _How:_ Test pages: /negotiate/start (zip search form, home list, pay button), /prices/page.tsx (calculator inputs, results), /worksheet/page.tsx (form fields), /prep/page.tsx (print button), SiteHeader navigation. Use Tab key in browser: verify focus ring appears on every focusable element (buttons, inputs, links), no elements skipped, no focus traps. Check z-index to ensure focus ring visible (currently focus-visible uses primary color, should be visible on all backgrounds). If issues found, adjust tab order via tabIndex or reorder DOM.
- [ ] **Create /robots.txt sitemap discovery page test; verify crawlers can reach and parse sitemap.xml in Google Search Console** — _eng · S_
  - _Why:_ robots.txt points to sitemap, but if sitemap generation fails or URL is wrong, crawlers won't find it. No verification documented. Test crawlability before first live traffic.
  - _How:_ Steps: (1) Deploy to production Vercel. (2) Visit https://honestfuneral.co/robots.txt — should list 'sitemap: https://honestfuneral.co/sitemap.xml'. (3) Visit https://honestfuneral.co/sitemap.xml — should return XML with 183+ URL entries, each with <url>, <loc>, <lastModified>, <changeFrequency>, <priority>. (4) Verify host directive: 'host: https://honestfuneral.co' (tells crawlers canonical domain). (5) Add property to Google Search Console, request sitemap crawl (may take 24–48 hrs). (6) Document in /docs/LAUNCH_SEOPA.md.

### QA, Testing & Edge Cases
- [ ] **Free-email bypass (HONEST_FUNERAL_FREE_EMAILS) avoids Stripe** — _eng · S_
  - _Why:_ Test/founder accounts must not be charged during QA. isPaidUser checks isFreeEmail before Stripe. Verify outreach still sends even though no Stripe charge.
  - _How:_ Set HONEST_FUNERAL_FREE_EMAILS=test@example.com in .env.local. Create auth with test@example.com. Run /negotiate/start → /negotiate/[id]/preview → hit 'Pay now'. Should redirect immediately to /negotiate/[id]/status?freebypass=1 (no Stripe Checkout). Verify negotiation_outreach.status='dry_run' (or 'sent' if OUTREACH_LIVE=true). Check stripe_payment_intent_id is null (no Stripe charge). Verify /api/stripe/checkout returns redirect to status, not Stripe session URL.
- [ ] **Dev-mode Stripe unavailability (stripeAvailable() false)** — _eng · S_
  - _Why:_ Local dev without STRIPE_SECRET_KEY should still let outreach flow work (dryrun). Code path: POST /api/stripe/checkout → !stripeAvailable() → sendOutreach + redirect. Verify graceful fallback.
  - _How:_ Unset STRIPE_SECRET_KEY in .env.local (or set to empty string). Run full flow. /api/stripe/checkout should log a Stripe unavailable error (requireServer throws), which propagates. Actually, the code checks stripeAvailable() before calling stripe(), so it should work. Verify: POST /api/stripe/checkout → !stripeAvailable() → sendOutreach + redirect to status?dryrun=1. Verify negotiation_outreach.status='dry_run'.
- [ ] **Zero homes found fallback (no contactable homes in zip)** — _eng · M_
  - _Why:_ Lookup can fail: no funeral_homes rows for zip (directory empty), all non-vetted, all denylisted. Family should see clear message, pay nothing, have option to use free tools.
  - _How:_ In Supabase, clear the funeral_homes table (or set all to active=false or vetted=false). Run /negotiate/start with a zip code. Check: findHomesFromDirectory returns [] or falls back to placeholder homes. POST /negotiate/start stores 0 rows in negotiation_outreach. Page redirects to /negotiate/[id]/preview. Preview page shows warning card: 'We couldn't find homes to contact here' + links to /prices (free lookup) and /dashboard. Verify no 'Pay now' button, no Stripe checkout possible. Also test: funeral_homes table has rows but all have email=null → should be filtered out in directory.ts (not email, is, null).
- [ ] **Double-submit on preview page (idempotent payment)** — _eng · S_
  - _Why:_ Family clicks 'Pay now' twice, hits back, form refreshes. Stripe checkout session is idempotent by design (one session ID per negotiation_id), but verify form submission guards.
  - _How:_ Full flow to /negotiate/[id]/preview. Click 'Pay now' → Stripe Checkout. Before completing payment, open browser dev tools Network tab. Simulate: close Stripe tab, return to preview, click 'Pay now' again. Verify: form POST to /api/stripe/checkout still uses same negotiationId. Stripe call may re-create session or return existing; either way, next payment should work. Also test: complete payment, webhook fires, status moves to 'contacting'. Click back to preview (if browser allows, e.g., no form resubmit dialog). Page should detect neg.status != 'pending_payment' and redirect to /negotiate/[id]/status.
- [ ] **Auth flow edge cases (not signed in, session expired, wrong user)** — _eng · M_
  - _Why:_ Negotiation has user_id RLS. Every endpoint checks auth and verifies ownership. Verify 404, 401, redirect-to-login behavior.
  - _How:_ Tests: (1) Unauthenticated access to /negotiate/start → requireSignedIn redirects to /login?next=/negotiate/start; (2) POST /api/negotiate/start without auth → 401 auth error; (3) Create negotiation as user A, try to access as user B (via direct /negotiate/[id]/preview URL or API) → 404 not_found or redirect; (4) Session expires during checkout (sign out, try to POST /api/stripe/checkout) → should redirect to /login; (5) NEXT_PUBLIC_SUPABASE_URL or anon key missing → Supabase client fails; verify error is caught and displayed (not a silent 500).
- [ ] **Stripe test card flows (decline, 3D Secure, expired card)** — _eng · M_
  - _Why:_ Real payment processing must handle declined cards gracefully. Stripe test mode provides cards that trigger these scenarios.
  - _How:_ Use Stripe test cards: 4000 0000 0000 0002 (card_declined), 4000 0025 0000 3155 (3d_secure), 4000 0000 0000 0069 (lost_card). Run checkout flow with each. Verify: (1) declined card → Stripe Checkout shows error, family can retry; (2) success_url not hit, negotiation status stays 'pending_payment'; (3) family can hit back, click 'Pay now' again to retry; (4) successful retry completes flow. Also verify: if OUTREACH_LIVE=false, even successful payment results in 'dry_run' outreach (not 'sent').
- [ ] **Webhook fired but negotiationId missing or invalid** — _eng · S_
  - _Why:_ Webhook payload should always have metadata.negotiationId. Edge case: old checkout session without it, or typo. Verify silently accepted (200), no outreach sent.
  - _How:_ Manually POST /api/stripe/webhook with valid signature but metadata.negotiationId missing or set to non-existent UUID. Verify: webhook returns 200 received=true (idempotent, no-op), no error logged, negotiation_outreach not modified. Check: sendOutreachForNegotiation is only called if negotiationId exists (guard: if (negotiationId) { ... }).
- [ ] **Stripe checkout session.payment_status=paid reconciliation (status page polling)** — _eng · M_
  - _Why:_ If webhook is delayed or missed, /api/negotiate/[id] should self-heal by checking Stripe. Verify this only runs when status='pending_payment' and stripe_payment_intent_id is set.
  - _How:_ Run checkout flow, get to Stripe Checkout page, complete payment. Before webhook fires (or simulate it being delayed), manually call GET /api/negotiate/[id]. Verify: response includes self-healing logic that checks session.payment_status='paid', calls sendOutreachForNegotiation if so, returns updated status='contacting'. Also verify: reconciliation only runs when status='pending_payment' (skip if already 'contacting' or 'closed'); when stripeAvailable() is true (skip if Stripe not configured).
- [ ] **Funeral home email denylist (isEmailDenylisted) at send time** — _eng · S_
  - _Why:_ Belt-and-suspenders: code-level denylist filters before storing in DB (/api/negotiate/start), and again at send time (sendOutreachForNegotiation). Verify both layers.
  - _How:_ Check /lib/negotiation/denylist.ts for the list. Test: (1) Populate funeral_homes with a denylisted email (e.g., 'no-reply@...'); (2) Run /api/negotiate/start → denylisted home should not appear in negotiation_outreach rows or should be filtered; (3) Manually add a denylisted row to negotiation_outreach with status='pending'; (4) Trigger sendOutreachForNegotiation → row should be updated to status='declined', not sent; (5) Verify logs show 'skipped' count increased. Code location: /lib/negotiation/send.ts line 42, /lib/negotiation/directory.ts line 85.
- [ ] **Resend email API failure (down, rate limit, bad key)** — _eng · M_
  - _Why:_ Resend is required for outreach. If down, emails fail to send. Verify: (1) silent failure with retry backoff, (2) rows left as 'pending' for later retry, (3) webhook can be replayed to retry.
  - _How:_ Test: (1) Set RESEND_API_KEY to invalid key. Run end-to-end flow. Payment succeeds, webhook fires, sendOutreachForNegotiation tries to send emails. Verify: (a) Resend call fails (async error), (b) try-catch in send.ts catches it (line 65 catch block), (c) row left as 'pending' (not updated), (d) sent count is 0; (2) Manually replay webhook (Stripe Dashboard → Resend) → sendOutreachForNegotiation retries pending rows. (3) Set RESEND_API_KEY to empty → code should gracefully handle (requireServer throws or FEATURES.email() returns false; verify fallback works).
- [ ] **Supabase (database, auth, RLS) unavailable or misconfigured** — _eng · M_
  - _Why:_ If Supabase is down, all flows fail. Verify error handling and user-facing messaging.
  - _How:_ Tests: (1) Unset NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY → createClient() fails; verify error is caught and user sees message (not blank page or 500). (2) Set URL to invalid host → network timeout; verify timeout is caught. (3) Test Supabase RLS: negotiation has user_id RLS. Create negotiation as user A, try to read as user B → Supabase should return 0 rows. Verify app handles gracefully (404 or error message). (4) Check profile auto-creation trigger (handle_new_user): does it fail silently if profiles table is down? (5) Test: missing SUPABASE_SERVICE_ROLE_KEY when trying to send outreach → requireServer throws; webhook should log the error and fail (verify it doesn't crash the server).
- [ ] **Navigation and state after payment (back button, refresh, browser close/reopen)** — _eng · M_
  - _Why:_ Family completes payment, lands on status page. What happens if they refresh, go back, close browser? Verify idempotent state.
  - _How:_ Run checkout flow to payment. Complete payment → success_url redirects to /negotiate/[id]/status?session_id={id}. Verify: (1) Refresh status page → GET /api/negotiate/[id] runs reconciliation again (idempotent, status already 'contacting', no double-send). (2) Hit back button → preview page. Should detect status != 'pending_payment' and redirect to status page (see /negotiate/[id]/preview page.tsx line 40-43). (3) Close browser, reopen, navigate to /negotiate/[id]/preview → same redirect. (4) Session expires, log out, log back in → auth check redirects to /login, then to preview/status as needed.
- [ ] **Form validation and error messages (bad zip, missing name, auth not accepted)** — _eng · M_
  - _Why:_ Wizard on /negotiate/start has required fields. Verify client/server validation and clear error feedback.
  - _How:_ Test /negotiate/start/Wizard.tsx form: (1) Submit with empty zip → validation error (client or server); (2) Submit with invalid zip format (< 3 chars) → Zod validation on server (Body.zip.min(3)) returns 400 with error.format(); (3) Submit without authorizationAccepted checkbox → 400 authorization_required (line 50-54 in /api/negotiate/start/route.ts); (4) Submit with senderFirstName missing → Zod validation error; (5) Submit with radii out of bounds (< 5 or > 100) → Zod validation (Body.radiusMiles.min(5).max(100)). Verify: clear error messages displayed to family, not raw Zod JSON.
- [ ] **Home selection (choose flow) — double-submit and closed negotiation guard** — _eng · M_
  - _Why:_ Family picks a home on /results. Endpoint: POST /api/negotiate/choose. Idempotent: only moves status to 'closed' once (line 50-55). Verify guard works.
  - _How:_ Full flow: create negotiation, pay, receive quotes. On /results, click 'Choose this home' for one of the outreach rows. Form POSTs: negotiationId, outreachId. Verify: (1) Negotiation status updates from 'contacting' → 'closed'; (2) notifyChosenHome() is called once; (3) Conditional update (neq('status', 'closed')) prevents double notification if form submitted twice (idempotent). (4) If you re-submit the form, status is already 'closed', second update returns 0 rows, notification not re-sent. (5) Redirect to /negotiate/[id]/closed?included=1 both times (page doesn't distinguish single vs double-submit, which is fine).
- [ ] **Results page (view outreach, update quote status, no results yet state)** — _eng · M_
  - _Why:_ Status page periodically polls API for outreach replies. Must handle: 0 replies, 1–3 replies, message threading.
  - _How:_ Path: /negotiate/[id]/status (client-side polling) and /negotiate/[id]/results (older results view). Status page: (1) Right after payment (webhook hasn't fired yet), page shows 'Negotiation in progress...'. (2) Webhook fires, outreach rows move to 'sent' (or 'dry_run'). Page polls every 6 sec; after reconciliation, should show homes in outreach. (3) Sister manually updates negotiation_outreach.quote_cents for one home (e.g., 50000 cents = $500). Next poll shows this home with quote. (4) Zero replies: page shows 'We're waiting...' messaging. (5) Multiple replies: page shows best 2–3 by quote_cents (logic in /results/page.tsx). Verify no crashes if outreach is empty.
- [ ] **Cross-browser testing (Safari, Chrome, Firefox, mobile Safari, Android Chrome)** — _eng · L_
  - _Why:_ Families are in crisis, may use any device. Form submission, Stripe Checkout redirect, polling, layout must work everywhere.
  - _How:_ Test full flow on: (1) Chrome desktop; (2) Firefox desktop; (3) Safari desktop (macOS); (4) Safari mobile (iOS 17+); (5) Chrome mobile (Android). Focus: (a) Form inputs render and submit; (b) Stripe Checkout session redirects correctly; (c) Status page polling works (setInterval on 'use client' component); (d) Layout responsive (mobile-first TW v4); (e) No console errors. Known issue to check: Next.js 16 + React 19 may have hydration edge cases; watch for 'Cannot hydrate' errors on /negotiate/[id]/status (use client component with useState).
- [ ] **Empty states and error messaging (no homes, API timeout, auth required messaging)** — _eng · M_
  - _Why:_ Families in crisis need clear, kind messages, not technical jargon. Verify every error path has UX-friendly copy.
  - _How:_ Scenarios: (1) Zero homes found → /negotiate/[id]/preview shows Card tone='warn' + 'We couldn't find homes to contact here' + button to /prices; (2) API error during start (e.g., Supabase down) → POST /api/negotiate/start returns error; Wizard should display message (not raw JSON). Check: Wizard.tsx error handling (currently doesn't show error state visible). (3) Auth required → /login with next= parameter set. (4) Not found (wrong negotiation ID) → 404 page (does one exist? Check for error.tsx in app/negotiate/[id]/). (5) Stripe checkout fails (Stripe down) → Stripe.checkout.sessions.create throws; catch and redirect with error message (currently no catch visible in /api/stripe/checkout; verify error is handled).
- [ ] **Admin tools readiness (/admin/funeral-homes, /admin/faith-qa, vetting flow)** — _eng · M_
  - _Why:_ Sister uses admin tools to vet homes (set vetted=true). Verify: /admin routes are behind ADMIN_EMAILS gate, directory lookup respects vetted flag.
  - _How:_ Tests: (1) Import funeral_homes CSV into Supabase funeral_homes table. Check: all rows have active=true but vetted=false initially. (2) Access /admin/funeral-homes without ADMIN_EMAILS set → should allow (or should it? check code). (3) Set ADMIN_EMAILS=sister@example.com. Access /admin/funeral-homes as different user → 403 or redirect to login. (4) Access as sister@example.com → admin page loads, can toggle vetted=true for homes. (5) Run /api/negotiate/start → should only return homes where vetted=true (and active=true, and email is not null). Unvetted homes are skipped. (6) Sister marks home as vetted in Supabase Table Editor → next /negotiate/start uses it. Verify the vetting gate prevents unreviewed homes from being emailed.
- [ ] **Dry-run logging and audit trail (confirm outreach was prepared, not sent)** — _eng · S_
  - _Why:_ Pre-launch, all outreach is dry_run. Logs and DB must record what would have been sent (email body, home name) for review.
  - _How:_ Run full flow with OUTREACH_LIVE=false. After payment: (1) negotiation_outreach rows have status='dry_run' (not 'sent'); (2) initial_email_id is null (no email actually sent); (3) Vercel logs show '[webhook] sendOutreach neg=... sent=0 dryRun=N skipped=M'; (4) Resend logs (or console if RESEND_API_KEY not set) shows '[email:dryrun]' entries with to=home_email, subject, bodyPreview (first 200 chars of email body). Verify: email body includes authorizationId, familyLabel, opt-out link, postal address line. Print one dry_run email and review for typos/clarity before flipping OUTREACH_LIVE=true.
- [ ] **Responsiveness and accessibility (WCAG 2.1 AA, form labels, error focus)** — _eng · M_
  - _Why:_ Grieving families may be elderly, on mobile, using screen readers. Verify semantic HTML and label associations.
  - _How:_ Quick audit (no formal WCAG scan needed for MVP): (1) Form inputs on /negotiate/start have <label> elements associated (htmlFor attribute); (2) Error messages are role='alert' or live region (screen reader announces them); (3) Button text is descriptive ('Pay now', not 'Submit'); (4) Contrast ratio (text vs background) meets AA standard (check with browser DevTools or Lighthouse); (5) Mobile: viewport is set (meta name='viewport' in layout.tsx), tap targets are 44x44px minimum; (6) Stripe Checkout is self-managed by Stripe (inherits a11y responsibility).
- [ ] **Webhook resilience (Vercel cold starts, transient network errors, retries)** — _eng · M_
  - _Why:_ Stripe webhook is async and critical. Vercel Hobby tier may have cold starts. Webhook must tolerate this.
  - _How:_ Resilience measures already in place: (1) sendOutreachForNegotiation is idempotent (only 'pending' rows sent); (2) Status page reconciliation (polling GET /api/negotiate/[id]) self-heals if webhook is delayed; (3) Stripe auto-retries webhook if no 200 response (max 3 days of retries). Test: (1) Webhook endpoint gets hit, sendOutreachForNegotiation throws (simulate by making a bad DB call) → catch should prevent 500, still return 200; (2) Stripe retries webhook 2+ times → idempotency ensures no double-send; (3) Vercel cold start (next deploy) — webhook fires and works (not suspended or timed out).
- [ ] **Funeral home directory import and search correctness (zip prefix fallback, ordering)** — _eng · M_
  - _Why:_ findHomesFromDirectory uses: exact zip match → 3-digit prefix → rest. If city spans multiple zips or has no exact match, fallback must work.
  - _How:_ Setup: Import funeral_homes CSV for launch city. Include homes in: exact zip (e.g., 84101), same 3-digit prefix (e.g., 841xx but different last 2), different 3-digit prefix. Test: (1) Query for zip 84101 (exact match exists) → results prioritize exact, then prefix, then rest; (2) Query for zip 84102 (exact doesn't exist, prefix does) → returns prefix + rest; (3) Query for out-of-state zip 10001 → returns rest (national fallback). Verify: /lib/negotiation/directory.ts line 39–43 implements this. Also test: select(n) parameter (homesForRadius maps radius to count) limits results correctly.
- [ ] **Session and cookie security (Supabase auth, SameSite, HTTPS)** — _eng · S_
  - _Why:_ Auth tokens must be secure. Supabase SSR client should use secure cookie settings.
  - _How:_ Verify: (1) middleware.ts uses Supabase SSR client (imports from @supabase/ssr) for cookie refresh on every request; (2) Auth cookies are HttpOnly, Secure, SameSite=Lax (set by Supabase, not manually); (3) NEXT_PUBLIC_APP_URL in prod is HTTPS (e.g., https://honestfuneral.co); (4) Supabase Auth → URL Configuration → Site URL and Redirect URLs include prod HTTPS URL; (5) Test: sign in on prod → browser DevTools Cookies tab shows supabase auth token (should have HttpOnly flag, no direct JS access). Check: /lib/supabase/server.ts and /lib/supabase/client.ts import ssr client correctly.
- [ ] **Logging and monitoring setup (Vercel Logs, Sentry or equivalent optional)** — _eng · S_ ⚠️PARTIAL
  - _Why:_ Launch day: need visibility into errors. Vercel provides basic logs. Verify key events are logged.
  - _How:_ Current logging: console.info/error statements in API routes (e.g., '[webhook] sendOutreach', '[choose] notifyChosenHome'). On Vercel: Deployments → (latest) → Logs (collects all console output). Test: run full flow, check Vercel Logs for: '[webhook] sendOutreach neg=... sent=X dryRun=Y', '[stripe/checkout] session created', any errors in negotiation_outreach queries. Optional Phase 2: add Sentry or Datadog for structured error tracking, uptime monitoring, metrics.


## 🟡 P2 — Soon after launch — 38 items

### Config, Environment & Infrastructure
- [ ] **NEXT_PUBLIC_HELP_PHONE: set to correct support number** — _YOU · S_ ⚠️PARTIAL
  - _Why:_ Displayed in footer (HelpFooter component). Public info for families to call support.
  - _How:_ .env.example line 14 = '+13855531141' (placeholder). Set to actual support number in E.164 format. .env.local already has this value.
- [ ] **NEXT_PUBLIC_HELP_HOURS: optional hours line in footer** — _YOU · S_
  - _Why:_ When support is available. Not required but improves UX.
  - _How:_ .env.example line 15 (commented out). Set to text like '9am–9pm ET, every day'. Optional.
- [ ] **Domain email inboxes: set up arrangements@, hello@, legal@, privacy@, safety@** — _YOU · M_
  - _Why:_ Mentioned in app (our-role/page.tsx, privacy/page.tsx). Families/homes need real mailboxes to contact.
  - _How:_ Create aliases or forwarding addresses at email provider (Gmail workspace, Microsoft, etc.). All should forward to one or more team member inboxes. Resend domain must be verified at registrar before sending from these addresses.
- [ ] **Document all secrets location (keeper/1Password/LastPass) with recovery path** — _YOU · S_
  - _Why:_ If Vercel account compromised, need to rotate Stripe/Resend/Supabase keys. Backup plan required.
  - _How:_ Store: Vercel login, Stripe API keys + webhook secret, Resend API key, Supabase service-role key, Postmark server token, domain registrar login. Use password manager with shared access to sister.
- [ ] **HONEST_FUNERAL_FREE_EMAILS: set to founder + QA emails for testing** — _eng · S_
  - _Why:_ Bypasses $49 fee for test users. Allows Ryan + sister to run full outreach flow without paying. Optional comma-separated list (case-insensitive).
  - _How:_ .env.example line 57. Set to ryan@, sister@ email addresses for dev testing. Leave blank in production (or set to Ryan's email if he needs to test).
- [ ] **Vercel production preview deployments disabled (or gated)** — _eng · S_
  - _Why:_ Optional but recommended: prevent accidental leaks of secrets in preview URLs shared with non-team members.
  - _How:_ Vercel Project Settings → Git → Disable preview environments, OR restrict deployment previews to team members. Not required but best practice.

### Data & Content
- [ ] **HONEST_FUNERAL_FREE_EMAILS QA bypass is configured (optional)** — _eng · S_
  - _Why:_ env.ts and .env.example document HONEST_FUNERAL_FREE_EMAILS comma-separated list: emails that run full outreach WITHOUT paying the $49 fee. Useful for Ryan's own login + QA before launch. Not required but improves testing.
  - _How:_ .env.example line 56-57: 'HONEST_FUNERAL_FREE_EMAILS='. lib/env.ts does not directly expose this variable; it's checked during payment reconciliation. Task: (1) set HONEST_FUNERAL_FREE_EMAILS=ryan@honestfuneral.co,qa@honestfuneral.co (or your QA emails) in .env.local on staging, (2) test a full negotiate flow from those emails, (3) verify payment is skipped but outreach still sends (when OUTREACH_LIVE=true), (4) useful for testing without burning Stripe test credits.
- [ ] **Inbound Postmark webhook is optionally configured (future expansion)** — _eng · S_
  - _Why:_ .env.example lines 64-70 document POSTMARK_INBOUND_USER/SECRET for parsing funeral-home replies to outreach emails. Not required for V1 Utah launch but enables future follow-up automation. app/api/inbound/resend-webhook/route.ts handles bounces + opt-outs.
  - _How:_ Postmark inbound parsing setup (POSTMARK_INBOUND_USER/SECRET) + /api/inbound/resend-webhook route. For V1: not required. For V2 (automated follow-ups): set up Postmark inbound route pointing to /api/inbound/resend-webhook with basic-auth headers, then decode POSTMARK_INBOUND_USER/SECRET. app/api/inbound/resend-webhook/route.ts handles 'bounced', 'unsubscribed', 'spam' events by marking negotiation_outreach status or funeral_homes.active=false.

### Payments, Email & Deliverability
- [ ] **Document payment reconciliation & chargebacks policy** — _YOU · S_
  - _Why:_ Code stores stripe_payment_intent_id but has no chargeback handling. If a family disputes the $49, Stripe will issue a chargeback. No policy exists for when/how to respond.
  - _How:_ Create /docs/PAYMENT_DISPUTES.md: (1) Stripe sends chargeback notification email + dashboard alert. (2) Typical dispute: customer claims 'unauthorized' or 'product not as described'. (3) Response strategy: (a) if refund already issued, respond with proof (email confirmation). (b) if no refund yet, issue one + explain it's within 14-day window. (c) if >14 days, respond with email trail showing family received outreach results. (4) Keep records: negotiation status, outreach sent date, family replies. (5) Likelihood is low (upfront payment, families chose to pay) but prepare defensible evidence.
- [ ] **Document bounce + complaint threshold policy** — _YOU · S_
  - _Why:_ Resend tracks bounce & complaint rates. If honestfuneral.co domain reaches high rates (varies by ISP, ~0.5%+ bounces), ISPs may throttle or block. No threshold/escalation plan is in code.
  - _How:_ Create /docs/EMAIL_HEALTH_MONITORING.md: (1) Weekly check Resend dashboard for bounce rate (should be <0.5%) + complaint rate (should be 0%). (2) If bounce >0.5%: investigate which homes are bouncing (check funeral_homes.active=false logs), reach out to Sister to verify addresses, blacklist SaaS domains. (3) If complaints >0%: investigate which homes/families complained, reach out to FD to understand issue (timing, email content clarity). (4) If domain reaches sender score warning, contact Resend support. (5) Warmup strategy (above) prevents high rates on launch.
- [ ] **Verify Stripe fee transparency & reconciliation** — _YOU · S_
  - _Why:_ FLAT_FEE_CENTS = 4900 cents ($49). Code stores it in negotiations.fee_cents but doesn't reconcile: Stripe charges 2.9% + $0.30 per transaction. Founder needs to know actual net revenue after fees.
  - _How:_ Document in /docs/FINANCIAL_TRACKING.md: (1) Stripe applies 2.9% + $0.30 per checkout session. On $49 charge: $49 * 0.029 = $1.42 + $0.30 = ~$1.72 fees → net $47.28 per charge. (2) Track actual payouts via Stripe Dashboard → Payouts. (3) Monthly: compare Stripe payout vs negotiation row count * $49 to verify no missing charges/refunds. (4) For accounting: Stripe sends CSV exports of transactions; use for bookkeeping.
- [ ] **Implement in-product refund request UI + approval workflow** — _eng · M_
  - _Why:_ Manual refund process doesn't scale past 10 deals. Code comments (LAUNCH_PLAYBOOK line 104) explicitly call this out as post-launch work, but without it, every refund request after deal 10 is a toil spike.
  - _How:_ Design: (1) Add a 'Request Refund' button on /negotiate/[id]/closed page (visible if closed ≤ 14 days ago); (2) Modal form captures reason + supporting notes; (3) Creates a record in a new 'refund_requests' table (negotiation_id, reason, notes, requested_at, approved_at, approved_by, stripe_refund_id); (4) Notification to support@honestfuneral.co with link to /admin/refunds; (5) Admin page lists pending requests, allows approve/deny with Stripe refund API call on approve. Once approved, emit email to family confirming. Code location: lib/stripe.ts already has stripe() client + FLAT_FEE_CENTS; call stripe().refunds.create({charge: stripeChargeId, reason: 'requested_by_customer'}).
- [ ] **Set up email suppression list for known-bad addresses** — _eng · S_ ⚠️PARTIAL
  - _Why:_ lib/negotiation/denylist.ts defines RAW_DENYLIST (currently empty). Sister will discover bad addresses (SaaS domains, generic inboxes, personal emails asking to stop). Without a suppression process, bad addresses can be re-sent to after a DB reset.
  - _How:_ Denylist is checked before every send (lib/negotiation/send.ts line 42; app/api/negotiate/[id]/messages/route.ts line 64). Add a process: (1) Sister flags bad email + reason in /admin/vetting (add a 'denylist' flag?). (2) Ryan periodically exports new entries → updates lib/negotiation/denylist.ts RAW_DENYLIST. (3) Redeploy. Alternatively, move to DB table (denylist_entries) + load in memory on startup if list grows large. For now, hardcoding is fine.
- [ ] **Implement outreach reconciliation on status page GET** — _eng · M_
  - _Why:_ Code comment (app/api/stripe/webhook/route.ts line 40–42) says 'status page reconciliation is the backup,' but no reconciliation code exists. If the webhook fails, payment succeeds in Stripe but outreach never sends.
  - _How:_ On /negotiate/[id]/status page load: (1) Check if negotiation.status = 'pending_payment' and stripe_payment_intent_id is set. (2) If so, call Stripe API to check session status. (3) If session.payment_status = 'paid', call sendOutreachForNegotiation(admin, negotiationId) (idempotent; only 'pending' rows send). (4) Emit a log entry for audit trail. (5) This is a best-effort backup; the webhook is the primary path. Code location: likely a server action or GET endpoint called when the status page mounts or polls.
- [ ] **Implement admin dashboard for refunds, disputes, and outreach monitoring** — _eng · L_
  - _Why:_ Sister needs to see: pending refund requests, bounce/complaint logs, outreach status per negotiation. No /admin/refunds or /admin/outreach-monitor page exists yet.
  - _How:_ Pages to build: (1) /admin/refunds: list negotiation_refund_requests (if table created), approve/deny with Stripe API call. (2) /admin/outreach-monitor: list all negotiation_outreach rows, filter by status (pending, sent, dry_run, replied, no-reply, declined), show email, home name, sent_at, bounce/complaint status. (3) /admin/email-deliverability: chart of bounces/complaints over time, list of flipped-inactive homes + reasons. Gated by isAdminEmail() check. Use existing service-role queries as foundation.

### Legal & Compliance
- [ ] **Confirm no cookie/consent management required for analytics** — _eng · S_
  - _Why:_ Privacy Policy mentions 'basic usage analytics' and 'city-level geography, discarding full IPs.' Must verify: (a) which analytics provider is used (none found in code yet—may be integrated via Vercel); (b) is consent required? (c) any GDPR/CCPA cookie consent banner needed?
  - _How:_ Files: app/privacy/page.tsx (§2), app/layout.tsx or similar (look for analytics script tags). If using Vercel Analytics or similar: (1) verify it's configured with privacy mode (no IP, no cookie); (2) counsel advises on consent requirement (typically not needed if analytics is privacy-first); (3) if GDPR applies, add opt-out link. Currently no Google Analytics or similar tracking found—likely fine.
- [ ] **If pursuing secondary referral revenue (insurance/financing/legal), obtain licensing requirements per referral type** — _legal · L_
  - _Why:_ LAWYER_BRIEF §H notes insurance referrals may require producer licensing, financing may implicate lending/credit rules, attorney referrals implicate bar referral rules. Currently secondary revenue is planned but not live. Must clear before any referral links go live. (CLAIMS_VALIDATION shows this is Phase 2, not launch.)
  - _How:_ Files: docs/LAWYER_BRIEF.md §H, app/terms/page.tsx §4 (referral disclosure already drafted). Action: (1) if referral page is live at launch, counsel confirms each referral type (insurance, financing, legal, cremation, casket vendors) complies with rules; (2) add specific disclosures (e.g., 'we earn referral fees from the following providers'). Currently, product has placeholder referral logic but not active revenue. Can defer until post-launch if no referral links are live.
- [ ] **ADA/accessibility legal audit—identify any exposure under Title III** — _legal · M_
  - _Why:_ No mention of ADA compliance or accessibility audit found in codebase. Honest Funeral is a public website open to anyone; Title III of the ADA may apply. Must conduct accessibility audit (WCAG 2.1 AA compliance) and determine any legal exposure. Low urgency if site is already reasonably accessible, but should be on the list.
  - _How:_ Action: (1) run WAVE, AXE, or similar accessibility scanner on key user flows (crisis entry, price lookup, outreach, dashboard); (2) counsel advises on ADA Title III risk for web-app; (3) any critical fixes (alt text, ARIA labels, color contrast) should be prioritized; remediation can be phased post-launch if no major blockers found. React + Tailwind should support accessibility well if properly configured.

### Security, Auth & Data Protection
- [ ] **Harden /api/inbound/email and /api/inbound/resend-webhook signature verification** — _eng · S_ ⚠️PARTIAL
  - _Why:_ Both webhook handlers verify signatures (Postmark Basic Auth, Resend Svix HMAC). Verification is correct, but logging includes email addresses and raw payloads. Webhook body could be stored without redaction.
  - _How:_ Files: /Users/ryancurrie/FH/app/api/inbound/email/route.ts, /Users/ryancurrie/FH/app/api/inbound/resend-webhook/route.ts. Confirm raw_payload in negotiation_messages is only logged internally (not sent to external services). Redact email from console logs (done; logs only show count/outcome, not email).

### Observability, Ops & Support
- [ ] **Document expected webhook latency and failure modes** — _YOU · S_
  - _Why:_ No SLA documented for when a family should expect their outreach to be sent, when they'll see responses, etc. This affects support expectations.
  - _How:_ Create docs/SLA.md: (1) Payment processing: immediately, Stripe webhook within 5 min; (2) Outreach send: within 5 min of payment confirmation; (3) Response arrival: depends on funeral homes (typically 24-72 hours); (4) Family notification: within 1 hour of response received via email; (5) Failure modes: if payment confirmed but no outreach within 30 min → check /negotiate/[id] status page (triggers reconciliation) or email support. Share expected timeline with families in UX (status page).
- [ ] **Create a health/status endpoint** — _eng · S_
  - _Why:_ No way to programmatically check if the app is healthy. Status page manual checks. For launch, need /api/health to report Supabase, Stripe, and Resend connectivity.
  - _How:_ Create /api/health/route.ts (runtime='nodejs'): return {status: 'healthy'|'degraded'|'down', checks: {supabase: ok, stripe: ok, resend: ok}, timestamp}. Call simple queries: Supabase ping (select now()), Stripe API key validation, Resend API key validation. Endpoint returns 200 if all OK, 503 if any fail. Add to uptime monitor (Vercel, Better Uptime, or simple cron).
- [ ] **Add analytics/funnel tracking** — _eng · M_
  - _Why:_ No visibility into user flow: how many reach /negotiate/start, how many convert to paid, drop-off rates. This is free tier in most tools and essential for understanding product-market fit.
  - _How:_ Install PostHog (generous free tier, Next.js-friendly) or Segment + Mixpanel. Instrument: (1) Page view events; (2) Negotiation started; (3) Payment initiated; (4) Payment succeeded; (5) Outreach sent; (6) Quote received; (7) Home chosen. Avoid tracking PII (deceased name, real quotes). Add NEXT_PUBLIC_POSTHOG_KEY to .env.example if chosen. Dashboard shows funnel: /negotiate/start → /negotiate/[id]/preview → payment page → confirm payment → status.
- [ ] **Add monitoring for negotiation_outreach rows stuck in 'pending' status** — _eng · M_
  - _Why:_ If a send fails and is never retried, outreach rows stay pending forever. Sister won't see which homes weren't contacted. Need a report or alert.
  - _How:_ Create /api/admin/pending-outreach/route.ts that queries for any negotiation_outreach rows in 'pending' status created > 24 hours ago + corresponding negotiation is not in dry_run mode. Return list with details. Add to admin dashboard. Alternatively, add daily cron that checks this and logs a warning if any found. Sister can use this to manually retry or investigate.
- [ ] **Add request ID / correlation ID logging throughout the stack** — _eng · M_
  - _Why:_ Currently impossible to trace a single family's transaction across multiple services (Stripe → webhook → Resend → Postmark). Each log entry is isolated.
  - _How:_ Middleware.ts or lib/request-id.ts: generate request ID on every HTTP request, store in request context (Next.js Headers or AsyncLocalStorage). Include in all logs: Sentry, console, structured logs. Format: negotiationId-timestamp-uuid (e.g., abc-1234567890-xyz). Log it on: checkout initiated, webhook received, outreach sent, bounce received. Makes end-to-end tracing trivial for support.
- [ ] **Log webhook signature verification failures explicitly** — _eng · S_ ⚠️PARTIAL
  - _Why:_ Both Stripe and Postmark inbound webhooks verify signatures. Verification failure is currently logged but could be more detailed for debugging spoofed/tampered requests.
  - _How:_ In /api/stripe/webhook and /api/inbound/email and /api/inbound/resend-webhook, when signature verification fails, log: {event: 'webhook_verification_failed', service: 'stripe|postmark|resend', ip: request.ip, timestamp}. Already returns 400, which is correct. Add to structured logging so Sentry captures. Helps detect replay attacks or misconfiguration.

### SEO, Performance, Accessibility & Mobile
- [ ] **Add structured data (Article schema) to all content pages currently missing it** — _eng · M_ ⚠️PARTIAL
  - _Why:_ Article JSON-LD increases rich snippet eligibility (FAQ, how-to, article panels in SERPs), boosts CTR, and improves content discoverability. Currently 23 pages use ArticleSchema or JsonLd, but many high-value content pages lack it (prep, planning, analyze, worksheet, decision-making guides).
  - _How:_ Pages WITH schema: /page.tsx (Organization ✓), /faq/page.tsx (FAQPage ✓), /guidance/[scenario]/page.tsx (Article ✓), /funeral-costs/[city]/page.tsx (Article ✓), /faith/[tradition]/page.tsx (uses JsonLd ✓), and 18+ article pages. Missing high-value schema: /prep/page.tsx, /worksheet/page.tsx, /decide/page.tsx, /how-it-works/page.tsx, /prices/page.tsx (is a calculator, add HowTo schema?), /plan-ahead/page.tsx, /final-days/page.tsx. Import ArticleSchema from /components/seo/ArticleSchema.tsx, call with slug, title, description, eyebrow. Or use /lib/article-schema.ts directly for custom schema.
- [ ] **Add canonical tags to all dynamic pages that may have URL variations (funeral-costs/city slugs, glossary terms, faith traditions, state estate pages)** — _eng · S_
  - _Why:_ Canonicals prevent duplicate content penalties when the same content is accessible via multiple URL patterns (e.g., 'cremation-cost' vs 'cremation costs'). Currently only static routes have explicit canonicals. Dynamic routes inherit from metadataBase but should declare their own.
  - _How:_ Already done: static pages in /page.tsx, /prices/layout.tsx, /decide/page.tsx, /about/page.tsx, /how-it-works/page.tsx. Add to dynamic generateMetadata functions: /funeral-costs/[city], /estate/[state], /faith/[tradition], /glossary/[slug] — add alternates: { canonical: `/route-path/${paramValue}` } to returned Metadata object. Example in /funeral-costs/[city]/page.tsx line 26–45 (generateMetadata function).
- [ ] **Implement sr-only class for screen-reader-only content (skip-to-content links, decorative aria-hidden elements, result summaries)** — _eng · M_
  - _Why:_ Screen-reader users need non-visual navigation cues (skip main content) and result clarity (search result count, form validation messages). sr-only (visually hidden but announced) is missing, forcing reliance on visual-only cues.
  - _How:_ Add to globals.css: .sr-only { position: absolute; width: 1px; height: 1px; padding: 0; margin: -1px; overflow: hidden; clip: rect(0, 0, 0, 0); white-space: nowrap; border-width: 0; } . Then add skip-to-main link in SiteHeader.tsx for keyboard users. Add sr-only status messages in forms (e.g., <span className='sr-only'>2 results found</span>).
- [ ] **Add missing metadata (title, description, canonical) to paywall redirect page (/app/paywall/page.tsx)** — _eng · S_
  - _Why:_ Paywall page redirects users, but if accessed directly or cached, needs proper metadata so redirect reason is clear. Currently no metadata, confusing crawlers.
  - _How:_ File: /Users/ryancurrie/FH/app/paywall/page.tsx. Add: export const metadata = { title: 'Upgrade to compare prices', description: 'This page has moved.', robots: { index: false } };
- [ ] **Add `preload` and `preconnect` link hints for critical resources (Google Fonts, Vercel CDN, Stripe/Resend domains)** — _eng · S_
  - _Why:_ Early resource loading signals (preload for Inter/Source Serif 4 fonts, preconnect for Stripe and Resend) reduce time-to-first-paint and improve CLS. Currently fonts are loaded via next/font with display: 'swap' (good) but preconnect to external services is missing.
  - _How:_ Add to root layout.tsx metadata export (Next.js 16 supports link hints in Metadata.metadataBase / alternates): add link hints for Stripe (js.stripe.com), Resend API (api.resend.com), and any external CDNs. Example: add to metadata object: link: [ { rel: 'preconnect', href: 'https://js.stripe.com' }, { rel: 'dns-prefetch', href: 'https://api.resend.com' } ].
- [ ] **Verify sitemap.xml completeness and lastModified dates; audit for orphaned URLs not in sitemap** — _eng · M_ ⚠️PARTIAL
  - _Why:_ Sitemaps guide crawlers to all indexable content. Current sitemap.ts (183+ routes, manually curated) is comprehensive but orphaned routes or routes added in future may be missed. New pages must be added to sitemap generation logic.
  - _How:_ File: /Users/ryancurrie/FH/app/sitemap.ts. Audit: ✓ Static routes (40+), ✓ dynamic scenario routes (4), ✓ after routes (3), ✓ faith routes (FAITH_TRADITIONS.length), ✓ estate routes (all state slugs), ✓ glossary routes, ✓ city routes. Check for missing: /login (noindex, correct to omit), /dashboard (noindex, omit), /negotiate/[id]/* (user-specific, noindex, correct). If any public content pages added in future, update sitemap.ts to include. Test: curl https://honestfuneral.co/sitemap.xml, verify all expected URLs present, sort by priority (1.0 > 0.9 > 0.8) makes sense.
- [ ] **Add reduced-motion support to any micro-interactions (button hover, collapse expand, page transitions) that use CSS animations** — _eng · S_ ⚠️PARTIAL
  - _Why:_ prefers-reduced-motion: reduce is already in globals.css (good ✓) but animations may still exist in individual components. Users with vestibular disorders or motion sensitivity must not see jarring transitions. WCAG 2.1 Level AA.
  - _How:_ globals.css already has: @media (prefers-reduced-motion: reduce) { animation-duration: 0.01ms !important; transition-duration: 0.01ms !important; }. Audit for other animations: check /components/ui/HashOpenDetails.tsx (details expand), /components/SiteHeader.tsx (menu slide), /components/ProgressBar.tsx (animation?), PriceCalculator (any transitions?). If custom animations found, wrap in @media (prefers-reduced-motion: reduce) with instant versions, or remove non-essential animations.
- [ ] **Add TypeScript/Zod validation for dynamic route params (city slugs, state abbreviations, faith tradition keys) to prevent malformed metadata generation** — _eng · M_ ⚠️PARTIAL
  - _Why:_ Dynamic routes accept user input (city slugs, state names). If invalid params passed, generateMetadata may fail silently or generate empty/malformed OG images. Error handling prevents 500s and ensures metadata always renders.
  - _How:_ Audit current dynamic routes: /funeral-costs/[city]/page.tsx (uses getCity(slug), checks !city → notFound ✓), /estate/[state]/page.tsx (validate state abbr), /faith/[tradition]/page.tsx (uses isValidKey() check ✓), /glossary/[slug]/page.tsx (checks getEntry(slug), notFound ✓), /guidance/[scenario]/page.tsx (checks VALID.includes() ✓). Already good validation in most. Ensure all generateMetadata functions call the same validators before accessing data.
- [ ] **Audit third-party scripts (Stripe, Resend, analytics) for layout shifts and blocking behavior; ensure they load async or deferred** — _eng · M_
  - _Why:_ Third-party scripts (Stripe, analytics beacons) can block page rendering and cause CLS if they resize layout on load. Currently no documented loading strategy. Async/defer these to avoid 'Main thread blocked' warnings in DevTools.
  - _How:_ Check /lib/stripe related imports and usage in /app/negotiate/[id]/preview/page.tsx and checkout flow. Ensure Stripe.js is loaded with async script tag (or via Next.js dynamic import). Resend is server-side only (good, no render blocking). If Google Analytics or other tracking added, ensure it loads async. Review /next.config.ts for script optimization settings. Test in Chrome DevTools Lighthouse: target 'First Contentful Paint' <1.8s.

### QA, Testing & Edge Cases
- [ ] **Anthropic Claude API failure (unavailable, rate limit, bad key)** — _eng · M_
  - _Why:_ Claude is optional (FEATURES.claude()). If down, negotiation email should degrade gracefully (use fallback text, not AI-generated personalization).
  - _How:_ Test: (1) Unset ANTHROPIC_API_KEY → /api/negotiate/start should still work, buildOutreachEmail uses fallback boilerplate (no AI personalization). Check /lib/negotiation/email-body.ts: does it call Claude? Currently, it builds deterministic emails (no Claude call visible). Verify: outreach emails are sent even if Claude is down. (2) If Claude is called elsewhere (e.g., for tailoring subject lines), set bad key or simulate timeout → verify graceful fallback (email sent with generic subject, not error).
- [ ] **Message threading and inbound email parsing readiness (infrastructure only)** — _eng · S_ ⚠️PARTIAL
  - _Why:_ negotiation_messages table exists (schema.sql, migration 2026-05-21). Inbound parsing via Postmark webhook not yet live. Verify schema, but don't test live parsing (out of scope for MVP).
  - _How:_ Check: (1) negotiation_messages table schema (migration 2026-05-21-coordinator-messages.sql); (2) /api/inbound/email/route.ts exists (Postmark webhook endpoint), but is it wired? (3) Verify POSTMARK_INBOUND_USER, POSTMARK_INBOUND_SECRET env vars are documented in .env.example; (4) For MVP launch, inbound parsing is disabled (status page note: 'For now, the family records what each home quotes them on the status page'). Do NOT wire Postmark for launch; plan for Phase 2.
- [ ] **Automated test suite setup (none currently exists)** — _eng · L_
  - _Why:_ Zero automated tests in package.json. Before expanding, establish testing framework (Jest/Vitest) and write critical-path tests.
  - _How:_ Scope for Phase 2, not MVP launch. But for a solo technical founder, at least one minimal smoke test would catch regressions. Options: (1) Jest + Supertest for API routes; (2) Vitest for unit tests; (3) Playwright/Cypress for E2E browser testing. Start with: test /api/negotiate/start (happy path + validation errors), test /api/stripe/checkout idempotency, test sendOutreachForNegotiation dry_run vs live. Once framework is in place, CI/CD can run tests before Vercel auto-deploy. For now, manual testing + LAUNCH_TEST_SCRIPT.md is the gate.
- [ ] **Performance and rate-limiting (concurrent checkouts, funeral home list load)** — _eng · M_ ⚠️PARTIAL
  - _Why:_ On launch day, multiple families may hit /negotiate/start and /api/stripe/checkout simultaneously. Verify: Supabase doesn't timeout, Stripe handles concurrency, funeral_homes query doesn't load all 1000s of rows in memory.
  - _How:_ Known issue (docs/FUNCTIONAL_AUDIT.md): directory.ts loads all funeral_homes rows in memory (no pagination or cursor). For MVP (30–60 homes), fine. For expansion (1000s), requires refactoring. Current fix: query filters by active=true, vetted=true, not-null email → should return ~30–50 rows max. Test: (1) Simulate 3–5 concurrent /api/negotiate/start requests → verify no deadlocks, all complete; (2) Monitor Supabase db connection pool (free tier: 10 concurrent conns); (3) Stripe Checkout sessions.create under load (should auto-queue). Load testing not required for MVP, but flag for Phase 2 if launch city gets >10/day orders.


## ✅ Already done (this session)

- [x] Sitemap + robots.txt completeness: verify no /admin or private routes are indexed
- [x] OUTREACH_LIVE kill-switch default is false; confirm blocking logic in all send paths
- [x] Enforce SUPABASE_SERVICE_ROLE_KEY is never leaked to client
- [x] Audit /api/negotiate/[id]/quote for authorization completeness
- [x] Verify Supabase auth email confirmation is enabled
- [x] Disable direct negotiation_outreach queries from client; enforce service-role-only writes
