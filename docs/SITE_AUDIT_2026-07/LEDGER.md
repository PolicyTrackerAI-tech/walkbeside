# Site Audit 2026-07 — Findings Ledger

Append-only. Every finding gets one row. Status flows LEAD → CONFIRMED / REJECTED →
FIXED / QUEUED / PARKED / KILLED. Severity: **P0** live safety/legal/privacy exposure ·
**P1** broken or dishonest functionality a real user can hit · **P2** misalignment/value
drag · **P3** polish. Criterion: **C**ontent / **F**unctionality / **U**se / **V**alue.

Rows below are pre-seeded from the 2026-07-27 mapping pass (main @ `c47a6a0`). Everything
is a LEAD until its audit day verifies it live.

## P0 candidates — verify first

| ID | Day | Sev | Crit | Finding | Status |
|---|---|---|---|---|---|
| A1-01 | A1 | P0 | F | `ADMIN_EMAILS` unset in prod would make EVERY logged-in user admin (`lib/admin.ts:34` permissive default; `lib/env.ts` only enforced under `OUTREACH_LIVE=true`, off). **FIXED (code): prod now fails closed** — unset allowlist denies everyone; dev stays permissive; test pins both branches; boot warning added. **FOUNDER: set `ADMIN_EMAILS` in Vercel before merge** (else team loses /admin). | FIXED + FOUNDER |
| A1-02 | A1 | P0 | F | `/funeral-home-opt-out` performed the `active=false` write on GET render — mail-scanner prefetch of the tokenized link could deactivate homes before a human clicked. **FIXED: confirm-button POST (server action); GET only validates + renders.** No live links exist yet (OUTREACH_LIVE never on). | FIXED |
| A1-03 | A1 | P1 | F | `/preferences/[id]` unsubscribe applied on GET via service role. **Downgraded P0→P1:** the anniversary email embeds the bare `/preferences/[id]` URL (verified `cron/anniversary/route.ts:149`), NOT `?action=unsubscribe`, so the email link never silently unsubscribed. **FIXED anyway: converted to POST server action.** The uuid-as-capability SMS-number concern (`/api/preferences/sms`) is a separate design tradeoff → A4. | FIXED |
| A2-01 | A2 | P0 | C/U | Negotiate flow completable by a real family today with "we're contacting funeral homes / most reply within 24 hours" while OUTREACH_LIVE is off — rows record `dry_run`, nothing sends, and the status page prints the literal string `dry_run` (status/page.tsx:521-527). Promoted from homepage, dashboard, analyzer, /prices, nurture email. **FIXED (honest-mode default, 2026-08-18): every in-case surface now derives its language from actual send state, not intent** — new `lib/negotiation/status-labels.ts` (`outreachStatusLabel` maps every enum to family-safe English, unknown → "In progress", unit-tested to never leak jargon; `anyOutreachSent` is the only condition under which any surface says "we're contacting"), `/api/negotiate/[id]` exposes display-only `outreachLive`, status page shows "Your outreach is prepared / nothing has gone out to any home" + per-row "Prepared — not sent" + the prepared request text behind a details toggle, wizard entry/authorize/submit disclose the pause ("Prepare my outreach →"), dashboard card same treatment, CaseStepper "We reach out"→"Outreach". Fabricated "most reply within 24 hours" stat (zero sends ever) removed in both modes. Verified in dev browser walk of all 8 wizard steps. Note: today's empty vetted directory means real families currently hit the (already honest) `no_homes_available` path — this trap armed the day the first SLC home got vetted. | FIXED |
| A8-01 | A8 | P0 | C | `/privacy` dated April 2026 predated the B2B2C pivot; zero mention of institutional partners, referral attribution, or aggregate de-identified partner reporting. **FIXED: `/privacy` rewritten (plain language, "Last updated: August 2026") against verified code — adds the partner section (referral code is a non-clinical reporting label, hospice transmits nothing, aggregate-only totals with n≥5 dollar suppression, attribution never affects ranking), corrects fossils the old policy carried (no "contracts/files you upload" — no upload storage exists; photos never stored; deletion is immediate not "30 days"; adds Twilio; Stripe holds no family payment). Not counsel-marked (item 6 of the retained engagement). | FIXED |
| A8-03 | A8 | P0 | F | **NEW (found verifying A8-01, corrects A1-C1).** `share_links` carried an anon RLS policy `select … using (expires_at > now())` with **no id predicate** (`2026-05-05-margaret-share-links.sql`): anyone with the public anon key could enumerate **every** non-expired family share payload in bulk (`GET /rest/v1/share_links?select=payload` — names, ZIP, target home, quoted price, notes, date of death, eulogy). Live anon-key probe returned **HTTP 200** (query allowed), `*/0` rows only because no non-expired rows exist right now — latent, not safe. A1-C1's "0 rows = airtight" read emptiness as denial. **FIXED (code): both `/api/share/*` routes moved to the service-role client, read scoped by id + expiry (mirrors `household_links`); lockdown migration `2026-08-18-share-links-lockdown.sql` drops the anon policies + revokes grants; BOOTSTRAP regenerated; VERIFY updated.** **FOUNDER: apply the migration in prod after deploy** (routes work either way; the migration is what closes the hole). | FIXED + FOUNDER |
| A8-04 | A8 | P1 | C | **NEW (Utah clearance §4 product requirement, PR #184).** No persistent "not a funeral home / we don't arrange or handle remains / you contract directly" disclaimer on the surfaces closest to "arranging" (unlicensed-funeral-directing exposure). **FIXED: pinned constant `NOT_A_FUNERAL_HOME` in `lib/copy.ts` (copy-law test added) rendered via `components/RoleDisclaimer.tsx` persistently under the whole `/negotiate` flow (layout) and on `/decide`, in the sitewide footer, and in the negotiate authorize step. Substance fixed pending counsel blessing; verified live in preview.** | FIXED |

## P1 — broken or dishonest, user-reachable

| ID | Day | Sev | Crit | Finding | Status |
|---|---|---|---|---|---|
| A1-04 | A1 | P1 | F | `/api/share/create`: anonymous, NO rate limit, 100KB payloads; `ResumeClient` hydrated EVERY payload key into sessionStorage. **FIXED: 5/hr/IP rate limit + `/resume/[id]` hydrates only `SHARE_KEYS` (extracted to `lib/share-keys.ts`, which had already drifted between two copies).** | FIXED |
| A1-05 | A1 | P2 | F | `/og` renders arbitrary query text under the brand mark — anyone can mint official-looking cards. **Downgraded P1→P2 (brand-abuse, not data breach).** Fix = sign OG URLs (helper + reject unsigned), touches all metadata call sites → decision + A6. | QUEUE |
| A1-06 | A1 | P1 | F | `UNSUBSCRIBE_SECRET ?? "fallback-please-set"` at 4 token sites (nurture-email.ts, negotiation/email-body.ts) = forgeable tokens. **FIXED: `lib/unsubscribe-secret.ts` prefers the env var, else derives a high-entropy secret from the service-role key (always set in prod); literal is dev-only.** | FIXED |
| A1-07 | A1 | P1 | F | `/api/family/digest` + `/api/planning/signup`: anonymous endpoints that email arbitrary addresses (digest carries attacker text in titles); per-instance in-memory rate limits are the only brake; domain reputation risk. Verify limits + consider origin checks. | LEAD |
| A2-02 | A2 | P1 | C | "Reach out to N homes" (9/14/20) in the wizard is template fiction from `sample-homes.ts` `homesForRadius` — contradicts intro's "3–5" and the actual vetted count (possibly 0). **FIXED: every client-side count removed** — radius buttons now say what the radius means ("closest homes only / most of a metro area / the wider region"), submit button carries no number, intro says "the vetted homes near you", /how-it-works "as many as nine" gone. `homesForRadius` survives only as the server-side directory-query CAP (start/route.ts), documented as a cap-not-a-promise; copy survives an empty directory by construction (counts never render). | FIXED |
| A2-03 | A2 | P1 | C | /how-it-works: "Sent from advocate@honestfuneral.co" vs code default `arrangements@` (email-body.ts:116) unless OUTREACH_FROM overrides in prod. **FIXED via single source of truth: the page now renders the address by calling `outreachFromAddress()` itself** (live-verified prod claim was advocate@; page now shows arrangements@ or whatever OUTREACH_FROM sets at deploy). Default pinned by new `lib/negotiation/__tests__/email-body.test.ts`. Replies route via `advocate+id@reply.honestfuneral.co` — unchanged. | FIXED |
| A2-04 | A2 | P1 | C | Closed page claims "We've let the home you chose know" even on the `no_quote` path where `notifyChosenHome` sends nothing; "we'll loop you in once a slot is set" has no mechanism. **FIXED: closed page now mirrors `notifyChosenHome`'s own guards** (OUTREACH_LIVE + chosen row has email + quote + not denylisted) and only claims notification when a send really fired; otherwise: "We haven't sent them anything… call or visit them directly", with matching next-steps list. The no-mechanism "we'll loop you in once a slot is set" removed from both branches. Results-page "we'll notify them" promise same treatment. | FIXED |
| A2-05 | A2 | P1 | C | Message relay stores family messages as sent-looking thread items when paused ("stored but not sent" — no visual distinction); family believes the home received it. **FIXED: `delivered_at` (stamped only on real send) now flows through `/api/negotiate/[id]`; outbound bubbles without it carry a "Saved — not sent to the home" chip.** Composer is honest up front when paused: "Our team isn't relaying messages…", button "Save note". A stored-while-paused message never retro-sends (send path fires on compose only), so the chip is permanently true. | FIXED |
| A2-06 | A2 | P1 | C | Present-tense "funded by the institutions we partner with" on /tell-your-hospice + /for-funeral-homes with zero paying institutions; /our-role claims operational features (messaging thread, meeting scheduling) never exercised by a real case. **FIXED (12 surfaces): the false present-tense funding claim is gone everywhere** — grep for "funded by the institutions" now returns 0 app hits. Canonical honest pattern: "any revenue we earn comes from the institutions that serve families — never from funeral homes or insurers" (true at zero revenue, true after the first payer); /faq "How do you make money?" now answers plainly "Right now, we don't — no one pays us anything yet"; /our-role regulator section states the licensing *model*; /terms §liability cap re-grounded on "you pay us no fees". Swept: how-it-works (meta+step2+money card), faq ×3, terms ×2, about ×2, for-funeral-homes ×2, funeral-home-tactics, homepage card+eyebrow, nurture email ×2, tell-your-hospice mailto, our-role. /our-role "What we do" capability bullets PARKed (they define the legal role boundary and the features exist in code; in-case surfaces now disclose send state) — founder ratifies via PR. | FIXED + PARK |
| A3-01 | A3 | P1 | C | "Typical overcharge $2,000–$5,000" — found on **10 surfaces** (homepage ×3, /partners, /employers, FAQ ×2 incl. FAQPage JSON-LD, nurture email ×2) — uncited on 8; /partners + /employers cited a methodology page that didn't contain the figure; **nurture email converted it to "typical savings" (Utah CSPA §13-11-4(2)(h) exposure, zero instrumented cases)**. **FIXED: new `/methodology#overcharge-figure` derives the band from published dispersion data (own catalog gaps + CFA-50%-on-NFDA-medians + FCA/CFA $2,580–$13,800 spreads) and states it is a projection, not a measured outcome; all 10 instances reworded to "overpayment risk / documented gap" + linked; savings phrasing removed; FAQ "year of living expenses" + "families tell us" + fabricated response-time stats deleted; ProofSheet fake testimonial (person-attributed "$2,000 overcharge" quote) → labeled illustrative scenario.** Evidence: A3-CLAIMS-REGISTER.md §2. | FIXED |
| A3-02 | A3 | P1 | C | /prices displayed zip-ADJUSTED fair ranges beside UNadjusted national predatory thresholds; ALSO /prices + /prep + Cheatsheet + /funeral-homes COLA-adjusted per-unit state fees the analyzer deliberately keeps national; funeral-homes-pricing.ts documented the opposite "raw predatory" judgment; computeOverall's "predatory begins around" cited the band's TOP. **FIXED: `displayThresholds()` in lib/pricing-data.ts is now THE display rule (per-unit → national; else fair AND predatory adjust together — the analyzer's tested rule), applied to /prices, /prep, Cheatsheet, /funeral-homes/[zip], funeral-homes-pricing; rateQuote (lib/price-rating.ts) sets bad at the adjusted predatoryLow the page displays. Tripwires: lib/__tests__/pricing-display.test.ts (analyzer-formula equivalence for all 30 items × 4 zips; catalog ordering invariant; boundary=display pin); funeral-homes-pricing pins recomputed.** Logged on /corrections per the methodology promise. | FIXED |
| A3-03 | A3 | P1 | C | Cross-surface contradictions — confirmed WORSE than seeded: casket savings 40–70 vs 50–80 (×3); dispersion published FIVE ways (2–3× / 3× / 3–10× / 200–400% / "thousands"); certificates 3 counts + 4 price ranges; probate 10-vs-25; 1.7M receive-vs-die conflation; PLUS decide-engine "$1,000–$2,500" surviving the 2026-06-26 catalog tightening, glossary basic-services "$3,800 = normal chain" vs catalog predatory-at-$3,500, embalming stated at 3 strengths, home-funeral 41 (static ×4) vs 42 (computed), how-it-works "nine homes" vs 3–5 everywhere else. **FIXED: one canonical value per fact (register §3); decide-engine + FIVE_QUESTIONS now DERIVE dollars from the catalog so copy can't drift again; dispersion claim canonicalized to FCA/CFA-attributed 200–400%; 1.7M corrected to NHPCO receive + ~half of Medicare decedents.** | FIXED |
| A4-01 | A4 | P1 | V | Outcomes capture has ONE mouth: the closed page, reachable only via choose-a-home (never run live). Abandon / independent-arrangement / no_homes paths are never asked → the moat's data funnel is nearly closed. **FIXED (in-product mouths, 2026-08-18): the ask now exists on every exit path** — `CaseSatisfaction` gained variant props (title/intro/showSurpriseFees; the surprise-fees field hides on never-closed paths because the API stores that text on the CHOSEN outreach row, which those paths lack — never silently drop an answer); `/negotiate/[id]/status` asks on `no_homes_available` (open card, "If you've already made arrangements") and on every open case (quiet `<details>` "Already made arrangements on your own?" — optional, never gating, the case stays open); dashboard outreach card links to `status#outcome` ("note how it turned out — optional"; the hash opens + scrolls to the disclosure). Closed page unchanged. Outcome route verified status-agnostic (zero API change needed); `savings_vs_listed_cents` derives from target-estimate − paid, so an abandoned case with a baseline still yields a savings point for the ≥$1,500 kill gate. **QUEUE (build item): the post-case email nudge** — cron behind its own default-off flag, ONLY to families with an activated case AND `profiles.anniversary_emails_opt_in` (the existing consent flag), single gentle ask linking to `status#outcome`; family-initiated-activation law holds (opt-in follow-up to an activated family, never cold contact). | FIXED + QUEUE |
| A4-02 | A4 | P1 | V | Anonymous analyzer checks (the default; tool never prompts sign-in) persist nothing — the wedge's data job depends on a sign-in funnel the page doesn't push. **FIXED (honest version, 2026-08-18): results still never require sign-in; a soft dismissible "Keep a record of this check?" card now follows every real signed-out result** (never on samples) stating plainly the check lives only on-screen; "Sign in to save it →" stashes text/zip/homeName in sessionStorage (`hf-analyzer:pending-save`), survives the /login round-trip, restores with a "still here — press Analyze" note (explicit re-run; the consent checkbox is deliberately NOT carried — unchecked default, Day-3 rule). Route now reports `saved` truthfully (a row really landed) and the UI says "Recorded privately to your account" only then. **Consent theater removed: the contribute checkbox no longer renders signed-out** — an anonymous check persists no row, so the box collected a consent we silently discarded. Browser-walked end-to-end in dev (stash → /login → restore → one-shot clear). **QUEUE: consented anonymous contribution** (an anonymous consented row needs a schema decision — `user_id NOT NULL` today — plus abuse controls; founder call whether the feed should accept anonymous rows at all). | FIXED + QUEUE |
| A4-03 | A4 | P1 | F | Phase-detector reads `honestfuneral.*` keys but /decide writes `hf-decide:*` — decide signal can never fire; `PhaseGating` has zero consumers (dead chain mounted in layout). **CONFIRMED then KILLED (2026-08-18):** `usePhase()`'s only consumer was PhaseGating; PhaseGating consumed by nothing; the detector's decide keys (`honestfuneral.faith.v1` / `honestfuneral.decide.v1`) are written by nothing anywhere. Deleted `lib/phase-detector.ts` + `components/PhaseContext.tsx` + `components/PhaseGating.tsx`, unwrapped the provider from `app/layout.tsx` — every page had mounted a storage-reading provider + `storage` event listener for zero UI. Grep clean, suite green. (Pre-empts the phase-chain line of A9-02.) | KILLED |
| A5-01 | A5 | P1 | C | /partners still carries the CAHPS / "Medicare Annual Payment Update" pitch card — the retired framing the market-research law bans and PR #167 replaced with referral-reputation. Only live sales surface with it. | LEAD |
| A5-02 | A5 | P1 | F | Pause split: token pages (`lib/partner-auth.ts`) gate only on `active`; session portal also parks `status` paused/archived — a status-paused partner keeps full token access (report, links, check). | LEAD |
| A5-03 | A5 | P1 | U | Token surface has no /materials twin — a coordinator with only the quick link cannot print the kit without a sign-in seat; first pilot friction. | LEAD |
| A6-01 | A6 | P1 | U | `/analyzer` — the wedge product — absent from sitemap AND near-empty metadata (no description, no OG, no canonical) while ~140 glossary pages got full treatment. **FIXED (pulled forward from A6, 2026-08-24 — the wedge is also the data funnel's mouth):** sitemap entry added at 0.9 beside /prices; layout metadata now carries a checker-first description, `alternates.canonical`, and an OG block (set on the layout, not page.tsx, so open PR #190's page edits merge clean — the page only overrides `title`). Remaining for A6: GSC index-coverage baseline, the rest of the sitemap gaps (A6-03), `lastModified` noise. | FIXED (partial → A6) |
| A6-02 | A6 | P1 | U | Measurement unproven: GSC verification + Vercel Analytics are env/dashboard toggles invisible to the repo — reach may be entirely unmeasured. | LEAD |
| A10-01 | A10 | P1 | F | No CI exists (no .github dir) — 638 tests, typecheck, lint, build, guardrail greps run only when a human remembers. **FIXED (partial, pulled forward 2026-08-24 → ci PR): `.github/workflows/ci.yml` runs typecheck + eslint + the full suite + an env-less build on every PR and push to main (env-less on purpose — calm degradation is itself a pinned guarantee; never add prod secrets to CI). Remaining for A10 day: promote the grep gates into the workflow + branch protection.** **UPDATE (tripwires PR, 2026-08-25): grep gates promoted — `lib/__tests__/grep-gates.test.ts` (CAHPS-pitch word-ban with comment-stripping + disclaimer allowance, mutation-tested; `regional_benchmarks` raw-table allowlist) runs inside `npm test`, so CI enforces them on every PR. Remaining: branch-protection setting (founder, GitHub UI).** | FIXED (partial) |
| A10-02 | A10 | P1 | F | Guardrail code without tripwires: vetted-gate `.eq("vetted", true)` unasserted (fake ignores eq args); consent WRITE path (declined-never-persists) untested; `resolvePartnerToken` untested; `OUTREACH_NOTIFICATIONS_ENABLED` untested; no single-send-path architecture test (3 gated sites, convention only). **PARTIAL (2026-08-24): consent WRITE path pinned (7-case route test — absent=decline, declined never legacy-retries, transient errors never launder consent, sample/anon persist nothing; MUTATION-TESTED: deleting the `contributed === true` guard fails exactly the load-bearing pin) → ci PR; `resolvePartnerToken`/`isPartnerParked` pinned in the A5 PR. **UPDATE (tripwires PR, 2026-08-25): the remaining three are DONE — vetted-gate eq assertion (directory test fake now RECORDS filters; mutation-tested: deleting `.eq("vetted", true)` fails); `OUTREACH_NOTIFICATIONS_ENABLED` cron gate (4-case route test: 401 before flag-read, disabled short-circuit touches neither DB nor email, only the literal "true" opens); single-send-path architecture test (`lib/__tests__/send-path-architecture.test.ts` pins ALL 17 sendEmail call sites to a recipient-classed allowlist and asserts every home-directed site carries OUTREACH_LIVE + denylist — a fourth ungated site now fails CI). Remaining for A10 day: eval fixtures (A10-03), rate-limit meta-test generation, smoke-check filter import.** | PARTIAL |
| A10-03 | A10 | P1 | F | Vision extractor (`extract-price-list-image`) — the photo-snap wedge's first impression — has zero eval fixtures; `inbound-quote-parse` also uneval'd AND sends raw FD reply bodies to Claude unredacted (undocumented exception). | LEAD |
| A4-07 | A4 | P1 | F | **NEW (found verifying A4-04's SHARE_KEYS lead).** `/resume/[id]` hydrated EVERY share-link key into sessionStorage while 10 of the allowlisted keys belong to tools that read ONLY localStorage (guidance ×4, next-30-days ×2, notifications, eulogy — plus the two added today): the recipient was told "their answers are saved on this device" while the checklist / guidance progress / who-to-tell tracker restored into a store nothing reads. Only the decide answers + wizard state actually landed. (A1-04 hardened WHICH keys hydrate; nobody checked WHERE.) **FIXED: `lib/share-keys.ts` now records each key's true read store — checked against every tool's own `getItem` call — and exposes `shareKeyTarget()`; ResumeClient writes each key where its tool looks. New `lib/__tests__/share-keys.test.ts` pins the per-key mapping and the injection boundary.** | FIXED |

## P2 — misalignment / value drag

| ID | Day | Sev | Crit | Finding | Status |
|---|---|---|---|---|---|
| A2-07 | A2 | P2 | C | /decide "Nothing is saved" vs sessionStorage persistence read by other pages; "as many as nine homes" claims; decide savings claim needs methodology trace. **PARTIAL FIX: "Nothing is saved" → "Your answers stay on this device (unless you share them)"** (true: sessionStorage, read by /prices prefill + share-link payloads only on explicit share). "As many as nine" was on /how-it-works — killed under A2-02. Savings-claim methodology trace → A3 (it's a numbers-defensibility item; the inline "gap between fair and predatory ranges, based on national benchmarks" phrasing stands until then). | FIXED (partial → A3) |
| A3-04 | A3 | P2 | C | Framing honesty: "2026 averages" over benchmark bands (page + printed cheatsheet); "year of groceries"; "required minimum / what a home must charge"; "This is what most families pay" beside a Modeled badge (calculator + homepage quick check); "Fair total (midpoint)" mislabeling `quoted − savings`; sample bill called "a real quote"; /fair-price-index metadata claimed regional adjustment over a national table; hardcoded metadata dollars. **FIXED: all reworded (register §3 end); cheatsheet now carries benchmark-review date + methodology URL; avg-cost metadata matches catalog.** | FIXED |
| A3-05 | A3 | P2 | C | Citation set verified at source (register §1): **42 CFR 418.64(d) says "up to 1 year" — the sitewide "~13 months Medicare requires" was a misattribution → fixed at all 5 CFR-cited sites + practice-framing clarified elsewhere**; SHED 40% → 37% (2024, attributed); SECURE 50% penalty → 25%/10% (stale law); gift tax $18k → $19k (2026); overdose "100k+/yr, more than guns+cars" → ~70k 2025, comparison dropped; "135 per suicide (CDC)" → Cerel et al. 2019; cremation ~60% → NFDA ~63% 2025; "60% die without a will" → "more than half, per surveys"; ~10% complicated grief → Lundorff 2017 9.8%, attributed; CFA "up to 50%" ✅ confirmed; FCA/CFA dispersion anchors confirmed ($2,580–$13,800). Headstone 30–60% kept as internally-reconcilable under-claim → source hunt queued to A7. | FIXED |
| A3-06 | A3 | P2 | C | /methodology promised every catalog change is logged on /corrections. **Verified: partially honored — the June process entries existed, but the 2026-06-26 direct-cremation fairHigh 2500→2200 tightening was NOT logged, and /corrections overclaimed ("we cite each source on our methodology page", "every range … sourced and dated", "published survey baseline") beyond what /methodology actually provides. FIXED: tightening entry added, this session's threshold-rule change logged, overclaims reworded to match reality.** | FIXED |
| A3-07 | A3 | P2 | C | (new, found in A3 sweep) Hospice directory counts describe a mirror that never shrinks: import retains CMS-dropped rows by design, so "All N Medicare-certified hospices" drifts above CMS truth on every future re-import and de-certified facilities keep rendering "Listed as … Medicare-certified (as of DIRECTORY_AS_OF)". Dated+sourced = defensible today; `DIRECTORY_AS_OF` (lib/hospice-directory.ts:34) is hardcoded with no test/gate. Queue: certification-status or last-seen column on import + pin the constant to the import runbook. | QUEUE (A6/A11) |
| A3-08 | A3 | P2 | C | (new) Per-item source mapping: only the 8 Wave-1 catalog items carry a concrete source annotation (code comment → BENCHMARK_EXPANSION_SPEC); 22 items have none; exactly one source renders publicly (NFDA 2023 $550 on service-facility). /corrections overclaim fixed (A3-06); the right end state is a public per-item source table once SLC GPLs land. | QUEUE (GPL sprint) |
| A3-09 | A3 | P3 | C | (new) Perishable-stat refresh register: aquamation state count, green-burial "~350 certified cemeteries", Open Path $40–$80/$65 pricing, SCI 1,500-homes count, MAID state list, digital-legacy platform steps, glossary cremation-temp/remains-weight (a known GLOSSARY_REVIEW error), `maxSavings` dead field in SERVICE_TOTALS. Each dated-or-hedged today; needs owners + cadence. | QUEUE (A7/A9/A11) |
| A3-10 | A3 | P2 | C | (new) Welcome email promised "one calm email a month … never marketing" while the nurture cadence is day-7 + day-28 and step-2 was promotional; HTML said "ignore this email and we'll remove the address" (no mechanism) while plaintext said "reply". **FIXED: cadence described honestly ("a couple in the first month, then rarely"), both variants now say reply-to-remove (true — founder-monitored inbox), step-2 savings promise removed (A3-01).** | FIXED |
| A4-04 | A4 | P2 | F | Compare-quotes: silent slot drop when one of three quotes fails; one persisted row per quote per click (near-duplicate benchmark feed rows); /family SHARE_KEYS omits plan-now + worksheet. **FIXED (2 of 3, 2026-08-18): a failed quote is now named above the comparison ("Quote N couldn't be read — it isn't in the comparison below") instead of silently rendering a smaller side-by-side; SHARE_KEYS now carries `hf-worksheet-v1` + `honestfuneral.plan-now.v1`** (the two most personal planning tools were silently dropped from shares — and they actually restore now, per A4-07). **QUEUE: near-duplicate `price_list_analyses` rows** (one per quote per Compare click, one per re-Analyze click) — needs a dedupe scope in the benchmark feed (content-hash or client run-token within user) before n counts inflate at promote time; benign today (founder reviews each promotion), matters at scale. | FIXED + QUEUE |
| A4-05 | A4 | P2 | F | Analyzer silent degradation: naiveExtract regex takeover is invisible to the family (extractionMethod exposed only on dev eval runs). **FIXED: `extractionMethod` now returns on every response and the analyzer shows a calm provenance note when the regex fallback read the list ("Read with our simpler backup reader… double-check the items below against your copy") — a silent parser downgrade no longer hides from the family acting on the verdict. Eval harness reads the field optionally (unaffected).** | FIXED |
| A5-04 | A5 | P2 | F | /admin/outcomes headline numbers have no is_test exclusion; demo orgs share prod tables with only DEMO_ORG_MARKER separating them; `partner_leads.handled_at` has no write path (perpetual re-triage); approve seats contact_email verbatim, no confirmation. **FIXED (3 of 4, 2026-08-24): marker promoted to `lib/demo-org.ts` (drift-pinned to seed-demo.mjs by test) and /admin/outcomes headline totals now exclude demo-org-tagged cases (+"demo cases excluded" pill); leads got their write path (PATCH `{leadId, handled}` + mark-handled/reopen button, handled leads dim + sort last). PARK: contact_email verbatim seating — documented as a known edge in `docs/PILOT_ONBOARDING_RUNBOOK.md`.** | FIXED + PARK |
| A5-05 | A5 | P2 | C | `codesWithClaims` (negotiations only) vs report `priceListChecks` show contradictory activity numbers for the same partner. | LEAD |
| A6-03 | A6 | P2 | U | Sitemap gaps: /rights, /our-role, /next-30-days, /tell-your-hospice, /eulogy missing; /briefing (localStorage-empty to crawlers) and /after redirect stub present; `lastModified: new Date()` everywhere. Robots: /api/ disallow blocks the advertised Dataset DataDownload URLs; /signup fossil. | LEAD |
| A6-04 | A6 | P2 | U | 87-city ISR cluster emits no canonicals; /funeral-homes/[zip] = unbounded ~100k thin-page space, indexable, no canonical/noindex, titles promise prices but page lists no homes. | LEAD |
| A6-05 | A6 | P2 | C | article-schema hardcodes datePublished 2026-05-14 across ~23 pages; Dataset license→/methodology; JSON-LD raw-script escape bypass on /fair-price-index; OG tagline predates checker-first positioning. | LEAD |
| A7-01 | A7 | P2 | C | Never-executed human-review gates shipped live: sensitive-grief redline (suicide/overdose/child pages say "Sister to redline" — reviewer model retired) and faith clergy sign-off (pending since 2026-05-21; /admin/faith-qa access model conflicts + localStorage-only state). Decide: commission or formally accept. | LEAD |
| A7-02 | A7 | P2 | U | Planning trio: /plan-now vs /planning vs /plan-ahead — one job, three pages, drifting duplicate copy, colliding footer labels, /where routes to the weakest, guides hub omits the newest. Merge decision. | LEAD |
| A7-03 | A7 | P2 | C | /headstone-vendors claimed "vetted monument companies" / "every vendor verified by our team" / "independently verified" (×3) while lib/cemetery-vendors.ts says re-verification never happened (ftcVerified never set); 18 vendors ≠ "serving your area". **A3 partial fix: all three verification claims removed ("starting points, not endorsements — we take no fee").** Remaining for A7: click-test the 18 vendors or decide the directory's fate; source the 30–60% figure. | LEAD (copy FIXED by A3) |
| A8-02 | A8 | P2 | C | Bundled legal-doc drift. **PARTIAL: `PRIVACY_RETENTION.md` updated (post-pivot classes added: partner attribution, SMS, share/household links, ip_hash, ai_cost_events; Stripe line + deletion caveats corrected; photos-never-stored noted) → FIXED. `LAWYER_OUTREACH.md` staleness banner added reconciling to the pre-drafted `docs/legal/` package (PR #184) and fixed-fee-review posture → FIXED. `ANTI_STEERING_EVIDENCE.md` re-verified live + drifted line anchor fixed → FIXED. `/terms` §8 arbitration clause left untouched (self-labeled "do not change without legal sign-off") → QUEUE(counsel). Entity LLC-vs-C-corp resolved by `ENTITY_PLAN_DRAFT` (PR #184) → noted. /terms had no fee-era fossils (grep-clean). | FIXED + QUEUE |
| A9-01 | A9 | P2 | C | Unmarked-stale docs misdirect agents: PLAN_OF_ATTACK / ATTACK_PLAN / EXECUTION_PLAN (no banners), ENGINEERING_BACKLOG (#1 item shipped), TRUST_SPINE ("$49 verified on main" — false), MARKETING_AUTHORITY (Index "not built"), SCORECARD (frozen 6/24), GO_TO_MARKET ($49 live, Index deferred), ROADMAP top half ("L3 does not exist"). BUSINESS_PLAN fork: canonical v2 sits on open PR #167. Stale PR #127. | LEAD |
| A9-02 | A9 | P2 | F | Dead code: commercial-suppression system (designed grief-safety feature silently inert — mapper disagreement on whether proxy.ts sets the cookie: resolve, then wire or delete), PriceTable, content.ts dead exports, sample-homes findHomes, partner-digest legacy branch, analyzer legacy-shape fallback (post-Migration-A), lib/partner-auth vs lib/partner/auth split. | LEAD |
| A9-03 | A9 | P2 | C | Payment-era naming/copy fossils: isPaidUser, gateUntilPaid, send.ts "paid negotiation" alert, notify-chosen-home Stripe docstring, outreach-preview "selects them and pays", dashboard stripe columns select, seed README, schema "Walk Beside" header, TODO-FD/"sister" comments, wb_ localStorage prefix. | LEAD |
| A8-05 | A8 | P2 | F | **NEW.** Account deletion (`/api/account/delete` → `auth.admin.deleteUser`) cascades all `auth.users`-owned tables, but `share_links` / `household_links` are anonymous (no user FK) so they survive; there is **no purge cron** (`vercel.json` has none), so an expired row persists indefinitely. A family who deletes their account reasonably expects these gone. `/privacy` now discloses this honestly; the fix (a scheduled purge of expired anonymous links) is a build item. | QUEUE |
| A8-06 | A8 | P2 | F/C | **NEW (relevant once OUTREACH_LIVE flips).** Inbound Postmark webhook stores the full provider payload verbatim (`negotiation_messages.raw_payload`, "for debugging") including any attachment base64, with no retention limit; meanwhile `/for-funeral-homes` tells homes "A PDF attachment is fine" but the 100KB `readLimitedJson` cap 413-rejects any real PDF, so that reply silently never lands. Reconcile the copy with the cap and stop persisting raw attachment bytes. | QUEUE |
| A4-08 | A4 | P2 | F | **NEW.** A signed-in visitor clicking the analyzer's "see it work on a sample" persisted the fake $10,730 demo bill as a real `price_list_analyses` row on their account — flipping the dashboard's has-checked-prices task state and polluting their own case record (contributed=false kept it out of benchmarks). **FIXED: the client marks demo runs (`sample: true`) and the route skips persistence entirely for them; `saved` reports false so no UI claims a save.** | FIXED |

## P3 — polish / hygiene

| ID | Day | Sev | Crit | Finding | Status |
|---|---|---|---|---|---|
| A1-08 | A1 | P3 | F | Postmark webhook Basic-Auth compare not timing-safe; resend-webhook no timestamp freshness (replay = re-deactivate); rate-limiter per-instance (documented). | LEAD |
| A4-06 | A4 | P3 | C | resume/[id] "Pick up where she left off" gendered metadata; negotiate/error.tsx payment-era copy + false "your requests still go out". **FIXED: metadata title → "Pick up where they left off"; the negotiate/error.tsx half was already fixed in passing by A2.** | FIXED |
| A5-06 | A5 | P3 | C | PartnersClient + materials handoff copy hardcode honestfuneral.co / "Honest Funeral" (brand-constant rule); "We run every case by hand" will silently become false at scale. | LEAD |
| A6-06 | A6 | P3 | C | DIRECTORY_AS_OF "July 2026" hardcoded (CMS ~quarterly refresh); PRICING_LAST_UPDATED 2026-06-26 aging on the citable Index; hospice state-page indexed titles embed live counts. Schedule the refresh clocks (A11). | LEAD |
| A7-04 | A7 | P3 | C | Glossary "call us" with no phone path; livestream password in plaintext localStorage; Miami blurb promises Spanish services on an English-only product; i18n-es drafts drifting from evolved EN pages. | LEAD |
| A9-04 | A9 | P3 | F | No /admin index page — nine tools, no cross-links; founder memorizes nine URLs. | LEAD |
| A8-07 | A8 | P3 | F | **NEW.** `planning_signups.ip_hash` is an **unsalted** SHA-256 of the client IP stored indefinitely alongside the full user-agent — pseudonymized, not anonymized (an IPv4 space is brute-forceable). Salt it (or drop it and keep only a coarse rate-limit signal). | QUEUE |
| A8-08 | A8 | P3 | C | **NEW.** Vestigial `price-lists` Supabase Storage bucket is still named in `supabase/schema.sql`, `BOOTSTRAP.sql`, `docs/PROD_SETUP.md`, `docs/LAUNCH_CHECKLIST.md`, but **no code reads or writes it** (photos are never stored). A reviewer diffing infra against `/privacy` finds a bucket literally named for uploaded price lists. Remove the setup step / bucket. → A9 docs sweep. | QUEUE |

## Verified / closed

### A5 (2026-08-24) — pilot readiness, code side
| ID | Item | Disposition |
|---|---|---|
| A5-C1 | **A2-09 closed: prepared-only cases now have a go-live path.** New admin-gated `POST /api/admin/negotiations/rerun` resets a case's terminal `dry_run` rows to `pending` and re-invokes the ONE gated send path — with `OUTREACH_LIVE` off it harmlessly re-records `dry_run`, so the endpoint cannot leak mail; the env flip stays the only send switch. Kill-switch census unchanged: all home-directed mail still flows through the three gated sites. | FIXED |
| A5-C2 | **Exit-gate deliverable:** `docs/PILOT_ONBOARDING_RUNBOOK.md` — the yes-to-activation path with every switch and gate named (org creation → approve → coordinator setup → activation → reporting → the OUTREACH_LIVE flip incl. re-run procedure → billing), plus the accepted known edges. | DONE |
| A5-C3 | **The prod dress rehearsal itself stays FOUNDER-gated** (admin approve sends a real email; needs a founder-owned test org + ADMIN_EMAILS): apply → approve → codes → activation → attribution → portal AND token views → materials print → digest `?test=` → pause both-gates check. Run it per the runbook before the first real pilot; the code-side holes it was meant to catch (pause split, materials twin, handled_at, demo pollution) are fixed this session. | FOUNDER |
| A5-C4 | Billing walk (Day-8 Stripe test flow) untested live — machinery verified inert (`BILLING_LIVE` unset); walk it with the test org when Stripe test envs are set. `lib/__tests__/billing-guardrails.test.ts` still pins the guardrails. | FOUNDER |

### A4 (2026-08-18) — L2 funnel + data moat: verifications and dispositions
| ID | Finding | Evidence / disposition |
|---|---|---|
| A4-C1 | **Consent WRITE path verified in code (the Day-3 load-bearing rule holds):** the insert stores `contributed ?? false`; the legacy pre-migration fallback (`analyze-price-list/route.ts` ~line 350) runs ONLY when `missingColumn && contributed === true` — a declined analysis cannot persist through either path, and a transient insert error never retries (won't launder the flag). Migration A applied in prod → the fallback should be dead code there; deliberately KEPT as the safety floor. Removal scheduled to the A9 dead-code sweep only AFTER A10 lands the consent write-path test (read-edge exclusion is already pinned by benchmark-sources.test.ts; the write path has no tripwire — A10-02). | Code read 2026-08-18; no change |
| A4-C2 | **Outcome route is status-agnostic by design** — any owned case can record (the new A4-01 mouths needed zero API change). Cohort surfaces (`/admin/outcomes` headline + client, `lib/partner/report-data.ts`, partner-digest cron) all key on `outcome_recorded_at IS NOT NULL` alone, so open-case / no_homes outcomes join every aggregate automatically; quote-less cases carry null `savings_vs_listed_cents` and drop out of savings math (no distortion). A10 should pin the no-status-filter contract when it builds the outcome-route test. | Code read of all 4 cohort readers |
| A4-C3 | **Funnel-walk coverage (exit-gate honesty):** analyzer funnel browser-walked signed-out in dev — paste → results → save prompt → "Sign in to save it" → /login round-trip → restore-with-note → one-shot stash clear; sample → no-persist + no-prompt verified; contribute box absent signed-out verified. Negotiate wizard steps 1–8 were browser-walked by A2 (honest mode). The NEW status-page/dashboard outcome UI is typecheck/build/suite-green but auth-gated — **signed-in browser walk pending: verify on the PR preview with a founder session** (open a no_homes case + an open case, check both asks + the dashboard link + `#outcome` anchor). | Dev browser walk 2026-08-18 |
| A4-C4 | **Preferences UUID-as-capability** (deferred here by A1-03): unchanged — a leaked `/preferences/[id]` link still lets the holder set the SMS number. QUEUE a founder decision: accept-and-document (link is unguessable, page holds no PII beyond the toggle) or add a confirm step to the SMS write. Not fixed today: any change touches the bereavement contact surface, which deserves its own careful pass. | Deferred → QUEUE (founder) |
| A4-C5 | **A2-09 pointer (dry_run rows are terminal):** unchanged by design today; the go-live runbook (admin re-run of prepared rows) belongs to A5's dress rehearsal. Note: the new outcome mouths reduce what's lost when a prepared-only case never converts — the family can still hand back the outcome. | Standing note for A5 |

### A3 (2026-08-18) — verified CLEAN (no action needed)
| ID | Finding | Evidence |
|---|---|---|
| A3-C1 | **Badge honesty holds with the verified tier empty** — live prod probe: Index data endpoint 30 national items / 0 overrides / lastUpdated 2026-06-26; tier API returns `modeled` (SLC). Every surface's verified/community rendering is gated on data presence (quick-check hardcodes modeled; city-page verified section on rows>0; zip page on overrideRowCount>0; analyzer on applied overrides; /prices note on non-modeled tier) — with zero GPLs ingested, no surface implies home-level verified data. | Live probe 2026-08-18 + code read of all 6 badge sites |
| A3-C2 | **Checker invariants intact** — no COLA on per-unit/state fees (checker-pipeline test green; now ALSO enforced on the display surfaces via displayThresholds), FTC under-claiming verdict language ("likely violation"/"worth confirming") unchanged, /methodology cross-links present on every tier badge + price surface, coverage signal (assessCoverage notes) unchanged. Full suite 711/711 green after the A3 changes. | vitest run 2026-08-18 |
| A3-C3 | CFA "up to 50%" comparison-shopping claim (/planning) verified against CFA publications — kept, already attributed. The "18% post prices online" stat renders NOWHERE live (docs only, correctly attributed to CFA 2022 there). | WebSearch verification 2026-08-18 |
### A2 (2026-08-18) — honesty pass: decision memo + new findings
**Outreach-posture decision memo (the day's centerpiece).** Implemented posture:
**honest-mode default** — the negotiate flow stays open, and when `OUTREACH_LIVE`
is off every surface describes what really happens: we prepare the family's
outreach (vetted homes + the exact request text, shown to the family), price
analysis, and documentation, and we say plainly that nothing is sent. In-case
language derives from **actual send state** (`anyOutreachSent` over row statuses),
not from the flag, so it stays correct in mixed/legacy states and after any
future flip. Alternatives flagged for the founder in the audit(a2) PR:
(a) hard-gate flow entry behind a "paused" interstitial (rejected as default:
the prepared-materials + record-your-own-quotes path has real value and real
instrumentation), (c) flip OUTREACH_LIVE for a bounded region (blocked on the
GPL/vetting sprint + founder go; selling starts ~Sep 8). Live-prod claims were
verified verbatim before fixing (honestfuneral.co/how-it-works still served
"advocate@…", "as many as nine", "You approve the list").
| ID | Finding | Evidence / disposition |
| A2-08 | **NEW (guardrail-#1 adjacent): /faq "How do you make money?" claimed funding by "hospices today, employers and insurers over time"** — insurers-as-payer contradicts guardrail #1 and the same paragraph's own "we never take money from insurers". Also "Why did my hospice recommend" claimed "Hospices and employers pay us" (zero payers exist). | **FIXED** in the A2-06 sweep: insurer removed from every funding sentence; both FAQ answers rewritten to the honest model framing. |
| A2-09 | **NEW (mechanism, founder should know): `dry_run` outreach rows are terminal.** `sendOutreachForNegotiation` only processes `pending` rows, so a case created while paused never sends — even after OUTREACH_LIVE flips on. Any family who completes the flow before go-live needs an admin re-run (rows reset to `pending`) or stays prepared-only. The new UI states this truthfully ("Prepared — not sent"); nothing falsely implies a later send. | Documented; no code change (send-path behavior untouched by audit design). Re-check in A4/A5 when designing the go-live runbook. |
| A2-10 | **NEW: /how-it-works step 1 claimed "You approve the list before anything goes out"** — no approval step exists (submit triggers send in the same request). | **FIXED**: step reworded to what's real (written authorization gates outreach; family sees every home by name on the case page). Also fixed there: fabricated "The prices we get back tend to reflect that" experience claim (zero sends ever) → design claim. |
| A2-P1 | **PARK (founder ratifies via PR): L1/CTA service-description copy** still says "we contact funeral homes on your behalf" on marketing surfaces (homepage, /prices + calculator, /decide CTA, /funeral-costs + 87 city pages, /average-funeral-cost, /faq, /planning, /employers, dashboard CTA card, nurture promo sentence, portal one-pager "help you get comparison quotes" [reworded]). Rationale: these describe the standing service, every path lands on the wizard which now discloses the pause before any commitment, and mode-aware rewrites of ~15 static/ISR surfaces would be reverted within weeks of go-live. Alternative (mode-aware CTAs) flagged in the PR. | PARK pending founder; revisit at go-live or A11. |
**Also fixed in passing:** `/negotiate` error boundary's false "your requests
still go out" + payment-era docstring (was queued under A4-06); negotiate
layout metadata reworded to durable framing; `sample-homes.ts` docstring no
longer claims client usage. **Verified:** typecheck ✓ · eslint ✓ · build ✓ ·
**710 tests** (702 + 8 new: status-label jargon tripwires, from-address pin) ✓ ·
dev-browser walk of wizard steps 1–8, /how-it-works, /decide in honest mode ✓.

### A1 (2026-07-27) — verified CLEAN (no action needed)

| ID | Finding | Evidence |
|---|---|---|
| A1-C1 | **RLS airtight across all 19 tables** — anon key gets 0 rows from every owner-scoped/deny-all table incl. `hospices` (~6,852 rows, proving denial not emptiness); non-granted `funeral_homes` columns → 401; anon INSERT → 401 RLS violation. ⚠️ **Corrected by A8-03:** `share_links` was NOT deny-all — it carried an anon `select` policy with no id predicate (enumerable in bulk). The A1 probe read its empty result as denial when it was emptiness (0 non-expired rows at probe time). Closed in A8. | Live anon-key probe of prod `bhadjvukoyvfbzbcqunp.supabase.co` |
| A1-C2 | **Outreach kill-switch holds** — all 3 home-directed sends gated on `OUTREACH_LIVE`; the 18-site `sendEmail` census found no ungated home send (`notify-family-of-reply` is family-directed). | Code review + grep |
| A1-08 | Postmark webhook non-timing-safe compare; Resend webhook no timestamp-freshness (replay = re-deactivate inactive home, negligible). Batched to A10. | Code review → QUEUE |
